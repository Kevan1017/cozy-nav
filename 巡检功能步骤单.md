# 巡检功能实施步骤单

> 对应《开发方案.md》第十章「开发点 #09：链接可用性检测 · 二期定期巡检」
> 目标：每 6 小时定时巡检 + fail_streak 连续失败自动判死 + TLS 到期告警 + 健康总览 + 冷链接筛选
> 8 个可配置项：`enabled / intervalHours / batchSize / deadStreak / recheckDead / tlsYellowDays / tlsRedDays / coldDays`
> 预计工时：约 6h。每步完成即验证，全部通过后整体回归。

---

## 步骤 1：数据库迁移

**改动文件**：`server/src/db/index.js`（`migrateDatabase()` 末尾追加，仿照现有 417-433 行幂等 try/catch 模式）

| 迁移内容 | SQL |
|---|---|
| 连续失败次数 | `ALTER TABLE links ADD COLUMN fail_streak INTEGER DEFAULT 0` |
| 证书到期日 | `ALTER TABLE links ADD COLUMN tls_expires_at DATETIME` |
| 巡检配置 | `ALTER TABLE preferences ADD COLUMN health_config TEXT` |
| 巡检索引 | `CREATE INDEX IF NOT EXISTS idx_links_last_check_at ON links(last_check_at)` + `CREATE INDEX IF NOT EXISTS idx_links_health ON links(health_status)` |

**验证**：重启后端无报错，日志出现"已添加"输出。

---

## 步骤 2：检测服务增强（utils/linkHealth.js）

**改动文件**：`server/src/utils/linkHealth.js`

1. 新增 `getTlsExpiry(url)`：Node 原生 `https.request` 做 HEAD 握手，读 `res.socket.getPeerCertificate().valid_to`，5s 超时，失败返回 `null`（undici 的 fetch 不暴露证书，故单独实现）
2. `checkLinkHealth()` 开头接入 SSRF：`if (!(await isPublicHostname(parsed.hostname)))` → 返回 `{ status:'skip', note:'内网地址，跳过检测' }`（复用 `utils/ssrfGuard.js` 的 `isPublicHostname`）
3. 返回结构增加 `tlsExpiresAt` 字段

**验证**：手测 `https://www.bing.com` 返回 `tlsExpiresAt`；手测 `http://127.0.0.1` 返回 skip。

---

## 步骤 3：共享批量执行器（healthRunner.js 新建 + linkController 改造）

**改动文件**：

- **新建 `server/src/utils/healthRunner.js`**：
  - `createBatchTask(total)`：内存态任务对象（搬入现有 linkController 的 batchTask 逻辑）
  - `runHealthBatch(links)`：并发 4 + 同域名节流 500ms + 每条持久化 + 进度回调 + 任务锁 `activeTask`
- **`server/src/controllers/linkController.js`**：
  - 删除本地 `batchTask` / `BATCH_CONCURRENCY` / `BATCH_MAX`
  - `checkAllLinks` 改调 `runHealthBatch`（去掉 500 条上限改全量）
  - `getCheckProgress` 读 healthRunner 状态
  - `persistHealth` 升级为连续失败规则：

| 本次结果 | fail_streak 处理 | health_status |
|---|---|---|
| ok / blocked / skip | 归 0 | ok / blocked / skip |
| fail | +1 | fail（<3）→ **down**（≥3） |

  - 同时写入 `tls_expires_at`

**验证**：`check-all` + `check-progress` 回归正常；连续失败 3 次的测试链接变为 down。

---

## 步骤 4：定时巡检调度器（scheduler.js 新建 + app.js 挂载）

**改动文件**：

- **新建 `server/src/utils/scheduler.js`**：`startHealthPatrol()`
  - 每周期从 `preferences.health_config` 读配置；`enabled=false` 时跳过
  - 分片查询：`WHERE deleted_at IS NULL AND (health_status IS NULL OR health_status != 'down') ORDER BY last_check_at ASC LIMIT ?`（条数用配置 `batchSize`）
  - 调 `runHealthBatch`；`patrolRunning` 防重入
  - 日志格式：`[时间][巡检][执行][成功] ok=x blocked=y fail=z down=w`
- **`server/src/app.js`**：import 并调用 `startHealthPatrol()`（与现有 `setInterval(cleanExpiredFailedRecords)` 并列）

**验证**：临时把间隔改为 10 秒触发一轮，日志输出正确。

---

## 步骤 5：统计接口（statsController.js + routes/stats.js）

**改动文件**：

- **`server/src/controllers/statsController.js`** 新增：
  - `getHealthOverview`：返回 `{ total, ok, blocked, fail, down, skip, unknown }` + `tlsExpiring`（按配置 `tlsYellowDays/tlsRedDays` 分黄/红，含 id/name/domain/tls_expires_at）+ `failTop`（fail_streak≥1，含 id/name/domain/fail_streak/last_check_at）
  - `getColdLinks(days)`：N 天未访问或访问极少的链接，按最后访问升序
- **`server/src/routes/stats.js`**：新增 2 条 GET
  - `GET /api/stats/links/health`（鉴权）
  - `GET /api/stats/links/cold?days=90`（鉴权，`query('days')` 校验 1-365 整数）
  - 均挂 `authMiddleware` + `validate`

**验证**：curl 带 token 验证 JSON 结构与字段校验。

---

## 步骤 6：preferences 读写（prefsController.js + routes/preferences.js）

**改动文件**：

- **`server/src/controllers/prefsController.js`**：
  - `PREFS_COLUMNS` 加 `health_config`
  - `getPreferences` 返回时 JSON.parse（仿 `parseCustomTheme`）
  - `updatePreferences` 接收 `health_config` 并 `JSON.stringify` 存入
- **`server/src/routes/preferences.js`**：`body('health_config')` 增加 custom 校验：
  - `enabled` 布尔；`intervalHours` ∈ {2,6,12,24}
  - `batchSize` 20-500；`deadStreak` 2-10
  - `recheckDead` 布尔；`tlsYellowDays` 7-90；`tlsRedDays` 1-30；`coldDays` 30-365

**验证**：PUT 保存后 GET 读回一致；非法值返回 400。

---

## 步骤 7：前端（SettingsHealth.vue + LinkManage + Dashboard）

**改动文件**：

- **新建 `web/src/components/admin/settings/SettingsHealth.vue`**：「🩺 链接巡检」卡片
  - 8 项表单（switch / select / input-number）
  - 保存按钮（调 `web/src/api/prefs.js` 的 `updatePrefs`）
  - 立即巡检按钮（调 `linkApi.checkAll()` 并显示进度）
- **`web/src/views/admin/Settings.vue`**：引入并挂载 `<SettingsHealth />`
- **`web/src/views/admin/LinkManage.vue`**：`healthTag` 加 `down`（🔴 死链）；健康列 tooltip 补「连续失败 N 次 / 证书 M 天后过期」
- **`web/src/views/admin/Dashboard.vue`**：新增健康总览卡片（ok/blocked/fail/down 数量 + TLS 即将到期 TOP5）

**验证**：`npm run build` 通过；保存配置立即生效。

---

## 步骤 8：整体回归

- 后端重启，确认调度器启动日志
- 前端功能回归：单条检测 / 批量检测 / 设置保存 / 移动端设置页
- 更新《开发方案.md》开发点 #09 状态（可选项）

---

## 改动清单汇总

| 类型 | 文件 | 动作 |
|---|---|---|
| 后端 | `server/src/db/index.js` | 迁移（2 字段 + 1 配置 + 2 索引） |
| 后端 | `server/src/utils/linkHealth.js` | 增强（TLS + SSRF + dead） |
| 后端 | `server/src/utils/healthRunner.js` | **新建**（共享批量执行器） |
| 后端 | `server/src/controllers/linkController.js` | 改造（checkAllLinks / persistHealth） |
| 后端 | `server/src/utils/scheduler.js` | **新建**（定时巡检） |
| 后端 | `server/src/app.js` | 挂载调度器 |
| 后端 | `server/src/controllers/statsController.js` | 新增（health / cold） |
| 后端 | `server/src/routes/stats.js` | 新增 2 条路由 |
| 后端 | `server/src/controllers/prefsController.js` | health_config 读写 |
| 后端 | `server/src/routes/preferences.js` | health_config 校验 |
| 前端 | `web/src/components/admin/settings/SettingsHealth.vue` | **新建**（巡检设置卡片） |
| 前端 | `web/src/views/admin/Settings.vue` | 挂载子组件 |
| 前端 | `web/src/views/admin/LinkManage.vue` | down 状态 + tooltip |
| 前端 | `web/src/views/admin/Dashboard.vue` | 健康总览卡片 |
