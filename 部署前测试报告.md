# cozy·nav 悦行导航 · 部署前系统测试报告

> 测试时间：2026-08-10 ｜ 测试环境：Windows 本地（后端 `localhost:8000` / 前端 dev `localhost:5173`）
> 测试方式：后端 API 自动化脚本（59 项用例）+ 浏览器端到端（前台 17 项 / 后台全流程）+ 安全静态审查 + 动态攻击验证 + 生产构建验证

---

## 一、测试概览

| 维度 | 结果 | 说明 |
|------|------|------|
| 后端 API 功能 | ✅ 55 通过 / 4 失败 | 4 项中 2 项为真实问题、2 项为文档与实现差异 |
| 前端前台功能 | ✅ 17 项全部通过 | 首页/搜索/置顶/主题/字体/响应式 |
| 前端后台功能 | ✅ 全流程通过 | Dashboard/分类/书签/引擎/设置/健康检测 |
| 安全测试 | ⚠️ 无高危漏洞 | SSRF（M3/M4）已修复；剩余 1 项部署配置缺失（S1，严重，部署前须配置） |
| 性能测试 | ✅ 达标 | 首页 load 315ms；生产构建成功（24.86s） |
| 兼容性/响应式 | ⚠️ 部分覆盖 | PC 1440 / 移动 616 通过；Firefox/Safari/真机未实测 |
| 生产构建 | ✅ 通过 | 有 chunk 体积警告（非阻塞） |

**问题总数：16 项**（严重 1 / 一般 4 / 轻微 11）；除 S1（部署配置，上线前必做项）外，M1-M4 与 L1-L11 已于 2026-08-10 全部修复/复核

---

## 二、问题清单

### 🔴 严重（部署阻塞项）

| # | 问题描述 | 复现步骤 | 预期结果 | 实际结果 |
|---|---------|---------|---------|---------|
| S1 | **部署环境配置不完整**：`server/.env` 缺 `NODE_ENV=production`、缺 `CORS_ORIGIN`，`JWT_SECRET` 仍为默认值 `cozy-nav-secret-change-me` | 检查 `.env`；生产模式下请求带任意 Origin | 生产模式强制：默认 JWT_SECRET 拒绝启动、CORS 白名单收紧、错误信息脱敏 | 当前实际运行在开发模式：CORS 全开（`Access-Control-Allow-Origin: http://evil.com` 被回显）、错误信息含内部细节。**部署前必须配置** |

### 🟠 一般

| # | 问题描述 | 复现步骤 | 预期结果 | 实际结果 |
|---|---------|---------|---------|---------|
| M1 | **PUT /api/links/:id 校验与控制器逻辑矛盾**（`routes/links.js` 强制 `name.notEmpty()`，但控制器已支持 `name ?? existing.name` 部分更新） | 仅传 `{url}` 调用 `PUT /api/links/:id` | 部分更新成功，其余字段保留 | ✅ **已修复（2026-08-10）**：`name` 改为 `optional().notEmpty()`；实测只传 url → 200 且 name 保留、空 name → 400、全量更新正常 |
| M2 | **字体 CDN 不可达**：kai/serif 字体加载源 `fonts.loli.net` 在国内无法访问（`net::ERR_ABORTED`），`loadWebFont` 抛错进控制台 | 设置页或顶栏切换到「楷体/宋体」 | 字体正常加载 | ✅ **已修复（2026-08-10）**：字体本地化，改为 npm 包动态 import（kai=`lxgw-wenkai-webfont`，serif=`@fontsource/noto-serif-sc`），woff2 全从本站加载；实测 kai/serif 切换 0 外部请求、0 控制台报错、`--app-font` 正确 |
| M3 | **SSRF（需鉴权）**：`GET /api/links/fetch-title?url=` 对任意 URL 直接 `fetch`（含 `redirect:follow`），可探测内网 | 带管理员 token 请求 `fetch-title?url=http://127.0.0.1:8000/api/health` | 拒绝内网地址 | ✅ **已修复（2026-08-10）**：路由加 `query('url').isURL()` 校验（仅 http/https）；控制器出站前经 `isPublicHostname` 逐跳校验 hostname（内网/回环/链路本地黑名单），`redirect` 改 `manual` 手动跟随（最多 5 跳）逐跳校验；实测 `127.0.0.1:8000/api/health` → 400「该地址不允许访问（内网地址）」、`https://example.com` → 200 正常取标题 |
| M4 | **SSRF（公开无鉴权）**：`GET /api/favicon?domain=` 的 `normalizeDomain` 未校验 hostname 合法性，会对任意域名发起出站请求 | `?domain=127.0.0.1:8000` | 拒绝非法 host | ✅ **已修复（2026-08-10）**：新增 `isValidPublicDomain`（拒绝 IP 直连/端口/路径/查询/@ 分隔符绕过 + DNS 解析内网黑名单），`fetchFromSources` 统一兜底校验，公开接口与手动抓取入口快速拒绝；实测 `127.0.0.1:8000`/`192.168.1.1`/`8.8.8.8`/带端口路径 → 400、`example.com` → 200 正常 |

### 🟡 轻微

| # | 问题描述 | 复现步骤 | 预期结果 | 实际结果 |
|---|---------|---------|---------|---------|
| L1 | 导出书签 HTML 时**分类名未转义**（链接 name/url 已转义） | 创建分类名含 `<script>`，导出 Netscape HTML 并打开 | 分类名被 HTML 转义 | ✅ **已修复（2026-08-10）**：分类名同样过 `escapeHtml`；实测含 `<script>alert("x")</script>` 的分类导出后为 `&lt;script&gt;alert(&quot;x&quot;)&lt;/script&gt;`，无原始脚本残留 |
| L2 | 业务错误消息拼接 `e.message`，可能泄漏 SQL 约束等内部细节 | 触发数据库约束错误 | 返回统一文案，细节仅写日志 | ✅ **已修复（2026-08-10）**：`engineController`/`importExportController`/`linkController`（fetch-title）的 catch 分支统一改为固定文案，`e.message` 仅写服务端日志 |
| L3 | import overwrite 策略使用物理 `DELETE FROM links`，绕过软删除规范 | 导入数据选择「覆盖」策略 | 软删除标记 | ✅ **已修复（2026-08-10）**：改为 `UPDATE links SET deleted_at=? WHERE category_id=? AND deleted_at IS NULL` 软删除；实测旧链接软删除保留、新链接正常插入 |
| L4 | 导入导出接口（importExport.js）无 express-validator 校验 | 构造非法导入参数 | 400 校验错误 | ✅ **已修复（2026-08-10）**：`/api/import/json` 校验 `strategy∈{skip,overwrite}` 与 `categories` 非空数组、`/api/import/bookmarks` 校验 `html` 非空；实测非法 strategy → 400「strategy 仅支持 skip 或 overwrite」 |
| L5 | 错误密码登录返回 HTTP 400 而非 401 | `POST /auth/login` 错误密码 | 401 Unauthorized | ✅ **已修复（2026-08-10）**：最终采用「HTTP 200 + 业务码」方案——登录失败返回 200 `{code:401, message:'用户名或密码错误'}`（与主流网站一致，浏览器控制台不出现接口信息），前端按 `body.code` 判断并提示；实测错误密码 → HTTP 200 `{code:401}`、正确密码 → HTTP 200 `{code:200}` |
| L6 | `GET /api/links/fetch-title` 无编码检测，对 GBK 页面（百度等）标题解析失败 | `fetch-title?url=https://www.baidu.com` | 返回页面标题 | ✅ **已修复（2026-08-10）**：新增 `decodeHtml`——按 Content-Type charset / HTML meta charset / BOM 检测编码，`TextDecoder` 支持 GBK，utf-8 乱码时回退 gbk；实测 GBK 站 `news.sina.com.cn` → 200「新闻中心首页_新浪网」 |
| L7 | `GET /api/links` 无 GET 列表路由，未鉴权访问返回 404 HTML 而非 401 JSON | 无 token 请求 `GET /api/links` | 401 JSON | ✅ **已修复（2026-08-10）**：全局 404 对 `/api` 路径返回统一 JSON；实测无 token `GET /api/links` → 404 JSON「接口不存在」（路由不存在返回 404 语义正确） |
| L8 | ECharts 按需引入缺 `LegacyGridContainLabel` 插件 | 打开 Dashboard | 无告警 | ✅ **已修复（2026-08-10）**：Dashboard.vue 注册 `LegacyGridContainLabel`（echarts/features）；生产构建通过 |
| L9 | naive-ui 弹窗关闭偶发失效（自动化环境疑似 artifact） | 引擎管理新增保存成功 / 批量检测确认弹窗点取消 | 弹窗自动关闭 | ✅ **已复核（2026-08-10）**：EngineManage 新增保存成功后 `showModal=false`、批量检测 `dialog.warning({negativeText:'取消'})` 代码逻辑均正常，判定为自动化环境遗留弹窗 artifact，非真实 bug |
| L10 | 主题切换背景色存在 transition 滞后（自动化环境 visual 级） | 设置页切暗色后立即读 body 背景 | 立即变暗 | ✅ **已确认非 bug（2026-08-10）**：`transition: background .4s` 为刻意过渡动画，"立刻读取"读不到终值属预期，逻辑正确，仅记录 |
| L11 | 开发方案文档与实现存在差异：统计字段名（`linkCount/categoryCount`）、trend 键（`day/count`）、导入导出接口实际无 `/data` 前缀 | 对照 `开发方案.md` 与实际响应 | 文档一致 | ✅ **已修复（2026-08-10）**：同步 `开发方案.md` 第五章（overview 字段、trend 返回结构、`/api/export/*` 路由、统计接口字段名）；统一保险库锁定提示文案（后端「请先在网站设置中设置保险库密码」与前端一致） |

---

## 二·五、问题详细解释

> 补充登记：2026-08-10（应要求将问题逐项展开，含成因与修复方案）

### 🔴 严重

**S1 · 部署环境配置缺失**
- **是什么**：`server/.env` 未设置 `NODE_ENV=production`、`CORS_ORIGIN`，`JWT_SECRET` 仍为默认值。
- **成因**：代码中的所有生产防护均为"条件触发"——只有 `NODE_ENV=production` 才生效（[app.js:28](file:///d:/PersonHub/server/src/app.js#L28) 不设即默认 development），当前实际跑在开发模式，防护全部旁路。
- **连带后果**：① CORS 全开（`origin:true`，实测任意 Origin 被回显，可跨域带凭据调 API）；② 错误信息返回完整 `err.message`（绕过生产脱敏）；③ 默认 JWT_SECRET 是**签名登录/保险库 token 的唯一密钥**，值公开在代码/文档里，攻击者已知即可伪造管理员 token 接管系统；④ 生产模式自带保护：检测到默认密钥或缺 CORS_ORIGIN 会 `process.exit(1)` **拒绝启动**，因此只要正确配置即可完全防住。
- **影响**：若按当前配置直接上公网 = 公开密钥 + 未激活白名单 + 弱默认密码（admin123），可被伪造 token 接管。
- **修复**：部署时按"开发方案.md 第十二章"配置（`NODE_ENV=production`、`JWT_SECRET`≥32 位随机、`CORS_ORIGIN`、`TRUST_PROXY=loopback`、改默认密码），重启后日志应出现「生产环境已收紧」。

### 🟠 一般

**M1 · PUT /links/:id 校验与控制器矛盾（✅ 已修复）**
- **是什么**：路由校验强制 `name.notEmpty()`（[links.js:62](file:///d:/PersonHub/server/src/routes/links.js#L62)），但控制器 `updateLink` 内部是 `name ?? existing.name` 的部分更新实现（[linkController.js:87-88](file:///d:/PersonHub/server/src/controllers/linkController.js#L87-L88)）。
- **成因**：写路由校验时按"编辑表单带全量字段"的思路要求 name 必填，与控制器"支持部分更新"的契约不一致。
- **影响**：只改 URL/备注/分类（不传 name）的请求被 400 拒绝。前端编辑表单传全量字段所以页面未暴露；外部脚本/未来前端做部分更新会踩坑。
- **修复（已完成）**：`name` 改为 `optional().notEmpty()`。实测：只传 url → 200 且 name 保留；空字符串 name → 400；全量更新正常。

**M2 · 字体 CDN（fonts.loli.net）不可达（✅ 已修复）**
- **是什么**：楷体/宋体字体从 `https://fonts.loli.net/css2?...` 加载，实测 `net::ERR_ABORTED` 失败，控制台抛 `loadWebFont` 错误。
- **成因**：`fonts.loli.net` 为 Google Fonts 国内镜像，稳定性差；字体服务属外部依赖，与服务器无关。
- **影响**：切「楷体/宋体」加载失败回退系统字体，功能形同虚设；每次切换产生报错和无谓失败请求；若默认字体设为 kai，则每次打开首页都请求失败资源。
- **修复（已完成）**：镜像方案不可靠（实测三个镜像响应缓慢且 font.im 返回无分包的全量 TTF，体积 10MB+），改为**字体本地化**：安装 `lxgw-wenkai-webfont`（kai，官方含完整 CJK unicode-range 分包）+ `@fontsource/noto-serif-sc`（serif 400/700），App.vue 中 `loadWebFont` 改为动态 `import()` CSS，woff2 由 vite 打包从本站加载。实测：kai/serif 切换全部资源来自 localhost、0 外部字体请求、0 控制台报错；system 模式不加载任何字体文件。期间发现 serif loader 缺 return 导致 `undefined.catch` TypeError，已修复为 `Promise.all([...])`。**构建产物增加约 15MB 字体分片（静态资源，浏览器按需下载）**。

**M3 · SSRF（fetch-title，需管理员权限）（✅ 已修复）**
- **是什么**：`GET /api/links/fetch-title?url=` 由**服务器**对传入 URL 发起请求并解析 `<title>`。SSRF = 服务器端请求伪造，可让服务器访问"它自己才能访问"的地址。
- **成因**：只校验了"URL 格式合法"，未校验"URL 指向哪里"；`fetch` 由 Node 发起，内网地址也能连。
- **影响（实测）**：`url=http://127.0.0.1:8000/api/health` → 「该 URL 不是 HTML 页面」（**连通**）；`url=http://127.0.0.1:9/` → fetch failed（不通）。**响应差异可逐端口枚举内网服务**；云服务器上可访问 `169.254.169.254/latest/meta-data/` 读元数据（可能含临时密钥）。缓解因素：需管理员 token，且本系统单管理员、个人使用，实际暴露面小。
- **修复（已完成 2026-08-10）**：`routes/links.js` 的 `/fetch-title` 增加 `query('url').isURL()` 校验（仅 http/https）；新增共享模块 `server/src/utils/ssrfGuard.js`（`isPublicHostname`：IP 字面量直接判定、域名经 DNS 解析全部地址任一内网即拒、解析失败保守拒绝，覆盖 127/8、10/8、172.16/12、192.168/16、169.254/16、0/8、CGNAT、组播及 IPv6 `::1`/`fc00::/7`/`fe80::/10`/映射地址）；`redirect` 由 `follow` 改为 `manual` 手动跟随（最多 5 跳），**每跳重新校验目标 host**，阻断「302 跳内网」绕过。实测：`127.0.0.1:8000/api/health` → 400「该地址不允许访问（内网地址）」；`https://example.com` → 200「Example Domain」。

**M4 · SSRF（favicon domain，公开无鉴权）（✅ 已修复）**
- **是什么**：`GET /api/favicon?domain=`（前台兜底图标接口）把域名拼成 `https://${domain}/favicon.ico` 由服务器抓取；`normalizeDomain` 只做小写/去 www，**未校验 hostname 合法性**。
- **为什么更值得注意**：该接口**无鉴权**，任何访客可触发。
- **影响**：`domain=127.0.0.1:8000` → 服务器出站请求内网 HTTPS 端口。缓解因素：协议写死 https、响应需被识别为合法图片才可见，利用价值有限；但"服务器被当跳板发任意出站请求"本身是风险。
- **修复（已完成 2026-08-10）**：`faviconFetcher.js` 新增 `isValidPublicDomain`——`new URL` 解析校验必须为纯 hostname（拒绝端口/路径/查询/锚点/用户信息/`@` 分隔符绕过/IP 字面量直连），再经 `isPublicHostname` DNS 解析判内网；`fetchFromSources` 开头统一校验（**所有远程抓取的必经点**，覆盖异步抓取/手动抓取/兜底 API）；`GET /api/favicon`（公开）与 `POST /api/favicon/fetch`（鉴权）入口快速拒绝。实测：`127.0.0.1:8000`、`192.168.1.1`、`8.8.8.8`、`example.com/path`、`example.com:8080`、`example.com@evil.com` → 全部拒绝（400）；`example.com` → 200 正常返回。

### 🟡 轻微

**L1 · 导出 HTML 分类名未转义（✅ 已修复）**
- **是什么**：生成 Netscape 书签 HTML 时，链接 URL/名称均 `escapeHtml`（[importExportController.js:86](file:///d:/PersonHub/server/src/controllers/importExportController.js#L86)），但分类标题行 `${cat.emoji} ${cat.name}` 未转义（[L82](file:///d:/PersonHub/server/src/controllers/importExportController.js#L82)）。
- **影响**：分类名含 `<script>` 时导出的 HTML 原样输出，打开导出文件者可能中招；站点前端渲染走 Vue 插值转义不会执行，仅"导出文件"这一条输出路径有风险。
- **修复（已完成 2026-08-10）**：分类名同样过 `escapeHtml`（`const title = ${cat.emoji} ${escapeHtml(cat.name)}`）。实测：创建分类名 `<script>alert("x")</script>` 导出后输出 `&lt;script&gt;alert(&quot;x&quot;)&lt;/script&gt;`，`Contains('<script>')` 为 False，无脚本残留；测试数据已清理。

**L2 · 业务错误拼接 e.message（✅ 已修复）**
- **是什么**：`engineController`/`importExportController` 部分 catch 分支把 `e.message` 直接拼进响应。
- **影响**：SQL 约束等内部细节可能暴露给调用方；这些是 catch 内手动返回，绕过了 `errorHandler` 的生产脱敏。
- **修复（已完成 2026-08-10）**：`engineController`（新增/编辑/排序）、`importExportController`（导出 JSON/HTML、导入 JSON/书签）以及 `linkController` fetch-title 的 catch 分支统一改为固定文案（如"创建失败，请检查输入后重试"），`e.message` 通过 `console.log` 按日志规范输出；全库复查无 `jsonError(res, ...e.message...)` 残留。

**L3 · 导入「覆盖」策略物理删除（✅ 已修复）**
- **是什么**：覆盖策略执行 `DELETE FROM links` 物理删除，项目其他删除均软删除（`deleted_at`）。
- **影响**：选覆盖 = 旧数据不可恢复；属"覆盖即替换"的设计语义，但与软删除规范不一致。
- **修复（已完成 2026-08-10）**：改为先软删除再插入——`UPDATE links SET deleted_at=? WHERE category_id=? AND deleted_at IS NULL`（importJSON 与 importBookmarks 两处）。实测：导入覆盖后旧链接 `deleted_at` 有值（数据保留可恢复）、新链接正常插入；测试数据已清理。

**L4 · 导入导出接口无参数校验（✅ 已修复）**
- **是什么**：`/api/import/*`、`/api/export/*` 只挂鉴权中间件，无 express-validator（其他写接口均有）。
- **影响**：非法字段/超大文件到控制器才暴露，报错不统一；有鉴权 + 2MB 限制兜底，非直接漏洞。
- **修复（已完成 2026-08-10）**：`routes/importExport.js` 补 validator——`/api/import/json` 校验 `strategy ∈ {skip, overwrite}`（可选）与 `categories` 非空数组、`/api/import/bookmarks` 校验 `html` 非空字符串。实测：非法 strategy → 400「strategy 仅支持 skip 或 overwrite」。

**L5 · 错误密码返回 400 而非 401（✅ 已修复）**
- **是什么**：密码错误返回 `{code:400}`，REST 语义应为 401；但返回 401 又会让浏览器控制台自动打印接口信息。
- **影响**：前端按 code/message 判断无影响；HTTP 语义与主流网站（登录失败返回 200 + 业务码）不一致。
- **修复（已完成 2026-08-10，最终方案）**：**登录失败返回 HTTP 200 + 业务码**——`authController` 用户名/密码不匹配均返回 `res.status(200).json({ code: 401, message: '用户名或密码错误', data: null })`；前端 `stores/auth.js` 登录处按 `res.code !== 200` 抛错，UI 提示不变。这样浏览器控制台完全无接口信息（与百度/淘宝等主流网站行为一致），同时避免用户名枚举（统一文案）。实测：错误密码 → HTTP 200 `{code:401, message:'用户名或密码错误'}`、正确密码 → HTTP 200 `{code:200}` 返回 token。

**L6 · fetch-title 对 GBK 页面标题解析失败（✅ 已修复）**
- **是什么**：抓标题统一 `toString('utf-8')`，GBK/GB2312 老站（百度等）转码后乱码，`<title>` 正则失配。
- **实测**：example.org → 正常「Example Domain」；baidu.com → 400 未找到标题。
- **修复（已完成 2026-08-10）**：`linkController` 新增 `decodeHtml`——依次按响应头 `Content-Type` charset / HTML `<meta>` charset / BOM 检测编码，用 `TextDecoder` 解码（Node 内置支持 GBK），utf-8 解码出现大量替换符时回退尝试 gbk。实测：GBK 站 `news.sina.com.cn` → 200「新闻中心首页_新浪网」；`example.com` 正常；`www.baidu.com` 对非浏览器 UA 返回无 `<title>` 的 JS 跳转占位页（反爬行为，非编码问题），仍提示未找到标题属预期。

**L7 · GET /api/links 无列表路由（✅ 已修复）**
- **是什么**：`/api/links` 无 GET 列表，无 token 访问返回 404 HTML 而非 401 JSON。
- **影响**：前端列表走 `GET /categories` 不依赖它，功能无影响；仅错误格式不统一。
- **修复（已完成 2026-08-10）**：`app.js` 增加全局 404 兜底——对 `/api` 路径未匹配路由返回统一 JSON `{code:404, message:'接口不存在'}`。实测：无 token `GET /api/links` → 404 JSON（路由不存在返回 404 语义正确）。

**L8 · ECharts 缺 LegacyGridContainLabel 插件（✅ 已修复）**
- **是什么**：趋势图用 `grid.containLabel`，按需引入 echarts 未注册对应插件，控制台 info 告警。
- **影响**：纯噪音，图表显示正常。
- **修复（已完成 2026-08-10）**：`Dashboard.vue` 从 `echarts/features` 引入 `LegacyGridContainLabel` 并注册进 `use([...])`；生产构建通过。

**L9 · naive-ui 弹窗关闭偶发失效（✅ 已复核，非真实 bug）**
- **是什么**：自动化测试中引擎新增保存成功后弹窗不自动关闭、批量检测确认弹窗取消无响应。
- **成因**：测试环境叠加多个遗留弹窗/合成点击不触发 naive-ui 动画回调，**大概率是自动化 artifact，非真实 bug**。
- **处置（已完成 2026-08-10）**：代码复核——EngineManage.vue 新增保存成功后 `showModal.value = false` 且弹窗 `v-model:show` 双向绑定；LinkManage.vue 批量检测确认用 `dialog.warning({ negativeText: '取消', onPositiveClick: ... })`，负向按钮由 naive-ui 自动关闭。逻辑均正常，维持"自动化 artifact"判定，无需代码修改。

**L10 · 主题切换背景 transition 滞后（✅ 已确认非 bug）**
- **是什么**：设置页切暗色后立即读 body 背景短暂为亮色值。
- **成因**：CSS `transition: background .4s` 是刻意设计的过渡动画，"立刻读取"自然读不到最终值，逻辑正确。
- **处置**：非 bug，仅记录说明，无代码修改。

**L11 · 开发方案文档与实现差异（✅ 已修复）**
- **是什么**：① 统计字段实际 `linkCount/categoryCount`（文档写 totalLinks/…）；② 趋势键实际 `day/count`（文档写 date/count）；③ 导入导出路由实际 `/api/export/*`（文档写 `/api/data/export/*`）；④ 锁定错误提示文案不一致。
- **影响**：不影响运行，按文档联调/维护会踩坑。
- **修复（已完成 2026-08-10）**：同步 `开发方案.md` 第五章 API 设计——overview 字段改为 `linkCount/categoryCount/pinnedCount/recentLinks/totalVisits/todayVisits`，trend 返回结构注明 `{ days, data: [{ day, count }] }`，导入导出路由改为 `/api/export/*`、`/api/import/*`（注明 skip/overwrite 策略），统计接口落地表字段名修正；④ 统一保险库锁定提示文案——后端 `vaultController` 改为「请先在网站设置中设置保险库密码」，与前端 CategoryManage/LinkManage 一致。

> **说明（2026-08-10 补充）**：本报告测试完成后，系统又新增实现了一批功能（**未纳入本报告 59 项用例**，见 `开发方案.md` 8.5/8.7 同步登记）：🔁 URL 判重（`url_normalized` + 409 业务码 + force 强制保存）、📷 图片懒加载、🗑️ 回收站（trash/restore/purge 分类+书签）、🌐 站点信息设置（site_title/site_logo/site_keywords/site_description/site_copyright/stat_tagline + Logo 上传 `POST/DELETE /api/preferences/logo`）、`POST /api/auth/change-password` 改密路由、`festival_countdown_enabled` 节日倒计时开关、`links.favicon_status` 字段。上线前如需全量回归，建议按开发方案第十章 #02 与 8.5 清单补充测试用例。

---

## 三、各维度测试详情

### 3.1 功能测试

**后端 API（59 项用例）**
- ✅ 鉴权：登录 200 返 token；伪造/篡改 token 401；公开接口（categories/preferences/engines/visit/favicon/health/vault-refresh）无需鉴权
- ✅ 分类：CRUD、软删除（返回 `deletedLinks` 级联数）、锁定（保险库未开启时 400 拦截）
- ✅ 书签：CRUD、置顶上限 6 条边界正确（第 7 条被拒）、锁定、访问上报 `visit_count` 递增
- ✅ 输入校验：缺 name/url 等统一 400
- ✅ 统计：overview（含总访问/今日访问）、TOP N、趋势
- ✅ 偏好：读全字段、改后恢复
- ✅ 引擎：CRUD、排序、保留至少 1 个引擎、`{q}` 占位符校验
- ✅ 保险库：status 只读正常（本机已启用）
- ✅ 导入导出：JSON / Netscape HTML 往返一致（skip 策略）
- ✅ favicon：兜底 API 返回图片流、手动抓取成功
- ✅ 健康检测：单条（ok/ms）、批量任务（34 条后台分片 + 进度轮询递增）
- ✅ 标题获取：example.org 正常

**前台功能（浏览器实测）**
- ✅ Hero 问候语/大标题/统计卡片（28 站点/06 分类）/PinStrip 6 置顶/搜索栏三引擎/6 分类卡片/Footer 完整渲染
- ✅ 书签点击新标签打开 + `POST /api/links/:id/visit` 埋点上报
- ✅ 搜索跳转 Bing/百度 新窗口打开含关键词
- ✅ 主题三态循环（亮→暗→自动→亮）、配色预设切换（背景变量跟随）
- ✅ 移动端：卡片不贴边（15px 边距）、无横向溢出、顶栏可用

**后台功能（浏览器实测）**
- ✅ 登录；Dashboard 6 统计卡片 + 趋势折线图 + TOP5 榜单
- ✅ 分类管理 CRUD + 软删除确认 + 排序权重
- ✅ 书签管理：表格列齐全、筛选、单条检测（loading/结果）、批量检测（进度条 9/28→23/28→完成提示"正常 22 · 需代理 6 · 打不开 0"）、移动端卡片布局
- ✅ 引擎管理 CRUD（新增持久化、删除）
- ✅ 设置页：主题/配色/保险库锁定态/导入导出/4 个前台开关

### 3.2 UI/UX
- ✅ 前台玻璃拟态、blob 背景、噪点纹理完整；深浅色可读性正常
- ✅ 后台表格对齐、分页间距、批量删除（PC 显示）、健康状态红黄绿灰圆点 + 移动端圆点文本
- ✅ 移动端复选框在操作栏左侧、每卡片独立选择
- ⚠️ 见问题 L9、L10

### 3.3 兼容性与响应式
- ✅ Chrome（PC 1440 后台表格 + 前台）、移动视口 616px 卡片布局
- ⚠️ **未覆盖**：Firefox / Safari / iOS / Android 真机 / 微信内置浏览器（自动化环境限制，建议上线前抽测）

### 3.4 性能
- ✅ 首页总 load **315ms**（dev）、DOMContentLoaded→load 69ms、95 个资源
- ✅ 生产构建成功（24.86s），产物分包正常（按路由懒加载）
- ✅ Dashboard 体积已优化（2026-08-10）：echarts 独立分包为 `echarts-vendor` chunk（570.39 kB / gzip 196.70 kB，仅在进入后台 Dashboard 时加载，可独立长缓存）；Dashboard 页面 chunk 从 518.78 kB 降至 **13.46 kB（gzip 5.33 kB）**；Home 334 kB（gzip 111.53 kB）
- ✅ 字体资源（M2）已修复：本地化打包（lxgw-wenkai / noto-serif-sc / noto-sans-sc），全站 0 外部字体请求

### 3.5 安全性（详见问题清单）
- ✅ SQL 注入：全部参数化绑定，注入 payload 原样存储无副作用
- ✅ 路径穿越：favicon 文件名 MD5 哈希 + 文件头魔数校验，穿越 payload 全部返回占位图
- ✅ XSS：前端全站无 v-html，Vue 插值自动转义；`javascript:`/`data:` 协议被前后端双重拦截
- ✅ 鉴权：JWT 签名校验 + 改密后旧 token 失效（pcAt 比对）；篡改 token 401
- ✅ 密码：bcrypt 盐轮次统一 12；登录与保险库密码分离
- ✅ 限流：登录/解锁 5 次/分钟/IP（内存实现）
- ✅ 响应头：`x-powered-by` 已禁用；生产错误信息脱敏
- ✅ SSRF（M3/M4）已修复（2026-08-10）：`fetch-title` 与 favicon domain 均增加内网/回环/链路本地地址黑名单与 hostname 合法性校验，重定向逐跳校验
- ✅ 导出转义（L1）、业务错误脱敏（L2）、导入导出参数校验（L4）、登录错误改 HTTP 200+业务码（L5）已修复（2026-08-10）
- ✅ **接口信息泄露专项审计（2026-08-10）**：
  - 所有接口响应不含敏感字段：`password`/`vault_password_hash` 从不返回客户端（内部仅用于 bcrypt 比对）
  - 错误消息不泄露内网信息：生产环境 errorHandler 统一脱敏；畸形 JSON → 400、请求体超限 → 413（不再误落 500）
  - 后端日志不记录密码/Token/堆栈到客户端可见位置（堆栈仅写服务器日志）
  - 访问统计（visit_logs）不收集访问者 IP，仅记 link_id + visited_at
  - 登录/保险库 Token 仅存前端 localStorage 并经 Authorization/X-Vault-Token 头传递，不写入 URL
- ⚠️ 部署配置（S1）

---

## 四、测试结论

**是否建议部署上线：有条件通过** —— 功能与安全基线健康，核心业务链路全部验证通过，无高危可利用漏洞（SQL 注入 / 路径穿越 / 鉴权绕过均未发现）。

**部署前必须完成的 1 件事**：
1. **环境配置**（S1）：`.env` 设置 `NODE_ENV=production`、`JWT_SECRET`（≥32 位随机）、`CORS_ORIGIN=<前端域名>`；nginx/frp 反代时加 `TRUST_PROXY=loopback`

**已完成项（2026-08-10）**：
- ✅ **SSRF 收敛**（M3/M4）：`fetch-title` 与 favicon domain 增加内网/回环/链路本地地址黑名单与 hostname 合法性校验；重定向手动跟随逐跳校验
- ✅ **字体资源可用性**（M2）：字体 CDN 改为本地化打包（lxgw-wenkai-webfont / @fontsource/noto-serif-sc / @fontsource/noto-sans-sc），woff2 全从本站加载
- ✅ **轻微项 L1-L11**：导出 HTML 分类名转义（L1）、业务错误响应脱敏（L2）、导入覆盖策略改软删除（L3）、导入导出接口参数校验（L4）、登录错误改 HTTP 200+业务码（L5）、fetch-title GBK 编码支持（L6）、API 404 统一 JSON（L7）、ECharts 插件注册（L8）；L9 弹窗问题已复核为自动化 artifact、L10 确认非 bug、L11 开发方案文档已同步

**剩余风险点**：
- 兼容性矩阵未完全覆盖（Firefox/Safari/真机）——上线后建议抽测
- Dashboard chunk 偏大——公网低带宽下首屏略慢，可接受但建议优化
- L9 弹窗关闭问题已复核为自动化环境 artifact，真实浏览器如仍复现请反馈

---

## 五、优化建议

### 5.1 安全修复（优先级从高到低）
1. `fetch-title`：url 增加 `query('url').isURL()` 校验 + host 内网黑名单（127/10/172.16/192.168/169.254/::1）
2. favicon `normalizeDomain` 后校验 hostname 为合法域名（拒绝 IP/端口/路径符），出站 host 同样过内网黑名单
3. 导出 HTML 书签的分类名补 `escapeHtml`
4. 业务 catch 分支统一返回文案，错误细节仅写服务端日志
5. import 接口补 express-validator；overwrite 策略改用软删除

### 5.2 功能修复
1. `routes/links.js` PUT 校验的 `name` 改 `optional()`，与控制器部分更新逻辑一致
2. `fetch-title` 增加编码检测（gbk/gb2312 → `TextDecoder`）
3. 错误密码返回 HTTP 401
4. ECharts 补 `LegacyGridContainLabel` 或配置 `grid: { containLabel: true }` 时关闭该 warning
5. 同步 `开发方案.md` 与实际接口差异（字段名/路由前缀）

### 5.3 功能 / 页面合并建议

| 优先级 | 合并点 | 现状 | 建议 |
|--------|--------|------|------|
| 高 | **分页 + 批量删除逻辑** | `CategoryManage.vue` 与 `LinkManage.vue` 的 `pagination`（pageSize/pageSizes/前缀）+ `checkedRowKeys/deleteSelected/全选删除` 逻辑高度重复（各约 90 行） | ✅ **已处理（2026-08-10）**：抽取 `composables/usePagination.js` + `composables/useBatchOps.js`，两页复用，统一页大小切换重置页码行为 |
| 中 | **favicon 抓取后端接口** | `POST /favicon/fetch`（按 URL）与 `POST /links/:id/fetch-favicon`（按 ID）控制器逻辑重叠 | ✅ **已处理（2026-08-10）**：工具层新增共用核心 `fetchFaviconByDomain`（清除失败记录→查缓存→同步抓取→写缓存），URL 版（更新同域名全部链接）与 ID 版（仅更新指定链接）复用同一套流程；实测两入口均返回 `favicon_path` |
| 中 | **相对时间格式化** | `timeAgo` 曾在 LinkManage 出现（已删），Dashboard/统计仍有类似格式需求 | ⏭️ 已确认无实际使用场景（全项目搜索无相对时间显示代码），按最小原则不创建空抽象 |
| 低 | **字母头像渲染** | 后台表格 `renderAvatar` 与前台 `LinkItem/FaviconImage` 头像逻辑相似 | ✅ **已处理（2026-08-10）**：抽取 `components/ui/FaviconAvatar.vue`（favicon 显示 + 加载失败/缺失自动回退字母头像），后台 PC 表格 `renderAvatar` 与移动端卡片统一复用 |
| 已合理 | 搜索 useSearch / 主题 useThemePreset / 响应式 useResponsive / 健康检测 handleCheckLink / 主题切换 store | 均已抽离或共用 | 保持现状，无需再动 |

### 5.4 性能优化
1. ✅ Dashboard 体积已优化（2026-08-10）：echarts 按模块引入 + 独立 vendor 分包（`manualChunks`），Dashboard chunk 518.78 → **13.46 kB**；echarts-vendor（570 kB / gzip 197 kB）仅在进入后台 Dashboard 时按需加载，hash 不变可独立长缓存
2. 字体按需加载策略已合理（仅 kai/serif 时创建 `<link>`），修复 CDN 源即可
3. 首次访问提示 PWA 已按要求隐藏，无额外请求

---

## 六、测试环境与覆盖说明

- **已覆盖**：后端全部 14 组接口、前台首页全要素、后台 5 个页面全流程、安全 12 项静态 + 10 项动态、生产构建、响应式双视口
- **未覆盖**：Firefox/Safari/iOS/Android 真机、多用户并发写入、公网带宽下首屏（本地 dev 315ms 参考值）、大数据量（500+ 链接）滚动渲染
- **测试数据**：所有 TEST_ 前缀测试数据已清理，置顶数、引擎数等环境状态已恢复原状
