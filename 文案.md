

# Cozy Nav

Cozy Nav 是一款功能强大的书签管理平台，提供浏览器扩展、Web 管理界面和智能搜索功能，帮助您高效管理和整理网络书签。

## ✨ 主要特性

### 🌐 智能书签管理
- **快速保存** - 浏览器扩展一键保存当前页面为书签
- **分类管理** - 支持多级分类组织书签
- **标签系统** - 为书签添加标签便于检索
- **收藏夹导入导出** - 支持标准 HTML 书签文件导入导出

### 🔍 强大搜索功能
- **多搜索引擎** - 内置多种搜索引擎，支持自定义
- **智能去重** - 自动检测重复书签
- **全文检索** - 快速定位所需书签

### 🛡️ 健康监测
- **链接可用性检查** - 自动检测失效链接
- **HTTPS/SSL 检测** - 监控证书过期情况
- **告警通知** - 通过 Server酱 等渠道推送异常告警

### 📦 数据安全
- **自动备份** - 支持 Git 版本控制备份
- **本地存储** - SQLite 数据库存储
- **数据迁移** - 支持不同环境间数据迁移

### 🎨 个性化定制
- **多主题支持** - 内置多种精美主题
- **自定义主题** - 支持自定义 CSS 样式
- **移动端适配** - 完美支持手机和平板访问

## 🏗️ 项目结构

```
cozy-nav/
├── extension/          # 浏览器扩展 (Chrome/Edge)
│   ├── manifest.json   # 扩展配置
│   ├── popup.html      # 弹出界面
│   ├── popup.js        # 界面逻辑
│   ├── background.js   # 后台脚本
│   └── icons/          # 图标资源
├── server/             # 后端服务
│   ├── src/
│   │   ├── app.js          # Express 应用入口
│   │   ├── controllers/    # 业务控制器
│   │   ├── routes/         # API 路由
│   │   ├── middlewares/    # 中间件
│   │   ├── utils/          # 工具函数
│   │   └── db/             # 数据库相关
│   └── scripts/            # 运维脚本
└── web/                # 前端应用 (Vue 3)
    ├── src/
    │   ├── api/            # API 接口封装
    │   ├── components/     # Vue 组件
    │   ├── views/          # 页面视图
    │   ├── stores/         # Pinia 状态管理
    │   ├── composables/    # 组合式函数
    │   └── styles/         # 样式文件
    └── public/             # 静态资源
```

## 🚀 快速开始

### 环境要求

- Node.js >= 16.x
- npm 或 yarn
- SQLite3

### 后端部署

```bash
cd server
npm install

# 配置环境变量
cp .env.example .env
# 编辑 .env 文件填入配置

# 启动服务
npm start
```

### 前端部署

```bash
cd web
npm install
npm run dev     # 开发模式
npm run build   # 生产构建
```

### 浏览器扩展安装

1. 打开 Chrome/Edge 浏览器，访问 `chrome://extensions/`
2. 启用**开发者模式**
3. 点击**加载已解压的扩展程序**
4. 选择 `extension` 目录

## 📖 使用指南

### 保存书签

1. 安装浏览器扩展后，点击工具栏图标
2. 在弹出界面中选择分类
3. 可选添加标签和备注
4. 点击保存即可

### 管理书签

1. 访问 Web 管理界面
2. 左侧可按分类浏览
3. 使用搜索框快速查找
4. 点击书签可进行编辑或删除

### 健康监测

1. 进入管理后台的"健康监测"页面
2. 可查看所有异常链接
3. 配置告警通知渠道

## 🔧 配置说明

### 后端配置 (`.env`)

```env
# 服务端口
PORT=3000

# JWT 密钥
JWT_SECRET=your-secret-key

# 数据库路径
DB_PATH=./data/cozynav.db
```

### 扩展配置

在 `extension/manifest.json` 中可配置：
- 扩展名称和版本
- 权限请求
- 浏览器兼容性

## 📝 API 文档

### 认证接口
- `POST /api/auth/login` - 用户登录
- `POST /api/auth/register` - 用户注册
- `POST /api/auth/refresh` - 刷新 Token

### 书签接口
- `GET /api/links` - 获取书签列表
- `POST /api/links` - 创建书签
- `PUT /api/links/:id` - 更新书签
- `DELETE /api/links/:id` - 删除书签

### 分类接口
- `GET /api/categories` - 获取分类列表
- `POST /api/categories` - 创建分类
- `PUT /api/categories/:id` - 更新分类
- `DELETE /api/categories/:id` - 删除分类

完整 API 文档请访问 `/api/docs` (需登录)

## 🤝 贡献指南

欢迎贡献代码或提出建议！

1. Fork 本项目
2. 创建分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 发起 Pull Request

## 📄 开源协议

本项目采用 MIT 协议开源。

## 🙏 致谢

- [Vue.js](https://vuejs.org/) - 前端框架
- [Naive UI](https://www.naiveui.com/) - Vue 3 组件库
- [Express](https://expressjs.com/) - Node.js Web 框架
- [SQLite](https://www.sqlite.org/) - 轻量级数据库

## 📧 联系方式

如有问题或建议，请提交 Issue 或联系维护者。

---

**Cozy Nav** - 让书签管理更简单、更优雅 🏠