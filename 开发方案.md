# cozy·nav 开发方案

> 基于 `d:\PersonHub\index.html` 页面设计的前后端分离开发方案

---

## 一、项目概述

| 项目 | 说明 |
|------|------|
| 名称 | 悦行（cozy·nav）温暖风格导航页 |
| 定位 | 个人书签导航站：一站式收藏 / 搜索 / 巡检 / 统计 / 备份 |
| 文档目的 | 记录系统架构、已实现功能、开发点待办与部署要求，作为开发与运维的唯一权威说明 |
| 设计风格 | 奶油暖色调 + blob 模糊球 + 噪点纹理 + 玻璃拟态卡片 |
| 字体 | Fredoka（圆润标题）、Quicksand（正文）、Caveat（手写体）、Noto Sans SC（中文） |
| 主题切换 | 纯 CSS `:has(#dm:checked)` 实现，无 JS 依赖 |
| 技术栈 | Vue 3 + Express + SQLite（Node.js） |
| 用户模型 | **单用户系统**，仅一个管理员账号，无注册功能，访问记录无需区分用户 |
| 双端适配 | **前台 + 后台均需适配 PC 端和移动端**，前台移动端为优先设计 |

### 页面结构

```
顶栏 TopBar
├── Logo（cozy·nav）
├── 导航（首页 / 书签 / 关于 / 设置）
├── 日期显示
└── 深色模式切换

Hero 区域
├── 时段问候（晚上好呀）
├── 大标题（嘿，今天想去哪里逛逛？）
├── 手写体标语
├── 统计卡片（站点数 / 分类数）

置顶栏 PinStrip
└── 管理员手动置顶的书签（横向滑动展示）

搜索栏 SearchBar
├── 搜索输入框
├── 搜索引擎切换（G / B / AI）
└── 出发按钮

快捷键提示条

分类网格（6 个分类卡片）
├── 每个：emoji 图标 + 标题 + 副标题 + 数量
└── 书签条目：头像 + 名称 + 域名

底栏 Footer
└── 版权 + 手写体签名

右下角通知卡片
└── 版本更新功能列表
```

### 非功能需求

| 维度 | 约定 |
|---|---|
| 性能 | 首页首屏 load ≤ 1s（本地 dev 实测 315ms）；大表查询走索引；图表按需分包懒加载（echarts-vendor gzip 197KB） |
| 安全 | 登录/保险库 JWT；密码 bcrypt（盐轮 ≥ 10）；SQL 全参数化；接口全 express-validator；`/login`、`/vault/unlock` 限流 5 次/分钟/IP；SSRF 内网黑名单（ssrfGuard）；部署生产模式激活 CORS 白名单与 JWT 强度校验 |
| 可靠 | 软删除 + 回收站；每日 03:00 自动备份（本地 + 可选 Git 异地）；数据库迁移幂等可重入 |
| 兼容 | PC + 移动端双端适配；Chrome/Edge 扩展（Manifest V3）；浏览器主流版本 |
| 可维护 | 代码注释中文；复杂逻辑入 composables / controllers；大页面组件化拆分；日志统一 `[时间][模块][操作][结果]` |

---

## 二、架构总览

```
┌──────────────────────────────────────────────┐
│                    浏览器                      │
│  ┌───────────┐   ┌──────────┐  ┌─────────┐  │
│  │ Vue 3 前端 │◄─►│ Pinia    │  │ Router  │  │
│  │ (Vite)    │   │ Store    │  │         │  │
│  └─────┬─────┘   └──────────┘  └─────────┘  │
│        │                                     │
│   ┌────▼─────┐                               │
│   │ Axios    │  拦截器：token 注入 / 401 跳转  │
│   └────┬─────┘                               │
└────────┼────────────────────────────────────┘
         │ JSON / JWT
         ▼
┌──────────────────────────────────────────────┐
│            Node.js + Express 后端              │
│  ┌────────┐  ┌────────┐  ┌────────────────┐  │
│  │ JWT    │  │ 路由层  │  │express-validator│  │
│  │ 中间件  │  │        │  │ 输入校验       │  │
│  └────────┘  └───┬────┘  └────────────────┘  │
│                  │                            │
│          ┌───────▼───────┐                    │
│          │  Controller   │                    │
│          │  (业务逻辑)    │                    │
│          └───────┬───────┘                    │
│                  │                            │
│          ┌───────▼───────┐                    │
│          │  SQLite       │                    │
│          │  (node:sqlite │                    │
│          │   DatabaseSync)                    │
│          └───────────────┘                    │
└──────────────────────────────────────────────┘
```
> 浏览器端另有 Chrome/Edge 扩展（`extension/`，Manifest V3）：右键菜单收藏 + popup 一键收藏，通过 HTTP + JWT 直连后端，不经过本架构图内的前端应用。

---

## 三、技术栈

### 前端

- **Vue 3** + Composition API（`<script setup>`）
- **Vite** 构建工具
- **Pinia** 状态管理
- **Vue Router** 路由

### 后端

- **Node.js** + **Express** Web 框架
- **node:sqlite** SQLite 数据库驱动（Node 内置 `DatabaseSync` 同步驱动，文件型，零配置零依赖）
- **jsonwebtoken** JWT 认证
- **bcryptjs** 密码加密
- **express-validator** 输入校验
- **pm2** 进程守护（部署用）

### 部署优势

```
Express + node:sqlite（SQLite 文件型，零配置零依赖）
├── 运行环境：Node.js（单进程，无需额外数据库服务）
├── 数据库：SQLite 文件型，零配置，无需安装 MySQL/PostgreSQL
├── 部署方式：
│   ├── 方式一：pm2 守护进程（推荐，自动重启）
│   │   npm install -g pm2
│   │   pm2 start server.js --name cozy-nav
│   │   pm2 save && pm2 startup  # 开机自启
│   │
│   ├── 方式二：直接 node 运行（简单但需配合 nohup/screen）
│   │   nohup node server.js &
│   │
│   └── 方式三：Docker（可选，更规范）
│
├── 服务器要求极低：
│   ├── 1 核 CPU + 512MB 内存足够
│   ├── 无需安装数据库服务
│   └── SQLite 数据库就是一个 .db 文件，备份直接复制文件
│
└── 与现有 newnavsite 项目部署方式一致（Express + pm2），可复用部署经验和服务器环境
```

---

## 四、数据库设计

> **当前数据库引擎**：Node.js 内置 `node:sqlite`（`DatabaseSync` 同步驱动），非 better-sqlite3；迁移 DDL 统一放 `db/index.js → migrateDatabase()`，try/catch 幂等补列。

```sql
-- 管理员表（单用户，固定 id=1）
CREATE TABLE admin (
  id                   INTEGER PRIMARY KEY CHECK (id = 1),
  username             TEXT UNIQUE NOT NULL,
  password             TEXT NOT NULL,            -- bcrypt 加密
  password_changed_at  DATETIME,                 -- 管理员密码修改时间（登录后强制改密用）
  vault_password_hash  TEXT,                     -- 保险库独立密码哈希（bcrypt）
  vault_enabled        INTEGER DEFAULT 0,        -- 保险库功能开关 0/1
  lock_version         INTEGER DEFAULT 0,        -- 保险库锁定版本号，改密后递增使旧 Token 失效
  created_at           DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 分类表
CREATE TABLE categories (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  name        TEXT NOT NULL,          -- "常用直达"
  subtitle    TEXT,                   -- "Direct · 每天都要去"
  emoji       TEXT DEFAULT '🧭',
  bg_color    TEXT,                   -- CSS 变量名：peach/mint/lav/sky/rose/butter
  sort_order  INTEGER DEFAULT 0,
  is_locked   INTEGER DEFAULT 0,      -- 🔒 分类级保险锁（0 未锁 / 1 已锁）
  created_at  DATETIME DEFAULT CURRENT_TIMESTAMP,
  deleted_at  DATETIME                -- 软删除
);

-- 书签表
CREATE TABLE links (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  category_id  INTEGER NOT NULL,
  name         TEXT NOT NULL,         -- "Google"
  url          TEXT NOT NULL,
  domain       TEXT,                  -- "google.com"
  url_normalized TEXT,                -- 🔁 URL 规范化指纹（判重唯一键，见开发点 #02，迁移追加）
  avatar_text  TEXT,                  -- "G"（头像文字）
  avatar_color TEXT,                  -- "#FF8A5B"（头像背景色）
  is_pinned    INTEGER DEFAULT 0,    -- 是否置顶：0 否 / 1 是
  pin_order    INTEGER DEFAULT 0,    -- 置顶排序权重
  sort_order   INTEGER DEFAULT 0,
  is_locked    INTEGER DEFAULT 0,    -- 🔒 书签级保险锁（0 未锁 / 1 已锁）
  favicon_path TEXT,                  -- favicon 文件名标识（如 'abc123.png'，存 data/favicons/ 目录）
  favicon_status TEXT,                -- 🖼️ favicon 获取状态：NULL 未获取 / ok 获取成功 / fail 获取失败（迁移追加）
  note         TEXT DEFAULT '',       -- 📝 书签备注（<=200字，仅管理员可见，前台接口不返回，迁移追加）
  last_visited DATETIME,              -- ⏰ 最近访问时间（前台点击上报写入，闲置标记依据，迁移追加）
  visit_count  INTEGER DEFAULT 0,     -- 📊 访问次数（冗余计数，避免统计时 SUM 全表，迁移追加）
  health_status TEXT,                 -- 🩺 可用性检测状态：ok 正常 / blocked 疑似被墙 / fail 打不开 / skip 跳过 / NULL 未检测（迁移追加）
  last_check_at DATETIME,             -- 🩺 最后检测时间（迁移追加）
  health_note  TEXT,                  -- 🩺 检测诊断备注（如"DNS 解析正常但连接超时，疑似被墙"，迁移追加）
  fail_streak  INTEGER DEFAULT 0,     -- 🩺 连续失败次数（>=deadStreak 判定死链，迁移追加）
  tls_expires_at DATETIME,            -- 🔐 HTTPS 证书到期日（定时巡检写入，TLS 预警依据，迁移追加）
  created_at   DATETIME DEFAULT CURRENT_TIMESTAMP,
  deleted_at   DATETIME,              -- 软删除
  FOREIGN KEY (category_id) REFERENCES categories(id)
);

-- 访问明细表（📊 访问统计：趋势图 / TOP 榜单 / 冷链接分析数据源，迁移追加）
CREATE TABLE IF NOT EXISTS visit_logs (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  link_id     INTEGER NOT NULL REFERENCES links(id),
  visited_at  DATETIME DEFAULT CURRENT_TIMESTAMP
);
CREATE INDEX IF NOT EXISTS idx_visit_logs_link ON visit_logs(link_id, visited_at);
CREATE INDEX IF NOT EXISTS idx_visit_logs_time ON visit_logs(visited_at);

-- 偏好表（单行表，存储主题和搜索引擎偏好）
CREATE TABLE preferences (
  id                   INTEGER PRIMARY KEY CHECK (id = 1),  -- 固定单行
  theme                TEXT DEFAULT 'light',   -- light / dark / auto（auto 跟随 19:00-07:00）
  search_engine        TEXT DEFAULT 'google',
  engine_display_count INTEGER DEFAULT 3,      -- 搜索栏展示的引擎数 1-10
  theme_preset         TEXT DEFAULT 'amber-pine',  -- 🎨 配色预设 key（见 themePresets.js）
  show_domain          INTEGER DEFAULT 1,      -- 是否在前台显示域名（1=显示 0=隐藏）
  view_mode            TEXT DEFAULT 'card',    -- 视图模式：card / list / compact
  font_family          TEXT DEFAULT 'system',  -- 字体族：system（黑体）/ kai（楷体）/ serif（宋体）
  font_switch_enabled  INTEGER DEFAULT 1,      -- 🔤 字体切换功能开关（1=开启 0=关闭，迁移追加）
  hero_tagline         TEXT DEFAULT '',         -- ✨ 自定义 Hero 标语（空字符串=使用默认标语，迁移追加）
  festival_enabled     INTEGER DEFAULT 1,       -- 🎉 节日彩蛋功能开关（1=开启 0=关闭，迁移追加）
  festival_countdown_enabled INTEGER DEFAULT 1, -- 🎉 节日倒计时显示开关（1=开启 0=关闭，迁移追加）
  idle_mark_enabled    INTEGER DEFAULT 0,       -- ⏰ 闲置书签标记开关（1=开启 0=关闭，迁移追加）
  custom_theme         TEXT DEFAULT '{}',        -- 🎨 自定义配色覆盖层 JSON：{light:{'--bg':'#xxx',...}, dark:{...}}（迁移追加）
  site_title           TEXT DEFAULT '悦行',     -- 🌐 网站标题（首页 <title> / Logo 文字回退，迁移追加）
  site_logo            TEXT DEFAULT '',          -- 🖼️ 站点 Logo（dataURL 或路径，顶栏显示，迁移追加）
  site_keywords        TEXT DEFAULT '',          -- 🌐 网站关键词（SEO meta，迁移追加）
  site_description     TEXT DEFAULT '',          -- 🌐 网站描述（SEO meta，迁移追加）
  site_copyright       TEXT DEFAULT '',          -- 📄 页脚版权信息（迁移追加）
  stat_tagline         TEXT DEFAULT '— everything in its place —',  -- 📊 统计卡底部标语文案（后台可编辑，迁移追加）
  health_config        TEXT,           -- 🩺 巡检配置 JSON：{enabled, intervalHours, batchSize, deadStreak, recheckDead, tlsYellowDays, tlsRedDays, coldDays}（迁移追加）
  notification_config  TEXT,           -- 🔔 通知中心配置 JSON：{channels: {serverchan, email}, events: {patrol, gitBackup}}（迁移追加）
  backup_config        TEXT,           -- 💾 备份配置 JSON：{autoEnabled, retainCount, gitEnabled, gitRemote, gitBranch}（迁移追加）
  category_sort_mode   TEXT DEFAULT 'sort_order:asc', -- 🔀 前台分类排序模式：sort_order:asc / created_at:asc|desc / name:asc|desc（迁移追加）
  link_sort_mode       TEXT DEFAULT 'sort_order:asc'  -- 🔀 前台书签排序模式（同分类，迁移追加）
);

-- 搜索引擎表（用户可自定义，前台可切换）
CREATE TABLE search_engines (
  id            INTEGER PRIMARY KEY AUTOINCREMENT,
  name          TEXT NOT NULL,            -- "Google"
  label         TEXT NOT NULL,            -- 按钮上展示的标签（单字/两字）
  key           TEXT NOT NULL UNIQUE,     -- 唯一标识，与 preferences.search_engine 对应
  url_template  TEXT NOT NULL,            -- "https://xxx?q={q}"，{q} 占位
  icon          TEXT,                     -- emoji 图标
  color         TEXT,                     -- 背景色类名
  sort_order    INTEGER DEFAULT 0,        -- 排序权重
  is_active     INTEGER DEFAULT 1,        -- 是否启用 0/1
  deleted_at    DATETIME,                 -- 软删除
  created_at    DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at    DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 巡检报告表（🩺 每轮巡检/批量检测结束写一行，保留最近 100 轮，迁移追加）
CREATE TABLE IF NOT EXISTS patrol_reports (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  started_at   DATETIME DEFAULT CURRENT_TIMESTAMP,  -- 巡检开始时间
  finished_at  DATETIME,                             -- 巡检结束时间
  trigger_type TEXT DEFAULT 'manual',                -- 触发方式：manual 手动 / patrol 定时 / batch 批量
  total        INTEGER DEFAULT 0,                    -- 本轮检测总数
  ok           INTEGER DEFAULT 0,                    -- 正常数
  blocked      INTEGER DEFAULT 0,                    -- 需代理数
  fail         INTEGER DEFAULT 0,                    -- 打不开数
  skip         INTEGER DEFAULT 0,                    -- 跳过数
  duration_ms  INTEGER DEFAULT 0,                    -- 耗时（毫秒）
  issue_links  TEXT                                  -- 本轮异常链接快照 JSON：[{id, status}]
);
CREATE INDEX IF NOT EXISTS idx_patrol_reports_time ON patrol_reports(started_at);
```

---

## 五、API 路由设计

> 状态图例：✅ 已实现 · 🔲 待实现（开发点 #01 等）

| 方法 | 路径 | 说明 | 认证 | 状态 |
|------|------|------|------|------|
| | **认证** | | | |
| POST | `/api/auth/login` | 管理员登录，返回 JWT | 否 | ✅ |
| POST | `/api/auth/change-password` | 修改管理员密码（校验原密码 + 新密码≥8位，更新 `password_changed_at` 使旧 Token 失效） | 是 | ✅ |
| | **分类** | | | |
| GET | `/api/categories` | 获取所有分类（含书签；保险库未开启时全员可见，开启后按 vault_token/管理员权限分级过滤） | 否（公开，前台展示） | ✅ |
| POST | `/api/categories` | 新建分类 | 是 | ✅ |
| PUT | `/api/categories/:id` | 编辑分类 | 是 | ✅ |
| DELETE | `/api/categories/:id` | 删除分类（软删除，级联软删除书签并返回 `{ deletedLinks: N }`） | 是 | ✅ |
| PUT | `/api/categories/:id/lock` | 🔒 切换分类保险锁状态 | 是 | ✅ |
| GET | `/api/categories/trash` | 🗑️ 回收站分类列表（LEFT JOIN 聚合书签数，支持服务端分页 `?page&pageSize`，兼容不传参返回全量） | 是 | ✅ |
| POST | `/api/categories/:id/restore` | 🗑️ 恢复回收站分类（仅恢复分类本身，书签需在书签回收站单独恢复） | 是 | ✅ |
| DELETE | `/api/categories/:id/purge` | 🗑️ 彻底删除回收站分类（先清 visit_logs → 再物理删 links → 最后删分类） | 是 | ✅ |
| | **书签** | | | |
| POST | `/api/links` | 新建书签（URL 规范化判重，命中且未带 `force` → 业务码 409） | 是 | ✅ |
| PUT | `/api/links/:id` | 编辑书签（部分更新，URL 变更时判重 + 清空 favicon 并异步重抓） | 是 | ✅ |
| DELETE | `/api/links/:id` | 删除书签（软删除） | 是 | ✅ |
| PUT | `/api/links/:id/pin` | 置顶/取消置顶书签 | 是 | ✅ |
| PUT | `/api/links/:id/lock` | 🔒 切换书签保险锁状态 | 是 | ✅ |
| POST | `/api/links/:id/fetch-favicon` | 🖼️ 编辑时同步抓取 favicon | 是 | ✅ |
| GET | `/api/links/fetch-title?url=xxx` | 📝 获取网页标题（isURL 校验 + SSRF 内网黑名单 + 手动跟随跳转逐跳校验 + GBK 编码检测） | 是 | ✅ |
| POST | `/api/links/:id/visit` | 📊 前台点击访问上报：事务内更新 `last_visited` + `visit_count` 并插入 `visit_logs` 明细 | 否 | ✅ |
| POST | `/api/links/:id/check` | 🩺 单链接可用性检测（直连 + DoH 判定：ok / blocked 疑似被墙 / fail / skip） | 是 | ✅ |
| POST | `/api/links/check-all` | 🩺 批量可用性检测（后台任务，4 并发分片，内存态进度） | 是 | ✅ |
| GET | `/api/links/check-progress` | 🩺 批量检测进度查询（running/total/done/ok/blocked/fail/skip） | 是 | ✅ |
| GET | `/api/links/trash` | 🗑️ 回收站书签列表（支持服务端分页，兼容不传参返回全量） | 是 | ✅ |
| POST | `/api/links/:id/restore` | 🗑️ 恢复回收站书签 | 是 | ✅ |
| DELETE | `/api/links/:id/purge` | 🗑️ 彻底删除回收站书签（物理删除 + 清理 visit_logs） | 是 | ✅ |
| | **统计** | | | |
| GET | `/api/stats/overview` | 总览：`linkCount` / `categoryCount` / `pinnedCount` / `recentLinks`（近7天新增） + `totalVisits` / `todayVisits`（6 卡片） | 是 | ✅ |
| GET | `/api/stats/links/top?days=7&limit=5` | 📊 热门链接 TOP N（近 N 天访问最多，含 favicon_path） | 是 | ✅ |
| GET | `/api/stats/visits/trend?days=7` | 📊 近 N 天每日访问趋势，返回 `{ days, data: [{ day, count }] }`，无访问天补 0，前端直接绘图 | 是 | ✅ |
| GET | `/api/stats/links/cold` | 📊 冷链接清单（N 天未访问，`?days=90`，开发点 #09 已实现） | 是 | ✅ |
| GET | `/api/stats/links/health` | 🩺 健康总览（total/ok/blocked/fail/down/skip/unknown + tlsExpiring + failTop + issueList，开发点 #09/#10 已实现） | 是 | ✅ |
| GET | `/api/stats/patrol/reports` | 🩺 历史巡检报告列表（分页 + `?limit=N` 趋势，开发点 #10 已实现） | 是 | ✅ |
| GET | `/api/stats/patrol/reports/:id` | 🩺 单轮巡检报告详情（关联当前链接信息，已删除标记 gone，开发点 #10 已实现） | 是 | ✅ |
| GET | `/api/admin/links/:id/health-log` | 🩺 单链接最近检测记录（开发点 #01 三期） | 是 | 🔲 |
| GET | `/api/links/recent?limit=5` | 🕘 最近访问书签（开发点 #05，公开，过滤锁书签与敏感字段） | 否 | 🔲 |
| GET | `/api/links/issues` | 🩺 异常链接分页列表（down/fail/blocked/skip/未检测按严重度排序，开发点 #10 已实现） | 是 | ✅ |
| POST | `/api/links/batch-check` | 🩺 异常链接批量重检（复用巡检任务锁，开发点 #10 已实现） | 是 | ✅ |
| PUT | `/api/links/health/reset` | 🩺 批量重置健康状态（清空 status/fail_streak/last_check_at，排入下次巡检优先，开发点 #10 已实现） | 是 | ✅ |
| GET | `/api/backup/config` | 💾 读取备份配置（autoEnabled/retainCount/gitEnabled/gitRemote/gitBranch + dataDir + gitRepoReady，开发点 #07 已实现） | 是 | ✅ |
| PUT | `/api/backup/config` | 💾 保存备份配置（Git 推送前自动 `remote set-url` 同步仓库地址，开发点 #07 已实现） | 是 | ✅ |
| POST | `/api/backup/run` | 💾 手动触发备份（本地一致快照 + 按配置推 Git，开发点 #07 已实现） | 是 | ✅ |
| GET | `/api/backup/list` | 💾 备份文件列表（文件名/大小/时间 + hasGit 推送标记，开发点 #07 已实现） | 是 | ✅ |
| GET | `/api/stats/visits/heatmap?days=365` | 📅 访问热力图聚合（开发点 #08 一期） | 是 | 🔲 |
| GET | `/api/stats/annual?year=2026` | 📈 个人上网年鉴聚合（开发点 #08 二期） | 是 | 🔲 |
| GET | `/api/links/timeline` | 🕑 收藏时间线（按月份聚合，开发点 #08 二期） | 是 | 🔲 |
| | **健康检查（原方案未登记，已实现）** | | | |
| GET | `/api/health` | 🩺 健康检查（status / db / uptime），供监控探活 | 否 | ✅ |
| | **偏好** | | | |
| GET | `/api/preferences` | 获取偏好（主题/引擎/配色/展示数量/域名显示/视图模式/字体/字体开关/Hero标语/节日彩蛋/闲置标记/自定义配色/站点信息/统计标语） | 否（公开，前台展示） | ✅ |
| PUT | `/api/preferences` | 更新偏好（theme/search_engine/engine_display_count/theme_preset/show_domain/view_mode/font_family/font_switch_enabled/hero_tagline/festival_enabled/festival_countdown_enabled/idle_mark_enabled/custom_theme/site_title/site_logo/site_keywords/site_description/site_copyright/stat_tagline/health_config/notification_config） | 是 | ✅ |
| POST | `/api/preferences/notify-test` | 🔔 通知测试（`{channel: 'serverchan', sendKey}` 或 `{channel: 'email', config}`，开发点 #10 已实现） | 是 | ✅ |
| POST | `/api/preferences/logo` | 🖼️ 上传站点 Logo（base64 dataURL，≤512KB，写 `site_logo`） | 是 | ✅ |
| DELETE | `/api/preferences/logo` | 🖼️ 移除站点 Logo | 是 | ✅ |
| | **Favicon** | | | |
| GET | `/api/favicon?domain=example.com` | 🖼️ 兜底获取 favicon（公开，前台用，7天强缓存） | 否 | ✅ |
| POST | `/api/favicon/fetch` | 🖼️ 按 URL 手动抓取 favicon（创建时用，无 linkId） | 是 | ✅ |
| | **搜索引擎（原方案未登记，已实现）** | | | |
| GET | `/api/engines` | 获取已启用的引擎列表 | 否（公开，前台用） | ✅ |
| GET | `/api/engines/all` | 获取全部引擎（含禁用，后台管理） | 是 | ✅ |
| POST | `/api/engines` | 新增搜索引擎 | 是 | ✅ |
| PUT | `/api/engines/sort` | 批量更新引擎排序 | 是 | ✅ |
| PUT | `/api/engines/:id` | 编辑引擎（状态/标签/URL 模板等） | 是 | ✅ |
| DELETE | `/api/engines/:id` | 删除引擎（软删除，至少保留 1 个） | 是 | ✅ |
| | **保险库（原方案未登记，已实现）** | | | |
| GET | `/api/vault/status` | 查询保险库开关 & 密码是否已设置 | 是 | ✅ |
| PUT | `/api/vault/toggle` | 开启 / 关闭保险库功能 | 是 | ✅ |
| POST | `/api/vault/password` | 首次设置保险库独立密码（bcrypt） | 是 | ✅ |
| PUT | `/api/vault/password` | 修改保险库密码，递增 lock_version 失效旧 Token | 是 | ✅ |
| POST | `/api/vault/unlock` | 输入密码解锁，返回短期 vault_token + refresh_token | 是 | ✅ |
| POST | `/api/vault/lock` | 立即锁定，清除会话 | 是 | ✅ |
| POST | `/api/vault/refresh` | 用 refresh_token 静默换取新 vault_token | 否 | ✅ |
| | **导入导出（原方案未登记，已实现）** | | | |
| GET | `/api/export/json` | 导出分类 + 书签 + 引擎 为 JSON | 是 | ✅ |
| GET | `/api/export/bookmarks` | 导出浏览器兼容的 Netscape HTML 书签 | 是 | ✅ |
| POST | `/api/import/json` | 导入 JSON 数据（支持 skip 跳过重复 / overwrite 覆盖现有） | 是 | ✅ |
| POST | `/api/import/bookmarks` | 导入 Chrome/Edge HTML 书签，自动按文件夹建分类 | 是 | ✅ |

### 统一响应格式

```json
// 成功
{ "code": 200, "message": "操作成功", "data": {...} }

// 失败
{ "code": 400, "message": "分类名称不能为空", "data": null }
```

---

## 六、前端结构

> 图例：✅ 实际存在并使用 · 🔲 规划中（开发点 #01）

```
src/
├── views/
│   ├── front/                     # 前台页面
│   │   └── Home.vue               # ✅ 导航主页（Hero + PinStrip + SearchBar + CategoryGrid + Footer）
│   │                              # ✅ 加载骨架屏 + 节日装饰 + 闲置书签时钟角标
│   ├── admin/                     # 后台管理页面（登录统一走前台 TopBar 弹窗，无 /login 路由）
│   │   ├── Layout.vue             # ✅ 后台骨架（PC 侧边栏 + 移动端抽屉，Naive UI 主题注入，菜单含 8 项）
│   │   ├── Dashboard.vue          # ✅ 数据概览（6 张统计卡片：书签/分类/置顶/近7天新增/总访问量/今日访问）
│   │   │                          # ✅ 近 7 天访问趋势折线图（vue-echarts）+ 本周热门 TOP5 榜单 + 🩺 健康总览 + 异常链接清单
│   │   ├── CategoryManage.vue     # ✅ 分类管理（CRUD + 拖拽排序 + 🔒保险锁 + 批量删除 + 背景色HEX显示）
│   │   │                          # ✅ 🗑️ 回收站 tab（CategoryTrashTable 子组件：恢复/彻底删除 + 服务端分页 + 移动端同步）
│   │   ├── LinkManage.vue         # ✅ 书签管理（CRUD + 置顶 + 筛选 + 🔒保险锁 + 批量删除 + 备注列 + 头像色HEX显示）
│   │   │                          # ✅ 🗑️ 回收站 tab（LinkTrashTable 子组件）+ 「访问次数/最近访问/健康状态」3 列 + 行内检测按钮 + 页头批量检测（进度轮询）+ 健康状态组合筛选 + 移动端同步
│   │   ├── EngineManage.vue       # ✅ 搜索引擎管理（新增/编辑/启禁/删除/拖拽排序/展示数量）
│   │   ├── DataManage.vue         # ✅ 数据管理（导出/导入 + BackupManager 自动备份卡片：Git 初始化引导）
│   │   ├── HealthMonitor.vue      # ✅ 链接巡检（SettingsHealth 巡检配置 + IssueManage 异常处理 + PatrolReports 历史报告）
│   │   ├── NotifyCenter.vue       # ✅ 通知中心（NotifyChannels 渠道 + NotifyEvents 事件）
│   │   └── Settings.vue           # ✅ 设置容器（已拆分 6 个子组件）：
│   │                              #     SettingsBasic 基础 / SettingsAppearance 外观 / SettingsFrontDisplay 前台显示
│   │                              #     SettingsDataManage 数据管理 / SettingsVault 保险库 / SettingsPassword 改密
│   └── NotFound.vue               # ✅ 404 页面
├── components/
│   ├── layout/
│   │   ├── TopBar.vue             # ✅ 前台顶栏（site_logo 图 Logo / site_title 文字 Logo 双形态 + 后台入口 + 日期/时段 + 亮暗切换按钮）
│   │   │                          # ✅ 字体切换 Aa 按钮受 fontSwitchEnabled 开关控制（v-if 显隐）
│   │   └── Footer.vue             # ✅ 底栏（版本号 + 手写签名 + site_copyright 版权回退）
│   ├── hero/
│   │   └── HeroSection.vue        # ✅ 问候语 + 大标题 + 手写标语（可自定义 hero_tagline）+ 搜索框嵌入
│   │                              # ✅ 统计卡片数字/文字独立变量（--stat-ink/--stat-site-text/--stat-cat-ink/--stat-cat-text）
│   ├── pin/
│   │   └── PinStrip.vue           # ✅ 置顶书签横滑（置顶栏内）
│   ├── search/
│   │   └── SearchBar.vue          # ✅ 搜索框 + 引擎按钮切换（数量从 prefs 取 engine_display_count）
│   ├── bookmark/
│   │   ├── CategoryGrid.vue       # ✅ 前台分类网格容器（v-for + 响应式列数）
│   │   ├── CategoryCard.vue       # ✅ 前台单分类卡片（emoji + 标题 + 书签列表）
│   │   ├── LinkItem.vue           # ✅ 前台单书签（头像/名称/域名 + 🔒锁/跳转动画 + 闲置时钟角标）
│   │   └── VaultUnlockModal.vue   # ✅ 保险库解锁弹窗（前台点击锁分类/锁书签时弹出）
│   └── ui/
│       ├── ShortcutHint.vue       # ✅ 快捷键提示条
│       ├── ShortcutHelp.vue       # ✅ 快捷键帮助大面板（? 键呼出）
│       ├── NoteCard.vue           # ✅ 右下角通知卡片（更新公告）
│       ├── BackgroundDecor.vue    # ✅ blob + 噪点纹理 + 玻璃拟态装饰（全站背景）
│       ├── SkeletonCard.vue       # ✅ 加载骨架屏（6 张模拟分类卡片 + shimmer 扫光）
│       ├── FaviconImage.vue       # ✅ favicon 图片组件（有路径加载静态文件 + loading="lazy"，无则字母头像降级）
│       ├── FaviconAvatar.vue      # ✅ 字母头像统一组件（favicon 显示 + 加载失败/缺失自动回退字母头像，PC 表格与移动端卡片复用）
│       ├── BackToTop.vue          # ✅ 回到顶部按钮（滚动出现）
│       └── FestivalDecor.vue      # ✅ 节日装饰（春节灯笼 / 圣诞雪花 / 中秋月亮 / 万圣蝙蝠 / emoji 飘落）
│   ├── admin/                     # ✅ 后台子组件（大页面拆分产物，相对导入到 src 上 3 层）
│       ├── settings/              # Settings 拆分的卡片组件（v1.4 合并为 3 卡片）
│       │   ├── CollapsibleCard.vue    # 可折叠卡片（标题栏点击展开/收起，localStorage 记忆）
│       │   ├── SettingsSite.vue       # 🌐 站点设置卡片（基本配置 + 前台显示，分段切换）
│       │   ├── SettingsAppearance.vue # 🎨 外观设置卡片（外观主题 + 默认主题 + 自定义配色，分段切换）
│       │   └── SettingsSecurity.vue   # 🔐 安全设置卡片（登录密码 + 保险库，分段切换）
│       ├── health/                # 巡检页组件
│       │   ├── SettingsHealth.vue   # 🩺 巡检配置（间隔 24/48/72/每周 + 判死阈值 + TLS 预警 + 立即巡检）
│       │   ├── IssueManage.vue      # 异常链接管理（列表/批量重检/重置/删除）
│       │   └── PatrolReports.vue    # 历史巡检报告（趋势图 + 列表 + 详情弹窗）
│       ├── notify/                # 通知中心组件
│       │   ├── NotifyChannels.vue   # 🔔 渠道配置（Server酱 / QQ 邮箱 + 测试按钮）
│       │   └── NotifyEvents.vue     # 事件配置（巡检结果 / Git 备份结果 + 模板占位符）
│       ├── data/                  # 数据管理组件
│       │   └── BackupManager.vue    # 💾 自动备份卡片（本地 + Git 引导 + 最近备份记录）
│       ├── categorymanage/        # 分类管理组件
│       │   └── CategoryTrashTable.vue  # 🗑️ 分类回收站（复用 useTrashTable）
│       └── linkmanage/            # 书签管理组件
│           ├── LinkFormModal.vue     # 新建/编辑书签弹窗
│           └── LinkTrashTable.vue    # 🗑️ 书签回收站（复用 useTrashTable）
├── stores/
│   ├── auth.js                    # ✅ 登录态、JWT、强制改密检测
│   ├── data.js                    # ✅ 分类+书签数据（含保险锁分类/书签过滤）
│   ├── prefs.js                   # ✅ 主题/引擎/配色预设 + 字体切换开关 + Hero 标语 + 节日/闲置开关 + customTheme + 站点信息（siteTitle/siteLogo/关键词/描述 + statTagline）（含旧预设 key 迁移）
│   └── vault.js                   # ✅ 保险库（开关/解锁态/密码修改 Token refresh）
├── api/
│   ├── request.js                 # ✅ Axios 封装（10s 超时、断网提示、401 自动跳登录、JWT 注入）
│   ├── auth.js                    # ✅ 登录
│   ├── category.js                # ✅ 分类 CRUD + lock + trash()/restore()/purge()（回收站）
│   ├── link.js                    # ✅ 书签 CRUD + pin + lock + visit（点击上报）+ checkLink() / checkAll() / checkProgress() + trash()/restore()/purge()（回收站）
│   ├── stats.js                   # ✅ getOverview() + getTopLinks() + getVisitTrend() + getColdLinks() + getHealthOverview() + getPatrolReports()/getPatrolReportDetail()
│   ├── prefs.js                   # ✅ 偏好 get/update + uploadLogo()/removeLogo() + sendTestNotify()
│   ├── engines.js                 # ✅ 搜索引擎 CRUD + 排序
│   ├── vault.js                   # ✅ 保险库状态/开关/密码/解锁/锁定/refresh
│   ├── importExport.js            # ✅ 导入导出（JSON + HTML 书签双向）
│   └── backup.js                  # ✅ 备份配置 get/save + runNow() + list()
├── composables/
│   ├── useClock.js                # ✅ 时钟 + 时段问候语 + emoji
│   ├── useShortcut.js             # ✅ 快捷键绑定（/搜索、1-6跳分类、p置顶、?帮助、t主题）
│   ├── useThemePreset.js          # ✅ 配色预设注入 html/body CSS 变量 + Naive UI overrides
│   ├── useResponsive.js           # ✅ 响应式断点检测
│   ├── useColor.js                # ✅ 颜色解析：resolveColor（HEX/变量名）→ isValidHex → displayHex（当前模式实际渲染色）
│   ├── useFestival.js             # ✅ 节日检测（lunar-javascript 农历 + 公历，带生效窗口期）
│   ├── useSearch.js               # ✅ 本地搜索（书签/分类过滤 + 高亮）
│   ├── useFont.js                 # ✅ 字体按需加载（system/kai/serif，动态 import woff2，font-display: swap）
│   ├── usePagination.js           # ✅ 分页逻辑抽取（pageSize/pageSizes/页码切换重置，CategoryManage 与 LinkManage 复用）
│   ├── useBatchOps.js             # ✅ 批量操作抽取（勾选/全选/删除选中，两管理页复用）
│   ├── useVersion.js              # ✅ 版本号读取
│   ├── useTrashTable.js           # ✅ 回收站全链路组合式函数（分页/恢复/彻底删除/批量操作，回收站组件复用）
│   └── useRenderCell.js           # ✅ 表格单元格渲染工具（renderAvatar / renderEmoji）
├── theme/
│   └── themePresets.js            # ✅ 6 套主题组（各含 light/dark 变体 + LEGACY_KEY_MAP 迁移）
│                                  # ✅ enrichMissingGlobals 为 12 个变体补全 --on-pop/--peach/--mint/--rose
├── router/
│   └── index.js                   # ✅ 路由配置（前台 / 后台 + 鉴权守卫 + 改密守卫）
└── styles/
    ├── tokens.css                 # ✅ CSS 变量 + blob 动效 + 玻璃拟态（CSS 默认即为移动端）
    ├── responsive.css             # ✅ 响应式栅格与断点辅助
    └── admin-common.css           # ✅ 后台共享样式（table-card / cat-emoji / mob-* / 分页，App.vue 全局 @import）
```

### 响应式适配策略（已落地实现）

```
断点：使用 Naive UI + 自定义 tokens.css 栅格（n-grid cols="1 768:2 1024:4" 模式）
前台适配：
  ├── 顶栏：移动端顶栏仅 logo + 后台按钮 + 主题切换，日期 Hero 内展示
  ├── Hero：始终单列（搜索框占主视觉），问候语按屏幕宽度 clamp() 缩放
  ├── 置顶栏：始终横滑（overflow-x: auto；移动端卡片 padding 自动收缩）
  ├── 分类网格：移动端 1 列（默认）→ 768px+ 2 列 → 1024px+ 3 列
  └── 搜索栏：自适应宽度 + clamp() 圆角/字号，引擎按钮最多展示 engine_display_count 个

后台适配（Naive UI 管理 Layout）：
  ├── 侧边栏：移动端抽屉式（Layout collapsed-mode="drawer"）→ PC 端固定侧边栏
  ├── 数据表格：n-data-table + scrollX，移动端水平滚动看完整列；或卡片化展示
  ├── 表单：n-form 单列布局（移动端友好）→ PC 端 n-grid 2 列
  └── 操作按钮：PC 行内（n-space inline）→ 移动端操作列固定宽度
```

---

## 七、关键实现要点（已落地实现）

| 功能 | 实现方式 | 状态 |
|------|---------|------|
| 亮暗主题模式切换 | light / dark / auto 三态，auto 跟随 19:00-07:00；`prefs.theme` + Naive UI `n-config-provider` 深色 | ✅ |
| 🎨 6 套配色预设（组内亮暗变体） | `themePresets.js` 定义 THEME_GROUPS，`useThemePreset.js` 把 CSS 变量注入 html/body + Naive overrides；切换模式自动用同组亮/暗变体 | ✅ |
| 书签置顶 | `PUT /api/links/:id/pin` 切换 `is_pinned`；前台 PinStrip 按 `pin_order` 横滑展示 | ✅ |
| 前台保险锁分类/书签 | `categories.is_locked` + `links.is_locked`；前台解锁前隐藏内容（VaultUnlockModal 输入密码，vault_token 存 store + refresh） | ✅ |
| 保险库独立密码体系 | 独立 `vault_password_hash`（bcrypt 盐轮 >=10），改密递增 `lock_version` 使旧 Token 失效 | ✅ |
| 搜索引擎可管理 | `search_engines` 表 CRUD + 软删除 + 批量排序；前台 SearchBar 从 `/api/engines` 拉启用列表 | ✅ |
| 数据导入导出 | JSON 全量导出/导入（替换/合并）+ 浏览器 Netscape HTML 格式导入导出（Chrome/Edge 兼容） | ✅ |
| 时钟问候 | `useClock` composable，每秒更新，按时段（早/午/晚/夜）切换问候语和 emoji | ✅ |
| 快捷键 | `useShortcut` composable，监听 `/`（搜索）、`1-6`（跳分类锚点）、`p`（置顶栏焦点）、`?`（帮助）、`t`（主题） | ✅ |
| blob / 噪点 / 玻璃装饰 | `BackgroundDecor.vue` 组件，`@keyframes bob` 动效一致；所有卡片背景使用 CSS 变量而非硬编码颜色 | ✅ |
| 管理员首次/强制改密 | `admin.password_changed_at` 为空时登录后强制跳 Settings 修改密码 | ✅ |
| 🖼️ Favicon 自动/手动抓取 | 文件存储+数据库存路径；多源回退（cccyun→DuckDuckGo→站点直抓→Google S2）；三级缓存（内存→磁盘→远程）；失败缓存24h；定时清理7天；兜底API+创建/编辑时手动获取按钮 | ✅ |
| 📝 网页标题获取 | `GET /api/links/fetch-title` 流式HTML解析（64KB限制，5s超时），表单手动按钮触发，获取后同步头像文字 | ✅ |
| 🌐 域名显示全局开关 | `preferences.show_domain`（1/0），书签管理域名列表头 `n-switch` 控制，前台 LinkItem 按 `v-if` 显隐 | ✅ |
| 💀 加载骨架屏 | `SkeletonCard.vue` 模拟分类卡片布局 + shimmer 扫光动画，`dataStore.isLoading` 时显示 6 个骨架卡片 | ✅ |
| 📊 多视图模式 | `preferences.view_mode`（card/list/compact），CategoryCard `.links` class 切换布局，TopBar 循环切换按钮 | ✅ |
| 🔤 字体切换（可开关） | `preferences.font_family`（system黑体/kai楷体/serif宋体），App.vue `--app-font` CSS 变量 + `FONT_STACKS` 映射 + 按需加载中文 web font（fonts.loli.net 镜像，`font-display: swap` 不阻塞），TopBar 循环切换按钮；`preferences.font_switch_enabled`（1/0）后台开关控制按钮显隐 | ✅ |
| ✨ 自定义 Hero 标语 | `preferences.hero_tagline`（空串=默认），Settings「前台显示」卡片输入 + 保存/恢复默认；HeroSection 手写体位置 `heroSubtitle` 计算属性回退默认「— 你的小角落，随时出发 ♡」 | ✅ |
| 📝 书签备注字段 | `links.note`（<=200字），后台 LinkManage 增删改查 + 列表「备注」列 + 移动端展示；公开前台接口不返回（仅管理员可见） | ✅ |
| 参数化 SQL + 软删除封装 | 所有查询统一加 `WHERE deleted_at IS NULL`（controller 层）；全部读写用 `?` 占位（`db.prepare(...)`） | ✅ |
| 🎨 自定义配色覆盖层 | `preferences.custom_theme` JSON 存 `{light:{...}, dark:{...}}`；Settings「自定义配色」卡片按 8 大前台区域分组逐项微调（取色器 + HEX 输入框，`#RGB`/`#RRGGBB` 校验规范化），未改项继承预设，亮/暗各配一套，可单项/整组还原 | ✅ |
| 🎯 字段级独立 CSS 变量 | 顶栏/Hero/统计卡片/搜索框/置顶/分类/书签/页脚均有独立变量，未自定义时 `var(--独立变量, var(--全局变量))` 回退；统计卡片数字与文字拆分（`--stat-ink/--stat-site-text/--stat-cat-ink/--stat-cat-text`）互不串色 | ✅ |
| 🎉 节日彩蛋 | `useFestival.js`（lunar-javascript 农历 + 公历）检测春节/元宵/中秋/圣诞/万圣/端午等 24 个节日，`FestivalDecor.vue` 按分组渲染灯笼/雪花/月亮/蝙蝠/emoji 飘落；`preferences.festival_enabled` 后台开关 | ✅ |
| ⏰ 闲置书签标记 | `POST /api/links/:id/visit` 点击上报写 `links.last_visited`；前台 LinkItem 超 30 天未访问显示红色时钟角标（`--link-idle`），悬浮提示天数；`preferences.idle_mark_enabled` 后台开关 | ✅ |
| 🩺 健康检查端点 | `GET /api/health` 返回 status / db / uptime，供监控探活 | ✅ |
| 🔒 后端 API 限流 | 自研零依赖内存限流 `middlewares/rateLimit.js`（Map 存 IP 计数 + 定期清理）；`POST /auth/login` 与 `POST /vault/unlock` 各 5 次/分钟/IP，超限 429 统一 `{code,message,data}` 格式 + 日志告警；反代部署需 `app.set('trust proxy', ...)`（环境变量 `TRUST_PROXY`，nginx/frpc 同机推荐 `loopback`），否则限流按 127.0.0.1 计数会误伤全站 | ✅ |
| 🩺 链接可用性检测 | `utils/linkHealth.js` 直连检测（HEAD→GET 降级、5s 超时、UA）+ DoH 辅助判定（腾讯 doh.pub→阿里 223.5.5.5→Google，区分"被墙/真挂"）；`links` 表 `health_status/last_check_at/health_note` 3 列；单链接 `POST /:id/check` 同步返回 + 批量 `POST /check-all` 后台任务（4 并发分片 + 内存态进度）+ `GET /check-progress` 轮询；LinkManage 新增「访问次数/最近访问/健康状态」3 列（🟢正常 🟡需代理 🔴打不开 ⚪未检测 + 行内检测按钮 + 页头批量检测），移动端卡片同步 | ✅ |
| 📊 访问统计面板 | `visit_logs` 明细表 + `links.visit_count` 冗余计数；`/stats/overview` 扩展总访问/今日访问（6 卡片）；`/stats/links/top` 热门 TOP N、`/stats/visits/trend` 每日趋势（缺天补 0）；Dashboard 6 卡片 + vue-echarts 趋势折线图 + 本周热门 TOP5 榜单 | ✅ |
| 🔗 批量删除 | 分类/书签管理页表格多选批量删除（PC 表格勾选 + 移动端每卡片独立复选框，全选后显示「删除选中」按钮）；逻辑抽离 `composables/useBatchOps.js` 两页复用 | ✅ |
| 🔁 URL 判重 | `links.url_normalized` 规范化指纹列 + 索引 + 存量回填；`utils/urlNormalize.js` 规范化（协议统一/hostname 小写/去默认端口/去尾部斜杠/去默认首页/查询参数排序/去锚点/去跟踪参数）；创建/编辑命中返回业务码 409 + 前端确认框（`force:true` 强制保存）；导入 skip/overwrite 策略复用判重 | ✅ |
| 📷 图片懒加载 | favicon `<img>` 统一加 `loading="lazy"`（`FaviconImage.vue` + `FaviconAvatar.vue`），减少首屏请求数与布局偏移（CLS） | ✅ |
| 🗑️ 回收站 | 分类/书签软删除后进回收站：`GET /trash`（LEFT JOIN 聚合活跃/回收书签数 + 服务端分页）、`POST /:id/restore` 恢复、`DELETE /:id/purge` 彻底删除（purge 按外键顺序清理：visit_logs → links → categories）；CategoryManage/LinkManage「回收站」tab 切换 + 行内恢复/彻底删除 + 移动端同步 | ✅ |
| 🌐 站点信息设置 | `preferences.site_title/site_logo/site_keywords/site_description/site_copyright/stat_tagline`；App.vue 动态写 `document.title` + `link.icon` + SEO meta；TopBar 图 Logo/文字 Logo 双形态（上传图片后隐藏渐变方块）+ Footer 版权回退；Settings「站点设置」卡片（标题/关键词/描述 + Logo 上传≤512KB base64/更换/移除）；`POST/DELETE /api/preferences/logo` | ✅ |
| 🩺 定时巡检（开发点 #09） | `preferences.health_config`（enabled/intervalHours/batchSize/deadStreak/recheckDead/tlsYellowDays/tlsRedDays/coldDays）；[scheduler.js](file:///d:/PersonHub/server/src/utils/scheduler.js) 按间隔巡检（默认 24h，可选 48/72/每周），分片优先最久未检；`fail_streak` 连续失败判死（down 终态）；TLS 证书到期预警（黄 30 天 / 红 7 天）；`GET /stats/links/health` + `GET /stats/links/cold`；任务锁防重入（手动批量与定时互斥） | ✅ |
| 🔔 通知中心（渠道/事件解耦，开发点 #10） | `preferences.notification_config`：channels（serverchan / email）+ events（patrol / gitBackup）；[notifier.js](file:///d:/PersonHub/server/src/utils/notifier.js) `sendEvent` 统一入口 + 渲染器注册表 + 旧结构两级自动迁移；独立菜单 /admin/notify；巡检结果 + Git 备份结果双事件，定时/手动备份均推送；`POST /preferences/notify-test` 按 channel 测试；发送失败静默不阻塞业务 | ✅ |
| 💾 自动备份体系（开发点 #07） | [backup.js](file:///d:/PersonHub/server/src/utils/backup.js) `VACUUM INTO` 一致快照 + favicons/logo 复制 + `retainCount`（默认 5）份数清理；[gitBackup.js](file:///d:/PersonHub/server/src/utils/gitBackup.js) Git 异地推送（remote 自动同步 + HEAD:branch + 60s 超时 + 失败静默）；数据管理独立菜单 /admin/data（Git 初始化引导 + 一键复制命令）；备份记录三态徽章；结果推送通知中心 | ✅ |
| 🩺 历史巡检报告（开发点 #10） | `patrol_reports` 表每轮巡检/批量检测写一行（保留 100 轮）；`GET /stats/patrol/reports`（分页/趋势）+ `GET /stats/patrol/reports/:id`（详情）；PatrolReports.vue 近 10 轮堆叠柱状图 + 报告列表 + 异常明细弹窗 | ✅ |
| 🩺 异常链接批量处理（开发点 #10） | `GET /links/issues` 异常分页列表（严重度排序）；`POST /links/batch-check` 批量重检（复用任务锁）；`PUT /links/health/reset` 批量重置；IssueManage.vue PC 表格多选 / 移动端卡片勾选 + 底部固定批量操作栏 | ✅ |
| ⏱️ 巡检间隔可配置 | `health_config.intervalHours` ∈ **24 / 48 / 72 / 每周（168）**，前后端默认值与校验白名单同步，旧值加载自动规范化 | ✅ |
| 🧩 浏览器集成扩展（候选池 #42/#44） | `extension/` Manifest V3：右键菜单收藏（页面/链接）+ popup 一键收藏（当前页 URL/标题 + 分类下拉 + 备注 + 登录 + 409 重复确认）；服务器地址/Token 存 `chrome.storage.local`；CORS 放行 `chrome-extension://` 来源（仍需 JWT）；#43 接管新标签页曾实施后移除（Chrome 强制提示无法隐藏） | ✅ |

---

## 八、开发进度全景

> 把原「六步开发计划」按**当前真实完成度**重写。每项标：🟩 已完成 · 🟨 进行中 · 🟥 待办。

### 8.1 基础平台层（原计划第 1、3 步）— 🟩 全部完成

| 子项 | 实际落地情况 | 状态 |
|---|---|---|
| 后端初始化（Express + 路由分层 + middlewares） | Node.js 原生 ESM；`app.js` 挂载 auth/validate/errorHandler；7 条路由文件分模块（auth 含保险库/links 含 favicon/categories/preferences/stats/engines/importExport；2026-08-10 已合并 favicon→links、vault→auth，URL 前缀不变前端零改动，备份见 `server/_backup_api_merge/`） | 🟩 |
| 数据库驱动 + 建表 + 种子数据 | `node:sqlite` `DatabaseSync` 同步驱动；`initDatabase()` 5 张表 + `seedDatabase()` 6 分类 24 书签 8 搜索引擎 | 🟩 |
| 数据库迁移框架 | `migrateDatabase()` try/catch 幂等补列，已有 25 次迁移记录（vault 系列、theme_preset、favicon_path、favicon_status、view_mode、font_*、note、festival_enabled、festival_countdown_enabled、last_visited、idle_mark_enabled、custom_theme、visit_count、visit_logs 表、site_* 站点信息、stat_tagline、url_normalized 等） | 🟩 |
| JWT 认证中间件 | `authMiddleware` 校验 `Authorization: Bearer <token>`，401 统一返回 | 🟩 |
| 登录 + 改密链路 | POST `/auth/login`；`password_changed_at` 机制强制改密；Settings 页面改密表单 | 🟩 |
| 前端骨架（Vite + Vue3 `<script setup>` + Pinia + Router） | 落地，4 个 store（auth/data/prefs/vault）| 🟩 |
| Axios 拦截器 | 10s 超时、断网提示"网络不可用"、401 自动跳登录、JWT 注入 | 🟩 |
| tokens.css CSS 变量体系 | 全部走 CSS 变量 + `clamp()` 响应式间距；blob/玻璃态/噪点均保留 | 🟩 |
| Naive UI 后台主题 overrides | 6 套主题的 naiveOverrides 覆盖 common/Layout/Menu/Input/Card/DataTable | 🟩 |

### 8.2 核心业务层（原计划第 2、5 步）— 🟩 全部完成 + 超出

| 子项 | 原计划 | 实际落地 | 状态 |
|---|---|---|---|
| 分类接口 | CRUD 4 个 | **8 个**：`POST/PUT/DELETE` + `GET`（公开）；`PUT /:id/lock`（保险锁）；🗑️ 回收站 `GET /trash` + `POST /:id/restore` + `DELETE /:id/purge`（彻底删除按外键顺序清理）；删除级联软删书签返回 `deletedLinks` | 🟩 |
| 书签接口 | CRUD + pin 4 个 | **12 个**：`POST/PUT/DELETE` + `PUT /:id/pin`；`PUT /:id/lock`（保险锁）；📊 `POST /:id/visit`（埋点）；🩺 `POST /:id/check` + `POST /check-all` + `GET /check-progress`（可用性检测）；🗑️ 回收站 `GET /trash` + `POST /:id/restore` + `DELETE /:id/purge`；所有删除走软删除 | 🟩 |
| 统计接口 | 1 个 overview | 3 个：overview（linkCount/categoryCount/pinnedCount/recentLinks + totalVisits/todayVisits）+ `links/top` TOP N + `visits/trend` 每日趋势 | 🟩 |
| 偏好接口 | 2 个（get/put） | 字段扩展：`theme` 支持 auto、`engine_display_count`（1-10）、`theme_preset`（6 套配色 key）、`view_mode`、`font_family`、`font_switch_enabled`、`hero_tagline`、`festival_countdown_enabled`、`custom_theme`、`site_title/site_logo/site_keywords/site_description/site_copyright/stat_tagline` + `POST/DELETE /logo` | 🟩 |
| 前台交互 | 跳转+搜索+快捷键+偏好 | 保险锁弹窗解锁 + 配色预设切换（亮暗保持色系一致）+ 快捷键帮助（?） | 🟩（超出） |

### 8.3 页面还原层（原计划第 4 步）— 🟩 全部完成

| 子项 | 状态 | 备注 |
|---|---|---|
| TopBar + Hero + PinStrip + SearchBar | 🟩 | Hero 时段问候 + 搜索框嵌 Hero 内 |
| 分类网格（响应式 1/2/3 列） | 🟩 | 新增 CategoryGrid 容器组件 |
| LinkItem（头像/名称/域名） | 🟩 | 🔒 锁分类/书签支持 + 跳转动画 |
| 🔒 解锁弹窗 | 🟩（新增） | 原方案没写，实际实现 VaultUnlockModal |
| ShortcutHint + NoteCard | 🟩 | 新增 ShortcutHelp 帮助大面板（?）|
| BackgroundDecor（blob/噪点/玻璃） | 🟩 | 抽成独立组件 `<BackgroundDecor />` |
| Footer | 🟩 | 版本号 + 手写签名 |

### 8.4 后台管理层（原计划第 6 步）— 🟩 全部完成 + 超出

| 原计划页面 | 状态 | 扩展点 |
|---|---|---|
| 登录（TopBar 弹窗） | 🟩 | 统一走前台 TopBar 登录弹窗（无 /login 路由）；首次改密检测 + 保险库初始化引导 |
| Dashboard.vue 数据概览 | 🟩 | 6 卡片（书签/分类/置顶/近7天新增/总访问量/今日访问）+ 近 7 天访问趋势折线图（vue-echarts）+ 本周热门 TOP5 + 🩺 健康总览 + 异常链接清单 |
| CategoryManage.vue | 🟩 | 拖拽排序 + 🔒 保险锁切换 + 批量删除 + 🗑️ 回收站 tab（CategoryTrashTable 子组件：恢复/彻底删除 + 分页） |
| LinkManage.vue | 🟩 | 筛选（分类搜索/关键词/健康状态组合）+ 🔒 保险锁切换 + 批量删除 + 🗑️ 回收站 tab（LinkTrashTable 子组件）+ 🩺 行内检测/批量检测 + 📊 访问次数/最近访问/健康状态 3 列 + 备注列 |
| Settings.vue（已拆分） | 🟩（大幅超出）| 62 行容器 + 6 子组件：SettingsBasic（站点信息/Logo）· SettingsAppearance（亮暗+6套配色+自定义配色+字体）· SettingsFrontDisplay（字体开关/Hero标语/节日/闲置）· SettingsDataManage（导入导出）· SettingsVault（保险库）· SettingsPassword（改密） |
| EngineManage.vue | 🟩（**原方案未登记，实际新增**）| 搜索引擎 CRUD + 启禁 + 拖拽排序 + 设置前台展示数量 |
| Layout.vue 后台骨架 | 🟩（原 AdminSidebar）| Naive UI Layout 抽屉式侧边栏 + 主题注入 + 菜单 8 项（含数据管理/链接巡检/通知中心） |

### 8.5 原方案"未登记"但实际已实现的功能清单（增量补充）

| 功能模块 | 落地细节 | 实现位置 |
|---|---|---|
| 🔒 保险库功能 | 功能开关 + 独立 bcrypt 密码 + lock_version 失效 Token + 前台解锁弹窗 + 分类/书签两级锁 | `vaultController.js` + `VaultUnlockModal.vue` + `vault.js` store |
| 🔍 搜索引擎管理 | 8 个默认种子引擎 + CRUD + 拖拽排序 + 启禁 + 前台引擎展示数量控制 | `engineController.js` + `EngineManage.vue` |
| 🎨 6 套配色主题组 | 琥珀青松/薄荷清凉/暮色橙橘/深海静谧/原木森野/葡萄紫韵，每套 light+dark 变体；旧预设 key LEGACY_KEY_MAP 迁移 | `themePresets.js` + `useThemePreset.js` |
| 💾 数据导入导出 | JSON 双向导入导出 + Chrome/Edge Netscape HTML 书签双向；导入支持全量替换/合并 | `importExportController.js` + Settings 上传/下载 UI |
| 🔐 改密守卫链 | `password_changed_at` 为空 → 登录即强制跳 Settings；Axios 拦截器配合路由守卫实现 | `auth.js` store + router beforeEach |
| 🌓 自动主题模式 | `theme === 'auto'` 时 19:00-07:00 暗色、其余时间亮色 | `prefs.js` + App.vue `effectiveIsDark` 计算 |
| 🖼️ Favicon 抓取体系 | 文件存储+数据库存路径；多源回退（cccyun→DuckDuckGo→站点直抓→Google S2）；三级缓存（内存→磁盘→远程）；失败缓存24h；定时清理7天；兜底API+创建/编辑时手动获取按钮 | `faviconFetcher.js` + `faviconController.js` + `FaviconImage.vue` + LinkManage.vue |
| 📝 网页标题获取 | `GET /api/links/fetch-title` 流式HTML解析（64KB限制，5s超时）；表单手动按钮触发，获取后同步头像文字 | `linkController.js` + LinkManage.vue |
| 🌐 域名显示全局开关 | `preferences.show_domain`（1/0）；书签管理域名列表头 `n-switch` 控制；前台 LinkItem 按 `v-if` 显隐 | `prefsController.js` + `prefs.js` store + LinkManage.vue + LinkItem.vue |
| 💀 加载骨架屏 | `SkeletonCard.vue` 模拟分类卡片布局 + shimmer 扫光；`dataStore.isLoading` 时显示 6 个骨架卡片 | `SkeletonCard.vue` + `Home.vue` |
| 📊 多视图模式 | `preferences.view_mode`（card/list/compact）；CategoryCard `.links` class 切换布局（卡片/列表/紧凑）；TopBar 循环切换按钮 | `prefs.js` store + `CategoryGrid.vue` + `CategoryCard.vue` + `TopBar.vue` |
| 🔀 多模式排序 | `preferences.category_sort_mode/link_sort_mode`（sort_order:asc 默认 / created_at / name 各升降序）；TopBar 排序下拉分组切换；CategoryGrid 前端 computed 排序；后台表格列头 sorter；新建/导入未填权重自动 `MAX(sort_order)+1` 排末尾，同权重 `id ASC` 兜底 | `useSort.js` composable + `CategoryGrid.vue` + `TopBar.vue` + `prefs.js` store + LinkManage.vue/CategoryManage.vue + category/link/importExport controller |
| 🔤 字体切换（后台可开关） | `preferences.font_family`（system黑体/kai楷体/serif宋体）+ `font_switch_enabled`（1/0）；App.vue `--app-font` CSS 变量 + `FONT_STACKS` 映射；TopBar 循环切换按钮，按钮按开关 v-if 显隐 | `prefs.js` store + `App.vue` + `TopBar.vue` + Settings.vue |
| ✨ 自定义 Hero 标语 | `preferences.hero_tagline`（空串=默认）；HeroSection 手写体位置显示自定义标语，无自定义回退默认；Settings 输入 + 保存/恢复默认 | `prefs.js` store + `HeroSection.vue` + Settings.vue |
| 📝 书签备注字段 | `links.note`（<=200字）；后台 LinkManage 表单输入 + 列表备注列 + 移动端展示；公开前台接口不返回（仅管理员可见） | `linkController.js` + `categoryController.js` + `LinkManage.vue` |
| 🎨 自定义配色覆盖层 | `preferences.custom_theme`（JSON `{light,dark}`）；Settings「自定义配色」卡片 8 大前台区域分组逐项微调 + 取色器/HEX 输入 + 单项还原/整组重置；`ccValue` 四级解析保证取色器默认色与真实渲染一致 | `prefsController.js` + Settings.vue + `prefs.js` store + `themePresets.js` |
| 🎯 字段级独立 CSS 变量 | 各区域独立变量 + 全局回退；统计卡片数字/文字拆分（`--stat-ink/--stat-site-text/--stat-cat-ink/--stat-cat-text`） | `tokens.css` + `HeroSection.vue` + Settings.vue |
| 🎉 节日彩蛋 | `useFestival.js`（lunar-javascript 农历 + 公历）检测 24 个节日，`FestivalDecor.vue` 分组装饰（灯笼/雪花/月亮/蝙蝠/emoji 飘落）；`preferences.festival_enabled` 后台开关 | `useFestival.js` + `FestivalDecor.vue` + Settings.vue |
| ⏰ 闲置书签标记 | `links.last_visited` + `POST /api/links/:id/visit` 公开埋点；前台 LinkItem 超 30 天未访问显示红色时钟角标（`--link-idle`）；`preferences.idle_mark_enabled` 后台开关 | `linkController.js` + `LinkItem.vue` + Settings.vue |
| 📊 访问统计面板 | `visit_logs` 明细表 + `links.visit_count` 冗余计数；`/stats/overview` 扩展总访问/今日访问；`/stats/links/top` 热门 TOP N、`/stats/visits/trend` 每日趋势；Dashboard 6 卡片 + vue-echarts 趋势图 + TOP5 榜单 | `statsController.js` + `linkController.js` + Dashboard.vue + `api/stats.js` |
| 🩺 健康检查端点 | `GET /api/health`（status / db / uptime） | `app.js` |
| 🔒 后端 API 限流 | `middlewares/rateLimit.js` 零依赖内存限流，挂 login（5 次/分钟/IP）+ vault unlock（5 次/分钟/IP）；`TRUST_PROXY` 环境变量控制反代 IP 信任（loopback 推荐） | `rateLimit.js` + `auth.js` + `vault.js` + `app.js` |
| 🔗 批量删除 | 分类/书签管理页勾选多行批量删除（PC 表格勾选 + 移动端每卡片独立复选框，全选后显示删除按钮）；🟩（部分，候选池 #23）：批量移动/批量加锁待做 | `CategoryManage.vue` + `LinkManage.vue` |
| 🩺 链接可用性检测 | `utils/linkHealth.js` 直连检测（HEAD→GET 降级、5s 超时、UA）+ DoH 辅助判定（腾讯 doh.pub→阿里 223.5.5.5/resolve→Google，确认域名存活区分被墙/真挂）；`links` 表 `health_status/last_check_at/health_note` 3 列；单链接 `POST /:id/check` 同步返回 + 批量 `POST /check-all` 后台任务（4 并发分片）+ `GET /check-progress` 轮询；LinkManage 新增「访问次数/最近访问/健康状态」3 列（🟢正常 🟡需代理 🔴打不开 ⚪未检测 + 行内检测按钮）+ 页头批量检测按钮（进度显示），移动端卡片同步 | `linkHealth.js` + `linkController.js` + `links.js` + `categoryController.js` + LinkManage.vue |
| 🔁 URL 判重 | `links.url_normalized` 规范化指纹列 + `idx_links_url_normalized` 索引 + 存量回填；`utils/urlNormalize.js` 规范化（协议统一/hostname 小写/去默认端口/去尾部斜杠/去默认首页/查询参数排序/去锚点/去跟踪参数 utm_*/fbclid）；创建/编辑命中重复返回业务码 409 + 前端确认框（确认后带 `force:true` 强制保存）；导入 skip/overwrite 策略同样基于规范化判重 | `urlNormalize.js` + `linkController.js` + `importExportController.js` + `db/index.js` + LinkManage.vue |
| 📷 图片懒加载 | favicon `<img>` 统一加 `loading="lazy"`，减少首屏请求数与布局偏移（CLS） | `FaviconImage.vue` + `FaviconAvatar.vue` |
| 🗑️ 回收站 | 分类/书签删除后进回收站：`GET /trash`（LEFT JOIN 聚合书签数 + 服务端分页）、`POST /:id/restore` 恢复、`DELETE /:id/purge` 彻底删除（purge 先清 visit_logs → 物理删 links → 删分类）；CategoryManage/LinkManage 顶部「回收站」tab 切换 + 恢复/彻底删除按钮 + 移动端同步 | `categoryController.js` + `linkController.js` + `categories.js` + `links.js` + CategoryManage.vue + LinkManage.vue |
| 🌐 站点信息设置 | `preferences` 新增 `site_title`（首页 <title>/顶栏标题/页脚版权回退）/`site_logo`（上传 base64 ≤512KB，顶栏图 Logo + 浏览器 favicon 同步）/`site_keywords`/`site_description`（SEO meta 注入）/`site_copyright`（页脚版权）；Settings「站点设置」卡片 + 上传/更换/移除 Logo；App.vue 动态写 document.title + link.icon + meta 标签 | `prefsController.js` + `preferences.js` + Settings.vue + App.vue + TopBar.vue + Footer.vue |
| 🔔 通知中心（渠道/事件解耦） | `preferences.notification_config` channels（serverchan/email）+ events（patrol/gitBackup）嵌套；`sendEvent` 统一入口 + 渲染器注册表；旧平铺结构两级自动迁移；独立菜单 /admin/notify（渠道卡片 + 事件卡片）；巡检结果 + Git 备份结果双事件，定时/手动备份都推送；测试接口按 channel 分别验证 | `utils/notifier.js` + `prefsController.js` + `preferences.js` + `scheduler.js` + `backupController.js` + NotifyCenter.vue + NotifyChannels.vue + NotifyEvents.vue |
| 💾 自动备份体系 | #84 本地快照（`VACUUM INTO` + favicons/logo 复制 + retainCount 份数清理）+ #85 Git 异地（remote 自动同步 + HEAD:branch 推送 + 60s 超时 + 失败静默）；数据管理独立菜单 /admin/data 含 Git 初始化引导；备份记录三态徽章（本地 / 本地+已推送 Git / 本地+Git 未推送）；结果推送通知中心 | `utils/backup.js` + `utils/gitBackup.js` + `backupController.js` + `routes/backup.js` + DataManage.vue + BackupManager.vue + `api/backup.js` |
| ⏱️ 巡检间隔可配置 | `health_config.intervalHours` 由 2/6/12/24 改为 **24 / 48 / 72 / 每周（168）小时**；前后端默认值与校验白名单同步，旧值加载自动规范化 | SettingsHealth.vue + `scheduler.js` + `prefsController.js` + `preferences.js` |
| 设置页卡片折叠 + 合并 | CollapsibleCard 组件（标题栏整行点击展开/收起，localStorage 按标题记忆）；Settings 从 5 卡片（基本/前台显示/外观主题/网页配色/自定义配色/保险库/修改密码）合并为 3 卡片（站点设置/外观设置/安全设置），每卡片内部分段切换子项，外观联动自定义配色锁定当前模式 | SettingsSite.vue、SettingsAppearance.vue、SettingsSecurity.vue、CollapsibleCard.vue |
| 🔢 视图布局开关 | 后台设置默认布局（card/list/compact），前台手动切换仅本地生效（刷新保留），清除浏览器数据后回落默认；关闭开关时全站统一默认布局，前台按钮隐藏 | prefs.js store + SettingsSite.vue + TopBar.vue |
| 🔀 前台排序开关 | 后台控制排序下拉按钮显隐，关闭时全站统一「自定义顺序」(sort_order)；刷新立即生效 | prefs.js store + SettingsSite.vue + TopBar.vue |
| ↑↓ 分类上移/下移 | 后台表格/移动端卡片 ↑↓ 按钮，按显示顺序全量重新编号 sort_order=1..N（根除导入数据重复值导致移动无效 bug），前台刷新后顺序即时生效 | CategoryManage.vue + categoryController.js + categories.js |
| 🖼️ 书签图标上传 | 512KB 限制，createImageBitmap 解码期缩放 64px 防卡顿，`up-` 前缀文件名区分手动上传与自动抓取 | LinkFormModal.vue + linkController.js + links.js |
| 🔗 书签名称新标签页打开 | renderAvatar 改为 `<a target="_blank">`，书签管理/链接巡检/回收站三处同步，悬停变色提示（无下划线） | useRenderCell.js + LinkManage.vue + admin-common.css |

### 8.6 开发点清单（待办功能登记）

> 开发点统一在 [第十章](#十开发点记录待办功能清单) 查阅完整方案。下方是进度追踪。

| 编号 | 开发点名称 | 一期（MVP） | 二期（打磨） | 三期（进阶） |
|---|---|---|---|---|
| #01 | 📊 访问统计 + 🩺 可用性检测 | 🟩 一期完成（✅ 埋点：`POST /:id/visit` + `visit_logs` 明细 + `links.visit_count` 冗余计数；✅ TOP5 + 每日趋势 + Dashboard 6 卡/折线图；✅ 单链接检测 `POST /:id/check` + 批量检测 `POST /check-all` + 进度 `GET /check-progress`；✅ 列表 3 列：访问次数/最近访问/健康状态 + 行内检测按钮 + 页头批量检测按钮）| 🟩 二期完成（✅ 定时巡检 + fail_streak 判死 + /health 总览 + TLS 告警 + 冷链接筛选，由 #09 落地）| 🟥 待办（可用性折线图 + 冷链接一键清理；检测历史表已由 #10 的 patrol_reports 实现）|
| #02 | 🔁 URL 判重 + 🔎 搜索历史补全（候选池 #22/#9） | 🟩 URL 判重已完成（✅ `utils/urlNormalize.js` 规范化指纹 + `url_normalized` 列 + 409 业务码 + 前端确认框 force 强制保存 + 导入策略复用，见 8.5）；🟥 搜索历史自动补全 ~1h | — | — |
| #03 | ⌨️ 快捷命令搜索 + 命令面板（候选池 #8/#38） | 🟥 待办（`g xxx`/`gh xxx` 等前缀快捷命令 ~2h）| 🟥 待办（Cmd+K 全局命令面板：搜书签/跳分类/切主题 ~4h）| — |
| #04 | 🧰 实用小工具：每日金句 + 老虎机 + 二维码（候选池 #73/#75/#74） | 🟥 待办（hitokoto 每日金句 ~0.5h + 老虎机随机跳转 ~0.5h）| 🟥 待办（书签 hover 二维码，手机扫码打开 ~1h）| — |
| #05 | 🕘 最近访问栏（候选池 #2，依赖 #01 的 last_visited） | 🟥 待办（前台顶部"最近访问 5 个"横滑栏 ~1.5h）| — | — |
| #06 | 📱 PWA 支持（候选池 #16） | 🟥 待办（manifest + service worker 离线缓存 + 可安装提示，~2h）| — | — |
| #07 | 💾 自动备份体系（候选池 #84/#85） | 🟩 一期已完成（✅ 每日 03:00 本地一致快照 + favicons/logo 复制 + retainCount 份数清理 + 手动触发/列表 + 数据管理独立菜单 /admin/data + Git 初始化引导）| 🟩 二期已完成（✅ Git 异地推送私有仓库：remote 自动同步 + HEAD:branch + 60s 超时 + 失败静默 + 记录三态徽章 + 结果推送通知中心）| — |
| #08 | 📈 数据可视化进阶：热力图 + 年鉴 + 时间线（候选池 #32/#30/#29） | 🟥 待办（时段热力图 GitHub 风格 ~4h）| 🟥 待办（个人上网年鉴 Spotify Wrapped 风格 ~6h）| 🟥 待办（收藏时间线视图 ~3h）|
| #09 | 🩺 链接巡检二期（开发点 #01 二期，2026-08-10 实施） | 🟩 已完成（✅ 定时巡检（24/48/72/每周）✅ fail_streak 判死 ✅ TLS 证书预警 ✅ /health 总览 ✅ /cold 冷链接 ✅ SettingsHealth 巡检卡片 8 可配置项 + 立即巡检）| — | — |
| #10 | 🩺 巡检增强 + 通知中心（2026-08-11 实施） | 🟩 已完成（✅ 异常链接集中查看 + 组合筛选 ✅ 批量重检/重置/删除 ✅ 历史巡检报告 patrol_reports ✅ 通知中心渠道/事件解耦（/admin/notify）✅ 巡检间隔 24/48/72/每周）| — | — |
| #11 | 🧩 浏览器集成扩展（候选池 #42/#44） | 🟩 一期已完成（✅ 右键菜单收藏 + popup 一键收藏 + 服务器地址/登录配置 + 分类下拉 + 409 重复确认 + CORS 扩展来源放行）| — | 🟥 待办（#43 接管新标签页：曾实施后移除，Chrome 强制提示无法隐藏）|

### 8.7 整体完成度

```
原「六步开发计划」  100%  🟩🟩🟩🟩🟩🟩
增量补充功能         39 项   🔒保险库 / 🔍引擎管理 / 🎨6套配色 / 💾导入导出 / 🔐改密守卫 / 🌓自动主题 / 🖼️Favicon抓取 / 📝标题获取 / 🌐域名开关 / 💀骨架屏 / 📊多视图 / 🔤字体切换(含开关) / ✨Hero标语 / 📝书签备注 / 🎨自定义配色覆盖层 / 🎯字段级独立变量 / 🎉节日彩蛋 / ⏰闲置书签标记 / 📊访问统计面板 / 🩺健康检查 / 🔒API限流 / 🔗批量删除 / 🩺链接可用性检测 / 🔁URL判重 / 📷图片懒加载 / 🗑️回收站 / 🌐站点信息设置 / 🔔通知中心(渠道/事件解耦) / 💾自动备份(Git异地) / ⏱️巡检间隔(24/48/72/周) / 🩺历史巡检报告 / 🩺异常链接批量处理 / 📖部署测试报告与第十二章部署配置 / 🧩浏览器集成扩展(右键+一键收藏) / 📦设置卡片折叠+合并 / 🔢视图布局开关 / 🔀前台排序开关 / ↑↓分类上移下移 / 🖼️书签图标上传 / 🔗书签名称新标签页打开
开发点 #01           一期+二期 100%  🟩 一期（埋点/建表/统计面板/单链+批量检测/列表 3 列）+ 二期（定时巡检/fail_streak 判死/TLS 预警/健康总览/冷链接，由 #09 落地）
开发点 #02           🔁 URL 判重 100% 🟩；🔎 搜索历史 🟥 待办  ✅ 规范化判重已落地（url_normalized + 409 + force 确认）；搜索历史自动补全约 1h 待做，见第十章 #02 完整方案
开发点 #07           🟩 已完成  ✅ 本地一致快照 + Git 异地推送 + 数据管理独立菜单 + 备份结果推送通知中心（见第十章 #07）
开发点 #09/#10       🟩 已完成  ✅ 巡检二期（24/48/72/周定时巡检 + fail_streak 判死 + TLS 预警 + 历史报告 + 异常批量处理）+ ✅ 通知中心（/admin/notify：Server酱 + QQ 邮箱双渠道，巡检 + Git 备份双事件）（见第十章 #09/#10）
开发点 #11           🟩 已完成  ✅ Chrome/Edge 扩展（右键菜单收藏 + popup 一键收藏 + 登录/分类/重复确认 + CORS 扩展来源放行）（见 `extension/`）
开发点 #03-#06/#08   🟥 待办  搜索历史、快捷命令/命令面板、实用小工具、最近访问栏、PWA、数据可视化进阶（见第十章完整方案）
部署就绪度           ⚠️ 有条件通过  S1 环境配置为唯一部署阻塞项；M1-M4 与 L1-L11 已全部修复/复核（见第十三章部署测试报告）
```

**下一步建议（从 0 到 1 价值最高的下一件事）**：开发点 #01 一期 + 二期巡检（#09/#10）、#07 自动备份、#11 浏览器集成扩展已全部完成，当前待办为候选池轻量项：**搜索历史自动补全**（约 1h，依赖 #02 已就绪）与**最近访问栏**（约 1.5h，依赖 #01 的 last_visited 已就绪），可先落地提升日常使用体验；随后再推进 #03 命令面板 / #04 小工具 / #06 PWA / #08 可视化进阶。**正式部署到公网前必须先完成第十二章部署前配置检查（S1 阻塞项）**。

---

## 九、AI 写代码约束

详见独立文档：[AI代码约束.md](./AI代码约束.md)

---

## 十、开发点记录（待办功能清单）

> 后续新增功能统一在此登记，按「优先级/工作量/收益」落地。每个开发点含：目标、数据设计、API、前端交互、估算工时、优先级。

---

### 开发点 #01：链接访问统计 + 可用性检测（2026-08-09 登记）

**一句话目标**：知道哪些链接"最常用 / 已沉睡 / 打不开"，为用户提供自动热门、死链预警、冷链接清理依据。

**模块归属**：统计模块（stats）+ 书签模块（links）扩展。

**整体评估**：好做，中等工作量、收益明确；MVP 一晚可跑通，完整约 15 工时。

---

#### 1. 数据设计（SQLite）

> **一期已落地（简化方案）**：实际建表为 `visit_logs`（id / link_id / visited_at，无 referer/source），`links` 表加 `visit_count` 冗余计数 + `health_status` / `last_check_at` / `health_note` 3 个可用性检测列；对应接口为 `GET /api/stats/links/top`、`GET /api/stats/visits/trend`、`POST /api/links/:id/check`、`POST /api/links/check-all`、`GET /api/links/check-progress`（见第五节 API 表）。下方 `link_visits + link_stats` 双表方案保留，作为二期扩展（冷链接筛选 / 可用性画像 / 半年归档）参考。

在 `db/index.js` → `migrateDatabase()` 中追加以下 DDL（CREATE TABLE IF NOT EXISTS / ALTER TABLE 按现有 try/catch 幂等模式）。

```sql
/* -------- 访问统计表（时序明细，半年归档） -------- */
CREATE TABLE IF NOT EXISTS link_visits (
  id           INTEGER PRIMARY KEY AUTOINCREMENT,
  link_id      INTEGER NOT NULL,
  visited_at   DATETIME DEFAULT CURRENT_TIMESTAMP,
  referer      TEXT,
  source       TEXT DEFAULT 'frontend',   -- frontend / admin
  FOREIGN KEY (link_id) REFERENCES links(id)
);
CREATE INDEX IF NOT EXISTS idx_link_visits_link ON link_visits(link_id);
CREATE INDEX IF NOT EXISTS idx_link_visits_time ON link_visits(visited_at);

/* -------- 链接画像表（每链接一行聚合，读多写少） -------- */
CREATE TABLE IF NOT EXISTS link_stats (
  link_id             INTEGER PRIMARY KEY,
  visit_count         INTEGER DEFAULT 0,
  last_visited_at     DATETIME,
  visit_count_7d      INTEGER DEFAULT 0,
  visit_count_30d     INTEGER DEFAULT 0,
  -- 可用性检测字段
  health_status       TEXT DEFAULT 'unknown',  -- unknown / ok / warning / down
  last_checked_at     DATETIME,
  http_status         INTEGER,
  response_ms         INTEGER,
  redirect_count      INTEGER DEFAULT 0,
  tls_expires_at      DATETIME,
  fail_streak         INTEGER DEFAULT 0,       -- 连续失败次数，>=3 判死链
  error_message       TEXT,
  FOREIGN KEY (link_id) REFERENCES links(id)
);
```

> 查询默认带 `WHERE deleted_at IS NULL`（controller 层基础查询封装），画像表通过 link_id 与 links 软删除自然对齐。

---

#### 2. 后端 API 设计

**访问统计**

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| POST | `/api/links/:id/visit` | 否 | 前台点击时上报（参数可选 source/referer），写入明细并同步画像计数 |
| GET | `/api/stats/links/visits?period=7d&limit=10` | 是 | TOP N 热门 + 访问量（按 period 聚合） |
| GET | `/api/stats/links/:id/visits?days=30` | 是 | 单链接按天趋势折线数据 |
| GET | `/api/stats/links/cold?days=90` | 是 | 冷链接清单（N 天未访问 / 访问极少） |

**可用性检测**

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| POST | `/api/admin/links/check/:id` | 是 | 立即检测单个链接，同步返回结果 |
| POST | `/api/admin/links/check-all` | 是 | 批量检测全部（后台分片执行，结果写 link_stats） |
| GET | `/api/stats/links/health` | 是 | 总览：ok/warning/down 数量、即将过期 TLS、失败 TOP 列表 |
| GET | `/api/admin/links/:id/health-log` | 是 | 单链接最近 20 次检测详情（可选二期） |

**核心检测服务 `checkUrl(url, opts)` 要点**（Node 内置 `http/https` 模块，零新依赖）：
- URL 合法性校验（`new URL()`）
- HEAD 优先，405 回退 GET + 响应头到达即 abort（不下载整页）
- 3xx 跟随重定向，`redirect_count` 计数（超过 5 记 warning）
- 超时 8s：`req.setTimeout(8000, destroy)`
- 状态分类：2xx=ok；401/403=warning（鉴权站常如此）；404/5xx/DNS/CONN/TIMEOUT=down
- 403 且含 Cloudflare 特征 → warning 而非 down（防误报）
- HTTPS：`socket.getPeerCertificate().valid_to` 写入 `tls_expires_at`
- fail_streak 连续失败 >= 3 → health_status=down，<3 → warning；成功即归零
- 并发策略：批量默认并发 5，同 domain 节流 500ms（防目标风控）
- file:// / localhost / 内网 IP → 标记 skipped，不检测

**定时任务**（`server/src/utils/scheduler.js`，app.js 末尾 import）：
- 每日 02:00 聚合：重算 7d/30d 访问量，清理 180 天前访问明细
- 定时巡检（默认每 24 小时，可选 48/72/每周）：按 `last_checked_at` 升序分片处理，每次 50 条

---

#### 3. 前端交互设计

**访问统计**
- [LinkItem.vue](file:///d:/PersonHub/web/src/components/bookmark/LinkItem.vue)：父组件 Home 监听 `open` 事件，fire-and-forget 调 `linkApi.reportVisit(id)` 再跳转（不阻塞，失败静默）。
- 管理 Dashboard：在现有 4 卡片旁加 2 张——「🔥 本周热门 TOP5」「🧊 冷链接预警 N 条」。
- LinkManage 列表：加两列「访问次数」「最近访问」，筛选栏新增 NSelect（热门/冷链接/死链）。

**可用性检测**
- LinkManage 列表：新增「健康状态」列（🟢ok 🟡warning 🔴down ⚪unknown），n-tooltip 展示详情（状态码/耗时/最后检测），右上角新增「批量检测」按钮。
- 单链接：行内「手动检测」小按钮，toast 返回结果并刷新列。
- 死链提示（前台可选）：LinkItem 右下角微缩点指示，死链点击前弹确认「该站点上次检测不可用，仍要打开？」。
- TLS 到期告警：tooltip 展示「证书 86 天后过期」，<=30 天黄，<=7 天红。

---

#### 4. 代码落位（与现有结构一致）

```
server/src/
├── controllers/
│   ├── visitController.js      # 访问统计接口（4 个）
│   └── healthController.js     # 可用性检测接口（4 个）+ checkUrl 工具
├── routes/
│   ├── stats.js                # 扩展：GET /links/visits、/links/:id/visits、/links/cold、/links/health
│   └── links.js                # 扩展：POST /:id/visit（公开）、/:id/check（鉴权）
├── utils/
│   └── scheduler.js            # 聚合任务（02:00）+ 健康巡检（每 24 小时，可选 48/72/每周），日志 [时间][模块][操作][结果]
└── db/
    └── index.js                # migrateDatabase() 追加 DDL

web/src/
├── api/
│   ├── link.js                 # reportVisit(id)、checkLink(id)、batchCheckLinks()
│   └── stats.js                # getLinkStats(period/limit)、getLinkVisitTrend(id,days)、getColdLinks(days)、getHealthOverview()
├── views/admin/
│   ├── Dashboard.vue           # 2 张新卡片（TOP5 热门 + 冷链接预警）
│   └── LinkManage.vue          # 两列(统计+健康)+ 筛选 + 批量/手动检测按钮 + tooltip
└── views/front/
    └── Home.vue                # LinkItem open 事件中上报访问（不阻塞跳转）
```

---

#### 5. 工作量估算 & 落地优先级

| 阶段 | 内容 | 工时 |
|---|---|---|
| **MVP 一期**（必做） | 建表迁移 + visit 上报 + 单链接手动检测 + Dashboard TOP5 + 管理列表 2 列 + 状态徽章 | ~7h |
| **打磨二期** | 6 小时定时巡检 + fail_streak 自动判死 + /health 总览 + TLS 告警 + 冷链接筛选 | ~5h |
| **进阶三期**（看需求） | 检测历史表 + 30 天可用性折线 + 冷链接一键清理 + 自定义检测频率 | ~3h |

**合计**：~15h。推荐先做一期（一周内一个晚上+一个周末即可上线）。

---

#### 6. 约束合规清单（对照 AI 代码约束）

- [x] **参数化 SQL**：所有读写用 `?` 占位，禁止字符串拼接
- [x] **routes/只定义 controllers/写 DB**：新接口全挂 visitController / healthController
- [x] **express-validator 校验**：POST /visit 校验 id 是 int；check-id 校验 id 是 int
- [x] **软删除过滤**：`WHERE deleted_at IS NULL` 写在 controller 基础封装
- [x] **管理员接口 JWT**：除 /:id/visit 外全加 `authMiddleware`
- [x] **顶层 errorHandler 捕获**：controller 内部 `throw new Error(msg)`，统一输出 `{code, message, data}`
- [x] **日志格式**：定时任务、检测批处理、聚合任务 全部 `[时间][模块][操作][结果]` 输出 console
- [x] **前端请求走 api/**：新增 API 方法全落在 `api/stats.js` / `api/link.js`，不裸调 axios
- [x] **超时/断网**：Axios 现有拦截器（10 秒超时 + "网络不可用"提示）自动覆盖

---

### 开发点 #02：URL 判重（✅ 已完成）+ 搜索历史补全（待办）（候选池 #22/#9，2026-08-10 登记）

**一句话目标**：①新增/编辑书签时自动识别重复地址，防止冗余收藏；②前台搜索框记住最近 10 条搜索词并自动补全，减少重复输入。

**模块归属**：书签模块（links）扩展 + 搜索模块（SearchBar / useSearch）。

**整体评估**：URL 判重已完成（0.5h 实际落地）；搜索历史纯前端工作量小、体验提升明显。

---

#### 1. 数据设计（URL 判重 — ✅ 已落地）

> 无需独立表，直接复用 links 表迁移追加列。

```sql
-- 迁移：links 表补 url_normalized 列（URL 规范化指纹，用于重复链接检测）
ALTER TABLE links ADD COLUMN url_normalized TEXT;
-- 判重索引（软删除数据不参与判重）
CREATE INDEX IF NOT EXISTS idx_links_url_normalized ON links(url_normalized);
-- 存量数据回填：只补 url_normalized IS NULL 的行（个人站数据量小，一次性跑完）
UPDATE links SET url_normalized = ? WHERE id = ?;
```

**URL 规范化规则**（`server/src/utils/urlNormalize.js` 的 `normalizeUrl()`，协议统一→hostname 小写→去默认端口→去尾部斜杠→去默认首页（index.html/index.php 等）→查询参数排序→去锚点→去跟踪参数（utm_*/fbclid 等）→无协议时自动补 https:// 重试）：

| 原始 URL | 规范化结果 |
|---|---|
| `HTTPS://WWW.Google.Com:443/?b=2&a=1#frag` | `https://www.google.com/?a=1&b=2` |
| `https://example.com/index.html` | `https://example.com/` |
| `https://example.com/?utm_source=x` | `https://example.com/` |
| `example.com/path` | `https://example.com/path` |

#### 2. 后端 API 设计（URL 判重 — ✅ 已落地）

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| POST | `/api/links` | 是 | 创建时 `normalizeUrl(url)` 查重，命中且未带 `force` → 返回 **HTTP 200 + 业务码 409** `{code:409, message:'已存在相同地址的书签', data:{duplicate:{id,name,url}}}` |
| PUT | `/api/links/:id` | 是 | 编辑时按最终 URL 规范化查重（`id != ?` 排除自身），命中逻辑同上 |
| POST | `/api/import/json` | 是 | skip 策略按 `url_normalized` 跳过重复；overwrite 策略先软删再插入 |
| POST | `/api/import/bookmarks` | 是 | 同上复用 `normalizeUrl` 判重 |

> 业务码 409 与 HTTP 400 语义区分：HTTP 层始终 200（避免浏览器控制台打印接口信息），业务层用 `code` 字段区分。

#### 3. 前端交互设计（URL 判重 — ✅ 已落地）

- [LinkManage.vue](file:///d:/PersonHub/web/src/views/admin/LinkManage.vue)：提交新建/编辑时若 `res.code === 409`，弹出确认框展示重复书签（id/name/url），用户确认后带 `force:true` 重发请求强制保存；取消则关闭表单。

#### 4. 代码落位（URL 判重 — ✅ 已落地）

```
server/src/
├── utils/urlNormalize.js          # normalizeUrl() 规范化 + 去跟踪参数
├── controllers/linkController.js  # createLink/updateLink 判重（409 + force）
├── controllers/importExportController.js  # 导入 skip/overwrite 复用判重
└── db/index.js                    # migrateDatabase() 幂等补列 + 存量回填

web/src/
└── views/admin/LinkManage.vue     # 409 确认框 + force 重试
```

---

#### 5. 数据设计（搜索历史 — 🟥 待办，纯前端，无需建表）

```
localStorage key：cozy-nav-search-history
结构：["关键词1", "关键词2", ...]（最多 10 条，去重后按最近在前排序）
```

#### 6. 前端交互设计（搜索历史 — 🟥 待办）

- [SearchBar.vue](file:///d:/PersonHub/web/src/components/search/SearchBar.vue)：聚焦输入框且已有输入时，下拉展示匹配的历史词（filter 模糊匹配）；点击历史词直接回填搜索；提交搜索时把关键词写入历史（去重 + 截断 10 条）。
- 键盘导航：↓/↑ 选择、Enter 确认，Esc 关闭下拉。
- 历史词左侧图标 🕘，可整清空（点击清空按钮）。
- 不涉及后端；`composables/useSearchHistory.js` 封装读写。

#### 7. 工作量估算 & 优先级

| 阶段 | 内容 | 工时 |
|---|---|---|
| URL 判重（已完成） | 规范化工具 + 409 业务码 + 前端确认框 + 导入复用 | 0.5h ✅ |
| 搜索历史 MVP | localStorage 读写 + 下拉匹配 + 键盘导航 | ~1h |

**合计**：URL 判重 0.5h 已落地；搜索历史约 1h。优先级：高（#9 在推荐 Top 15 第 4 位）。

#### 8. 约束合规清单（搜索历史）

- [x] 纯前端实现，无 SQL / 无鉴权需求
- [x] 前端逻辑抽离 `composables/useSearchHistory.js`（复杂逻辑入 composables）
- [x] 样式走 tokens.css CSS 变量，不做硬编码颜色
- [x] 搜索历史属个人本地数据，不上传（隐私友好）

---

### 开发点 #03：快捷命令搜索 + 命令面板（候选池 #8/#38，2026-08-10 登记）

**一句话目标**：①搜索框输入 `g xxx`/`gh xxx`/`b xxx` 等前缀快捷命令直达指定引擎；②`Ctrl/Cmd+K` 呼出全局命令面板，搜书签/跳分类/切主题一步到位。

**模块归属**：搜索模块（SearchBar）+ 效率工具（新增 CommandPalette 组件）。

**整体评估**：键盘党核心需求，价值高；一期内置常见前缀 + 二期做全量命令面板。

---

#### 1. 数据设计

- 前缀命令映射：**前端静态配置**（`composables/useShortcutSearch.js`），无需建表。

```js
// 内置命令前缀表（与 search_engines.key 关联，缺失引擎 key 时忽略该前缀）
const COMMAND_PREFIXES = {
  g:  { key: 'google', desc: 'Google 搜索' },
  gh: { key: 'github', desc: 'GitHub 搜索' },
  b:  { key: 'baidu', desc: '百度搜索' },
  // 可选扩展：y=YouTube、z=知乎、a=AI
};
```

- 命令面板命令清单：静态数组 `{ id, label, icon, hint, action }`（切主题/切视图/跳分类/搜书签等）。

#### 2. 后端 API 设计

> 一期无需新接口（前缀命令仅前端拦截）；二期命令面板「搜书签」复用 `GET /api/categories`（公开接口已有数据）。

#### 3. 前端交互设计

**快捷命令搜索（一期）**
- [SearchBar.vue](file:///d:/PersonHub/web/src/components/search/SearchBar.vue)：输入以 `前缀+空格` 开头时，识别前缀并展示引擎提示（如「使用 Google 搜索「xxx」」）；回车后用对应引擎 URL 模板打开。
- 前缀表从 `useShortcutSearch.js` 读取，`search_engines.key` 在前端已有的 engines 列表中存在才启用（避免失效前缀）。

**命令面板（二期）**
- 新增 [CommandPalette.vue](file:///d:/PersonHub/web/src/components/ui/CommandPalette.vue)：`Ctrl/Cmd+K` 与 `/` 可呼出（`useShortcut.js` 扩展绑定）；顶部输入框 + 命令列表。
- 命令分组：
  - **跳转**：跳首页 / 跳后台各管理页（需登录）
  - **操作**：切换主题（亮/暗/自动）、切换视图（card/list/compact）、切换字体（system/kai/serif）
  - **搜索**：搜书签（本地过滤 → 选中后新标签打开）／跳分类（锚点滚动）
- 键盘：↑↓ 选择、Enter 执行、Esc 关闭；移动端底部弹出（Vant Popup）。

#### 4. 代码落位

```
web/src/
├── composables/
│   ├── useShortcutSearch.js      # 前缀命令映射 + 解析（一期）
│   └── useCommandPalette.js      # 命令列表构建 + 执行分发（二期，复用 prefs/data store）
├── components/search/SearchBar.vue  # 前缀识别 + 引擎提示（一期）
├── components/ui/CommandPalette.vue # 命令面板弹层（二期）
└── composables/useShortcut.js       # 扩展 Ctrl/Cmd+K 绑定
```

#### 5. 工作量估算 & 优先级

| 阶段 | 内容 | 工时 |
|---|---|---|
| 一期 | 前缀命令解析 + 引擎提示 + 回车直达 | ~2h |
| 二期 | Cmd+K 命令面板（命令列表 + 键盘导航 + 移动端弹层） | ~4h |

**合计**：~6h。优先级：一期高（Top 15 第 9 位）、二期中。

#### 6. 约束合规清单

- [x] 无新后端接口（复用公开 categories/engines 数据）
- [x] 复杂逻辑入 composables（useShortcutSearch / useCommandPalette）
- [x] 组件命名 PascalCase，UI 组件放 components/ui/
- [x] 样式走 CSS 变量，键盘快捷键复用 useShortcut 注册模式

---

### 开发点 #04：实用小工具——每日金句 + 老虎机 + 二维码（候选池 #73/#75/#74，2026-08-10 登记）

**一句话目标**：①Hero 区显示每日一句（hitokoto）；②🎰 老虎机按钮随机跳转一个书签；③书签 hover 显示二维码，手机扫码打开。

**模块归属**：Hero 区域（HeroSection）+ 书签（LinkItem）。

**整体评估**：三个小功能独立、见效快、零后端表设计（金句走第三方 API / 老虎机走现有数据 / 二维码走前端库）。

---

#### 1. 数据设计

- 金句：`localStorage key: cozy-nav-quote-{YYYY-MM-DD}` 按天缓存（命中当天直接显示，不重复请求）。
- 老虎机：复用现有书签数据（`GET /api/categories` 已返回全部书签）。
- 二维码：`navigator.clipboard` 之外用前端库 **qrcode**（npm 包）或 `https://api.qrserver.com` 图片接口生成。

#### 2. 后端 API 设计

- 金句：直接调 **hitokoto API**（`https://v1.hitokoto.cn/?encode=json`）——**前端调用，不经后端**（避免后端出站请求再引入 SSRF 面）；若改经后端代理，则必须走 `ssrfGuard` 白名单校验。
- 老虎机 / 二维码：复用现有接口，无新增。

#### 3. 前端交互设计

**每日金句**
- [HeroSection.vue](file:///d:/PersonHub/web/src/components/hero/HeroSection.vue)：手写体标语下方小字展示金句（灰色、14px、淡入动画）；点击可换一句；无网络时静默隐藏。
- `composables/useQuote.js` 封装：按天缓存 + 失败静默 + 5s 超时。

**老虎机 🎰**
- HeroSection 手写体标语旁加 🎰 按钮：点击随机取一个未锁定书签，新标签打开并上报 `POST /:id/visit`。
- 连续两次点击尽量不重复（记录上一次 index）。

**二维码**
- [LinkItem.vue](file:///d:/PersonHub/web/src/components/bookmark/LinkItem.vue)：PC hover 书签条目时右下角出现小二维码图标，点击弹出 n-popover/qr 浮层（`qrcode` 生成 dataURL，`qr` 小卡片 96×96px，下方显示完整 URL 截断 + 复制按钮）；移动端长按唤起。
- 二维码链接 = 书签 url，编码用 `encodeURIComponent`。

#### 4. 代码落位

```
web/src/
├── composables/
│   ├── useQuote.js            # hitokoto 拉取 + 按天缓存
│   └── useRandomLink.js       # 老虎机随机不重复逻辑
├── components/
│   ├── hero/HeroSection.vue   # 金句展示 + 🎰 按钮
│   └── bookmark/LinkItem.vue  # hover 二维码浮层
└── package.json               # 新增依赖：qrcode（约 40KB）
```

#### 5. 工作量估算 & 优先级

| 内容 | 工时 |
|---|---|
| hitokoto 每日金句（缓存 + 静默失败 + 点击换句） | ~0.5h |
| 老虎机随机跳转（不重复 + 访问上报） | ~0.5h |
| 书签 hover 二维码（qrcode 生成 + 浮层 + 移动端长按） | ~1h |

**合计**：~2h。优先级：中高（金句/老虎机在 Top 15 第 5/6 位）。

#### 6. 约束合规清单

- [x] 金句外部 API 前端直连，**不新增后端出站面**（如需后端代理必须过 ssrfGuard）
- [x] 二维码链接只做 `encodeURIComponent`，不做 `v-html`（防 XSS）
- [x] 复杂逻辑入 composables；样式走 CSS 变量
- [x] 新增依赖 `qrcode` 评估体积与兼容性后引入

---

### 开发点 #05：最近访问栏（候选池 #2，依赖 #01，2026-08-10 登记）

**一句话目标**：前台顶部展示最近访问的 5 个书签横滑栏，一键回到高频站点。

**模块归属**：前台布局（Home）+ 书签（links）。

**整体评估**：数据已就绪（`last_visited` + `visit_logs`），纯前端展示 + 一个小公开接口，价值高成本低。

---

#### 1. 数据设计

> 复用现有 `links.last_visited`（前台点击上报已写入）+ `visit_logs`。无需新表。

#### 2. 后端 API 设计

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| GET | `/api/links/recent?limit=5` | 否（公开） | 返回 `last_visited IS NOT NULL` 且按 `last_visited DESC` 取前 N 个，仅含 id/name/url/favicon_path/avatar_text/avatar_color/domain，不含 note 等敏感字段 |

> 在 `links.js` 路由新增一条公开 GET；controller 在 `linkController.js` 新增 `getRecentLinks`。查询默认 `WHERE deleted_at IS NULL AND is_locked = 0`（锁书签不展示）。

#### 3. 前端交互设计

- [Home.vue](file:///d:/PersonHub/web/src/views/front/Home.vue) + 新增 [RecentStrip.vue](file:///d:/PersonHub/web/src/components/pin/RecentStrip.vue)：
  - 位置：PinStrip 下方、搜索栏上方，横滑（`overflow-x: auto`，与 PinStrip 一致），卡片更小（favicon 32px + 名称）。
  - 无访问记录（接口返回空数组）时整栏隐藏。
  - 点击 = 普通书签点击：新标签打开 + 上报 `POST /:id/visit`。
  - 标题「🕘 最近访问」，右侧「全部」链接跳后台书签管理（需登录）。
- `api/link.js` 新增 `getRecentLinks(limit)`。

#### 4. 代码落位

```
server/src/
├── controllers/linkController.js  # getRecentLinks（公开 + 过滤锁书签）
└── routes/links.js                # GET /recent

web/src/
├── api/link.js                    # getRecentLinks(limit)
├── components/pin/RecentStrip.vue # 最近访问横滑栏
└── views/front/Home.vue           # 数据加载 + 挂载 RecentStrip
```

#### 5. 工作量估算 & 优先级

| 内容 | 工时 |
|---|---|
| 后端公开接口（过滤锁书签 + 字段裁剪） | ~0.5h |
| 前端横滑栏（骨架 + 空态隐藏 + 点击上报） | ~1h |

**合计**：~1.5h。优先级：高（Top 15 第 12 位）。

#### 6. 约束合规清单

- [x] 公开接口字段裁剪（不含 note/favicon_path 敏感之外内容，不含锁书签）
- [x] express-validator 校验 `limit` 为 1-20 的 int
- [x] 所有查询 `WHERE deleted_at IS NULL`；排序用 `last_visited DESC`
- [x] 前端请求走 `api/link.js`；样式走 CSS 变量 + 与 PinStrip 一致的横滑动效

---

### 开发点 #06：PWA 支持（候选池 #16，2026-08-10 登记）

**一句话目标**：manifest.json + Service Worker，手机可"添加到主屏幕"，离线打开导航页。

**模块归属**：工程化 / 前端构建（Vite）。

**整体评估**：Vite 生态成熟（vite-plugin-pwa），部署后收益明确；注意用户偏好：**PC 与移动端均不显示"添加到主屏幕"系统提示条**（此前已按该约束隐藏），PWA 仅提供可安装能力，不主动弹提示。

---

#### 1. 数据设计

> 无数据库改动。

```
web/public/（或 vite-plugin-pwa 自动生成）
├── manifest.webmanifest   # name/short_name/theme_color/background_color/icons(192/512)
└── icons/icon-192.png、icon-512.png（占位用现成 svg 转 png 或在线生成）
```

- `theme_color` 与 `background_color` 取自 tokens.css 当前 `--bg` 变量默认值（琥珀青松暗色 `#1A1410`），避免启动白屏与主题色撕裂。
- `start_url: '/'`，`display: 'standalone'`，`scope: '/'`。

#### 2. 后端 API 设计

> 无新增。Service Worker 缓存策略：`networkFirst` 兜底静态资源；API 请求一律 `networkOnly`（不缓存动态数据，保证实时性）。

#### 3. 前端交互设计

- 引入 `vite-plugin-pwa` + `vite-plugin-pwa/client`：
  - `manifest` 按上面配置；`registerType: 'autoUpdate'`（有新版本静默更新，不打断用户）。
  - `workbox`：`navigateFallback` 指向 `/`（SPA 路由刷新可用）；`globPatterns` 缓存打包产物。
  - **不加** `beforeinstallprompt` 弹窗逻辑（用户已明确：PC 端与移动端都不要"添加到主屏幕"提示）。图标显示仅在浏览器菜单"安装应用"入口可见。
- App.vue 挂载 `virtual:pwa-register` 注册 SW；`devOptions.enabled: true` 便于本地调试。

#### 4. 代码落位

```
web/
├── vite.config.js               # vite-plugin-pwa 配置（manifest + workbox）
├── src/main.js                  # registerSW（autoUpdate，静默）
└── public/icons/icon-192.png / icon-512.png
```

#### 5. 工作量估算 & 优先级

| 内容 | 工时 |
|---|---|
| vite-plugin-pwa 配置 + manifest + 图标 | ~1h |
| SW 缓存策略调优 + 本地/生产验证 | ~1h |

**合计**：~2h。优先级：中（Top 15 第 10 位；本地开发收益低，部署后收益高）。

#### 6. 约束合规清单

- [x] 不实现安装提示条（遵循用户 PC/移动端均不显示偏好）
- [x] API 请求不缓存（防陈旧数据）；只缓存静态资源与导航回退
- [x] manifest 主题色与背景色取自主题变量，避免白屏
- [x] 部署后需要 https（SW 要求安全上下文），部署方案第十二章已含 https 说明

---

### 开发点 #07：自动备份体系（候选池 #84/#85，2026-08-10 登记，2026-08-11 已实施 ✅）

> **实施状态（2026-08-11）**：#84 本地备份 + #85 Git 异地备份全部落地。
> - 独立菜单「数据管理」（/admin/data）[DataManage.vue](file:///d:/PersonHub/web/src/views/admin/DataManage.vue) 含导出/导入 + [BackupManager.vue](file:///d:/PersonHub/web/src/components/admin/data/BackupManager.vue) 自动备份卡片（Git 初始化引导步骤 + 一键复制命令）
> - 本地备份：`VACUUM INTO` 一致快照 + favicons/logo 复制 + 按 `retainCount`（默认 5）份数清理，毫秒时间戳防同秒冲突
> - Git 异地：推送前自动 `remote set-url` 同步仓库地址、`HEAD:branch` 兼容默认分支名、60s 超时、失败静默；备份记录三态徽章（本地 / 本地+已推送 Git / 本地+Git 未推送）
> - 备份结果自动推送通知中心（`gitBackup` 事件），定时与手动「立即备份」均触发

**一句话目标**：数据每日自动备份（一致快照 + favicons），按保留份数清理；二期支持 Git 私有仓库自动提交。

**模块归属**：后端定时任务（scheduler）+ 运维。

**整体评估**：个人数据安全底线，成本低收益高；一期自备份、二期 Git 版本化。

---

#### 1. 数据设计

> 无新表。备份产物落盘：

```
server/data/backups/
├── cozy-nav-2026-08-10.json      # 全量导出 JSON（categories/links/engines/preferences）
├── cozy-nav-2026-08-10.db        # 数据库文件副本（sqlite VACUUM INTO 或文件复制）
└── favicons/                     # favicon 目录副本（与数据库 favicon_path 对应）
```

- 保留策略：实施后由"7 天"改为 **`retainCount` 份数**（默认 5 份，超出的删最旧，毫秒时间戳文件名防同秒冲突）。

#### 2. 后端 API 设计（✅ 已实施，实际接口）

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| GET | `/api/backup/config` | 是 | 读取备份配置（autoEnabled/retainCount/gitEnabled/gitRemote/gitBranch + dataDir + gitRepoReady） |
| PUT | `/api/backup/config` | 是 | 保存备份配置（Git 推送前自动 `remote set-url` 同步仓库地址） |
| POST | `/api/backup/run` | 是 | 手动触发备份（本地一致快照 + 按配置推 Git），返回本次备份文件 |
| GET | `/api/backup/list` | 是 | 列出 backups/ 目录现有备份（文件名 + 大小 + 时间 + hasGit 推送标记） |

**定时任务**（`server/src/utils/scheduler.js`，与健康巡检同一调度器）：
- 每日 **03:00**：`VACUUM INTO` 生成 DB 快照 + 复制 favicons/logo；按 `retainCount` 清理最旧份。
- 日志格式 `[时间][备份][操作][结果]`。

#### 3. 前端交互设计（✅ 已实施，独立菜单）

- **独立菜单「数据管理」/admin/data**：[DataManage.vue](file:///d:/PersonHub/web/src/views/admin/DataManage.vue) 含导出/导入 + [BackupManager.vue](file:///d:/PersonHub/web/src/components/admin/data/BackupManager.vue) 自动备份卡片。
- 自动备份开关（默认开）+ 保留份数 + Git 异地开关（仓库地址/分支 + 初始化引导步骤 + 一键复制命令）；「立即备份」按钮 + 最近备份记录（三态徽章：本地 / 本地+已推送 Git / 本地+Git 未推送）。
- 移动端：Naive UI 自适应布局，与其余后台卡片同风格。

#### 4. 代码落位（✅ 已实施）

```
server/src/
├── controllers/backupController.js  # config get/save + run + list（dataDir/gitRepoReady 探测）
├── routes/backup.js                 # 4 条路由（鉴权 + express-validator）
├── utils/backup.js                  # 本地备份（VACUUM INTO + favicons/logo + 份数清理）
├── utils/gitBackup.js               # Git 异地推送（remote 自动同步 + HEAD:branch + 60s 超时）
├── utils/scheduler.js               # 每日 03:00 备份任务 + 备份结果通知
├── utils/notifier.js                # notifyGitBackup → sendEvent('gitBackup')
└── data/backups/                    # 产物目录（启动时确保存在）
```

#### 5. 工作量估算 & 优先级

| 阶段 | 内容 | 工时 |
|---|---|---|
| 一期 | 定时 DB 一致快照 + favicons/logo 复制 + retainCount 份数清理 + 手动触发/列表 | ~1.5h |
| 二期 | Git 自动 commit 到私有仓库（child_process 调 git + 密钥注入） | ~3h |

**合计**：~4.5h。优先级：一期高（Top 15 第 14 位）、二期中。

#### 6. 约束合规清单

- [x] 删除/下载备份严格校验文件名（`path.basename` + 白名单正则），防路径穿越
- [x] 备份接口全部 `authMiddleware` + express-validator
- [x] 定时任务日志 `[时间][备份][操作][结果]` 格式
- [x] DB 副本用 SQLite 安全方式（`VACUUM INTO` 或先校验文件可写再复制），避免备份中写库损坏
- [x] favicons 目录随备份复制，保证 favicon_path 引用完整性

---

### 开发点 #08：数据可视化进阶——热力图 + 年鉴 + 时间线（候选池 #32/#30/#29，2026-08-10 登记）

**一句话目标**：①GitHub 风格访问时段热力图；②年底 Spotify Wrapped 风格个人上网年鉴；③按月份组织的收藏时间线视图。

**模块归属**：统计模块（stats）+ Dashboard + 前台视图。

**整体评估**：数据源全部就绪（`visit_logs` 时序明细 + `links` 创建时间），可视化增强为主，工作量大但价值高。

---

#### 1. 数据设计

> 复用 `visit_logs`（热力图/年鉴数据源）与 `links.created_at`（时间线数据源），无新表。

#### 2. 后端 API 设计

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| GET | `/api/stats/visits/heatmap?days=365` | 是 | 返回近 N 天每日访问数 `{ days, data: [{ date: '2026-01-01', count: 5 }] }`，无访问天补 0（与 trend 同结构，前端直绘热力图） |
| GET | `/api/stats/annual?year=2026` | 是 | 年鉴聚合：总访问、最常访问 TOP10、分类占比、月度趋势、首次/最后一次访问时间 |
| GET | `/api/links/timeline` | 是 | 按月份聚合书签 `{ months: [{ month: '2026-08', links: [...] }] }`（公开版可不含锁书签） |

> 注意：`visit_logs` 半年归档策略（见 #01 二期）会截断 180 天前明细，年鉴按年统计需在归档前取数或调整归档策略（#01 三期评估：保留 2 年明细）。

#### 3. 前端交互设计

**时段热力图（一期）**
- Dashboard 新增「📅 访问热力图」卡片：vue-echarts `heatmap`（x=周一~周日，y=0~23 时，色阶深浅），tooltip 显示"周三 14 点 · 12 次"。
- 与现有趋势折线同 `echarts-vendor` 分包，不额外增首屏体积。

**个人上网年鉴（二期）**
- 每年 12 月底自动生成卡片 + 手动入口（Dashboard 右上「生成年鉴」）：报告页含年度总览数字、TOP 站点榜、分类占比饼图、月度趋势、emoji 词云。
- 前端 `views/admin/AnnualReport.vue`，全屏滚动长页 + 分享按钮（导出图片 `canvas.toDataURL`）。

**收藏时间线视图（二期）**
- 前台视图模式扩展第四种「timeline」：按 `created_at` 月份分组的纵向时间轴（`view_mode` 扩展或独立页面 `/timeline`）。
- CategoryCard/Home 按月份标题分组渲染，无书签月份不显示。

#### 4. 代码落位

```
server/src/
├── controllers/statsController.js  # getHeatmap / getAnnual / getTimeline
└── routes/stats.js                 # 3 条新路由（鉴权）

web/src/
├── api/stats.js                    # getHeatmap / getAnnual
├── api/link.js                     # getTimeline
├── views/admin/Dashboard.vue       # 热力图卡片（一期）
├── views/admin/AnnualReport.vue    # 年鉴报告页（二期）
├── router/index.js                 # 新增 /admin/annual 路由（二期）
└── composables/useTimeline.js      # 月份分组逻辑（二期）
```

#### 5. 工作量估算 & 优先级

| 阶段 | 内容 | 工时 |
|---|---|---|
| 一期 | 热力图（heatmap 聚合 + Dashboard 卡片） | ~4h |
| 二期 | 个人上网年鉴（聚合接口 + 报告页 + 导出图片） | ~6h |
| 三期 | 收藏时间线视图（月份分组 + 新视图模式） | ~3h |

**合计**：~13h。优先级：中（Top 15 第 16-18 位递补项，等 #01 二期归档策略定稿后做更稳）。

#### 6. 约束合规清单

- [x] 聚合查询全部参数化 SQL；大表查询走索引（`idx_visit_logs_time`）
- [x] 热力图/年鉴接口鉴权（管理员）；公开时间线接口裁剪敏感字段
- [x] 图表复用 echarts-vendor 分包，不新增全局依赖
- [x] 年鉴导出图片仅客户端 canvas 生成，无后端存储需求

---

### 开发点 #09：链接可用性检测 · 二期定期巡检（开发点 #01 二期，2026-08-10 实施，2026-08-11 登记 ✅）

**一句话目标**：把"打不开 / 证书快过期"的发现自动化——定时巡检（默认每 24 小时，可选 48/72/每周）+ `fail_streak` 连续失败自动判死 + TLS 到期告警 + 健康总览 `/health` + 冷链接筛选，让死链发现不再依赖人工点击检测。

> **实施状态（2026-08-10）**：步骤 1-7 全部完成，备份 `server\_backup_health_patrol\` + `web\_backup_health_patrol\`；后端重启后调度器日志 `[巡检] [启动] 每 24 小时巡检一轮`（当时默认 6 小时，2026-08-11 已调整为 24/48/72/每周）正常；`GET /api/stats/links/health`（total/ok/blocked/fail/down/skip/tlsExpiring/failTop）与 `GET /api/stats/links/cold?days=90` 均返回 200；`GET /api/preferences` 返回 `health_config` 8 项默认值；前端 `npm run build` 通过。

**模块归属**：书签模块（links）+ 统计模块（stats）扩展。

**整体评估**：好做，改动集中后端（检测服务增强 + 调度器 + 2 个统计接口），前端仅展示层补充；预计 ~5h。

---

#### 1. 现状与缺口（一期已落地项）

| 已有能力 | 位置 |
|---|---|
| 检测服务 `checkLinkHealth(url)` → `{status: ok/blocked/fail/skip, httpCode, ms, note}`（直连 5s + DoH 二次判定，适配国内网络） | [linkHealth.js](file:///d:/PersonHub/server/src/utils/linkHealth.js) |
| 单条检测 `POST /api/links/:id/check`、批量 `POST /check-all`、进度 `GET /check-progress`（内存态 `batchTask`，并发 4，上限 500 条） | [linkController.js](file:///d:/PersonHub/server/src/controllers/linkController.js) |
| links 表 3 个检测列：`health_status` / `last_check_at` / `health_note` | [db/index.js](file:///d:/PersonHub/server/src/db/index.js) |
| 前端健康列 + 行内检测 + 批量检测进度条 | [LinkManage.vue](file:///d:/PersonHub/web/src/views/admin/LinkManage.vue) |

**二期缺口**：无定时调度器（`scheduler.js` 未建）；无 `fail_streak` / `tls_expires_at` 字段；`checkLinkHealth` 未做 TLS 证书读取、未接 SSRF 内网黑名单、无连续失败判定；无 `/stats/links/health` 与 `/stats/links/cold` 接口。

---

#### 2. 数据设计（db/index.js 迁移，幂等 try/catch 模式，仿照现有 417-433 行写法）

```sql
-- links 表追加
ALTER TABLE links ADD COLUMN fail_streak INTEGER DEFAULT 0;      -- 连续失败次数
ALTER TABLE links ADD COLUMN tls_expires_at DATETIME;            -- HTTPS 证书到期日

-- preferences 表追加：巡检配置（JSON 集中一个字段，避免拆散列）
ALTER TABLE preferences ADD COLUMN health_config TEXT;  -- JSON：{ enabled, intervalHours, batchSize, deadStreak, recheckDead, tlsYellowDays, tlsRedDays, coldDays }

-- 巡检分片排序 + 状态过滤索引
CREATE INDEX IF NOT EXISTS idx_links_last_check_at ON links(last_check_at);
CREATE INDEX IF NOT EXISTS idx_links_health ON links(health_status);
```

---

#### 3. 检测服务增强（utils/linkHealth.js）

1. **TLS 证书读取**：对 https 链接用 Node 原生 `https.request`（HEAD）完成一次握手，读 `res.socket.getPeerCertificate().valid_to` 写入 `tlsExpiresAt` 返回。undici 的 `fetch` 不暴露 peer certificate，故单独实现一个小函数（`getTlsExpiry(url)`）。
2. **SSRF 收敛**：接入 [ssrfGuard.js](file:///d:/PersonHub/server/src/utils/ssrfGuard.js)，`file:// / localhost / 内网 IP` → 返回 `status:'skip'`（部署前检查项 S2，必须随巡检一并落地）。
3. **连续失败判定**：返回结构扩展为 `{ status, httpCode, ms, note, tlsExpiresAt, dead }`，`dead = 原 fail_streak >= 2 且本次仍 fail`。

**`persistHealth` 升级规则**（controller 写库逻辑）：

| 本次结果 | fail_streak 处理 | health_status |
|---|---|---|
| ok / blocked / skip | 归 0 | ok / blocked / skip |
| fail 且 原 streak < 2 | +1 | fail |
| fail 且 原 streak >= 2 | +1 | **down**（终态死链） |

> `blocked`（域名仍可解析、疑似被墙）不计入连续失败，避免被墙站点被误判为死链。

---

#### 4. 共享批量执行器（utils/healthRunner.js 新建）

抽出手动批量与定时巡检共用的执行逻辑，消除两处重复：

```js
// 对外暴露：
createBatchTask(total)             // 内存态任务对象（现 linkController 的 batchTask 逻辑搬入）
runHealthBatch(links, { throttle })// 并发 4 + 同域名节流 500ms，每条 persistHealth + 进度回调 + 任务锁
```

- [linkController.js](file:///d:/PersonHub/server/src/controllers/linkController.js) 的 `checkAllLinks` 改调 `runHealthBatch`，去掉 500 条上限改全量分片（行为不变，`check-progress` 进度接口照旧）。
- 任务锁：`healthRunner` 内单一 `activeTask`，手动批量与定时巡检互斥，`running` 时新任务直接拒绝/跳过。

---

#### 5. 定时巡检调度器（utils/scheduler.js 新建）

- [app.js](file:///d:/PersonHub/server/src/app.js) 末尾引入（与现有 `setInterval(cleanExpiredFailedRecords, 24h)` 同模式）。
- **按配置间隔巡检（默认每 24 小时，可选 48/72/每周）**，每轮从 `preferences.health_config` 读取最新配置（开关/间隔/批条数/判死阈值/死链重检），改动即时生效、无需重启。
- 取数分片（批条数用配置 `batchSize`，默认 50）：
  `WHERE deleted_at IS NULL AND (health_status IS NULL OR health_status != 'down') ORDER BY last_check_at ASC LIMIT ?`
  优先最久未检；`down` 终态默认不再重复请求（`recheckDead=false` 时）。
- 防重入：`patrolRunning` 标志 + `runHealthBatch` 任务锁双保险。
- 日志格式：`[时间][巡检][执行][成功] ok=x blocked=y fail=z down=w`。

---

#### 6. 可配置项（用户可掌控与设置）

> 8 个可配置项统一存于 `preferences.health_config`（JSON），后台 Settings「🩺 链接巡检」卡片修改，保存即时生效，无需重启。

| # | 配置项 | 说明 | 默认值 | 可调范围 |
|---|---|---|---|---|
| 1 | `enabled` | 是否启用定时巡检 | `true` | true / false |
| 2 | `intervalHours` | 巡检间隔（小时） | `24` | 24 / 48 / 72 / 168（每周） |
| 3 | `batchSize` | 每轮检测条数 | `50` | 20~500 |
| 4 | `deadStreak` | 连续失败判死阈值 | `3` | 2~10 |
| 5 | `recheckDead` | 判死后是否纳入后续巡检 | `false` | true / false |
| 6 | `tlsYellowDays` | TLS 黄色预警剩余天数 | `30` | 7~90 |
| 7 | `tlsRedDays` | TLS 红色预警剩余天数 | `7` | 1~30 |
| 8 | `coldDays` | 冷链接判定天数 | `90` | 30~365 |

- 同一卡片提供「立即巡检」按钮：手动触发一轮，不占用定时节奏。
- **不开放的系统级参数**（安全/性能底线）：并发 4、同域名节流 500ms、直连超时 5s、DoH 超时 3s、SSRF 内网黑名单。

---

#### 7. 后端 API（routes/stats.js + statsController.js）

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| GET | `/api/stats/links/health` | 是 | 总览：`{ total, ok, blocked, fail, down, skip, unknown }` + `tlsExpiring`（≤30 天黄 / ≤7 天红，含 id/name/domain/tls_expires_at）+ `failTop`（fail_streak≥1，含 id/name/domain/fail_streak/last_check_at） |
| GET | `/api/stats/links/cold?days=90` | 是 | 冷链接清单（N 天未访问或访问极少，按最后访问升序），`days` 校验 1-365 整数 |

- 均 `query()` express-validator 校验 + `validate` 中间件 + 统一 `{code, message, data}` 返回。
- 全部 SQL 参数化，`deleted_at IS NULL` 过滤贯穿。

---

#### 8. 前端交互设计

- [LinkManage.vue](file:///d:/PersonHub/web/src/views/admin/LinkManage.vue)：`healthTag` 增加 `down`（🔴 死链）分支；健康列 tooltip 补充「连续失败 N 次 / 证书 M 天后过期」。
- **Dashboard.vue**：新增「🩺 健康总览」卡片——ok/blocked/fail/down 数量 + TLS 即将到期 TOP5（点击跳 LinkManage）。
- **Settings.vue**：新增「🩺 链接巡检」卡片——8 个可配置项表单 + 立即巡检按钮（PC 表格 + 移动端 Vant 表单，与其他设置卡片同风格）。
- 冷链接筛选：LinkManage 筛选栏可选「只看冷链接」（最后做，二期核心是巡检自动化）。

---

#### 9. 代码落位（与现有结构一致）

```
server/src/
├── controllers/
│   ├── linkController.js          # 改造：checkAllLinks 改调 healthRunner，persistHealth 升级
│   ├── statsController.js         # 新增：getHealthOverview、getColdLinks
│   └── prefsController.js         # preferences 读写支持 health_config 字段
├── routes/
│   ├── stats.js                   # 新增：GET /links/health、GET /links/cold
│   └── preferences.js             # 校验 health_config 各字段范围
├── utils/
│   ├── linkHealth.js              # 增强：TLS + SSRF + dead 判定
│   ├── healthRunner.js            # 新建：共享批量执行器（并发/节流/任务锁）
│   └── scheduler.js               # 新建：定时巡检（读 health_config）+ 防重入
└── db/
    └── index.js                   # 迁移：fail_streak / tls_expires_at / health_config + 2 索引

web/src/
├── api/
│   └── stats.js                   # 新增 getHealthOverview()、getColdLinks(days)
├── views/admin/
│   ├── Dashboard.vue              # 新增健康总览卡片
│   ├── Settings.vue              # 新增「🩺 链接巡检」设置卡片（8 个可配置项 + 立即巡检）
│   └── LinkManage.vue            # down 状态 + tooltip 增强
```

---

#### 10. 工作量估算 & 实施步骤（每步完成即验证）

| 步骤 | 内容 | 工时 | 验证方式 | 状态 |
|---|---|---|---|---|
| 1 | db 迁移：字段 + 索引 | ~0.5h | 重启后端无报错，`SELECT` 新字段可用 | ✅ 已实施 |
| 2 | linkHealth 增强：TLS + SSRF + dead | ~1.5h | 手测 https 链接返回 tlsExpiresAt；内网 IP 返回 skip | ✅ 已实施 |
| 3 | healthRunner 重构 + linkController 改造 | ~1h | `check-all` + `check-progress` 回归 | ✅ 已实施 |
| 4 | scheduler 定时巡检 + app.js 挂载 | ~0.5h | 手动触发一轮，日志输出正确 | ✅ 已实施 |
| 5 | /health + /cold 统计接口 | ~1h | curl 验证 JSON 结构与校验 | ✅ 已实施 |
| 6 | 前端：down + tooltip + Dashboard 卡片 | ~0.5h | `npm run build` 通过 | ✅ 已实施 |
| 7 | 设置界面：`health_config` 读写 + Settings 巡检卡片 | ~1h | 保存后立即生效，重启后仍保留 | ✅ 已实施 |

**合计**：~6h。优先级：高（开发点 #01 二期，自动化死链发现收益明确，建议下一件事推进）。

---

#### 11. 约束合规清单

- [x] **SSRF**：巡检/手动检测统一走 `ssrfGuard`，内网地址标记 skip（部署前 S2 项随本功能落地）
- [x] **参数化 SQL**：所有读写 `?` 占位，禁字符串拼接
- [x] **鉴权 + 校验**：新接口 `authMiddleware` + express-validator + 统一 400
- [x] **日志格式**：巡检/批处理 `[时间][模块][操作][结果]`
- [x] **软删除过滤**：`WHERE deleted_at IS NULL` 贯穿巡检分片与统计查询
- [x] **顶层 errorHandler**：controller 内 `throw`，统一 `{code, message, data}`

---

### 开发点 #10：巡检二期增强 + 管理员通知（开发点 #09 的迭代，2026-08-11 登记，通知部分 2026-08-11 已实施 ✅）

> 背景：开发点 #09 巡检功能上线后，用户反馈「巡检完无法集中查看异常链接」「刷新后台进度条消失」，由此产生的二次增强；并新增巡检完成后的管理员通知（Server酱微信推送）。

#### 1. 已实施（✅ 2026-08-11）

**1.1 异常链接集中查看（解决"巡检完找不到异常链接"）**

- **后端** `statsController.getHealthOverview` 新增 `issueList` 字段：
  - 查询条件：`deleted_at IS NULL AND (health_status IS NULL OR health_status != 'ok')`
  - 排序：`down → fail → blocked → skip → 未检测`（严重度递减），再按 `last_check_at` 升序（最久未检优先）
  - 每项附带 `failStreak`（连续失败次数）与 `tls` 预警（黄色/红色阈值内时返回 daysLeft + level）
  - `LIMIT 50`
- **前端** [LinkManage.vue](file:///d:/PersonHub/web/src/views/admin/LinkManage.vue)：筛选栏新增**健康状态下拉**（🩺 全部 / 🟢 正常 / 🟡 需代理 / 🔴 打不开 / 💀 死链 / ⚪ 跳过 / ❔ 未检测），可与分类、关键词**组合筛选**
- **前端** [Dashboard.vue](file:///d:/PersonHub/web/src/views/admin/Dashboard.vue)：健康总览面板新增**异常链接清单**区块（展示前 10 条 + 状态色标签 + 证书剩余天数，超出提示去管理页筛选查看）

**1.2 巡检进度刷新恢复（解决"刷新后台进度条消失"）**

- [SettingsHealth.vue](file:///d:/PersonHub/web/src/components/admin/settings/SettingsHealth.vue) `onMounted` 时主动查询 `GET /links/check-progress`，若后端 `running=true` 则自动恢复进度条并继续轮询；手动启动与刷新恢复共用同一 `startPolling()`

**1.3 管理页数据源 Bug 修复（重要）**

- **根因**：前台首页以 `public` 模式缓存 `categories`（公开接口**不含** `health_status`），进入书签管理页时因缓存非空而**跳过重新拉取** → 表格内所有链接无健康状态字段 → 状态筛选全空，而「未检测」（判 `!health_status`）反而显示全部
- **修复**：LinkManage `onMounted` 改为**始终**以管理员模式 `fetchCategories()`，保证拿到含 `health_status` 的完整字段与最新巡检结果

**1.4 巡检设置卡片布局**

- [SettingsHealth.vue](file:///d:/PersonHub/web/src/components/admin/settings/SettingsHealth.vue) 卡片改为：PC 端**一行 4 个功能模块**、文本与控件**同一行**（label 固定宽右对齐 + 控件自适应），窄屏降级 3/2/1 列

#### 2. 通知中心（渠道/事件解耦，✅ 2026-08-11 已实施：独立菜单 /admin/notify + Server酱/QQ邮箱双渠道 + 巡检/Git备份双事件）

> **背景**：巡检通知升级为「通知中心」——把「怎么发」（渠道 channels）与「什么时候发、发什么」（事件 events）解耦，独立菜单 `/admin/notify`；未来新增推送（安全告警、统计周报等）只需新增一个事件，前端加一个事件卡片。

**架构**：
- **channels 渠道（"怎么发"，全局一份）**：
  - 📱 **Server酱**：微信推送，零依赖（Node 18+ 内置 `fetch` 调 `https://sctapi.ftqq.com/{SendKey}.send`），免费版每天限 5 条
  - 📧 **QQ 邮箱**：SMTP 发送，依赖 `nodemailer`；端口 465 用 SSL、587 用 STARTTLS；授权码在 QQ 邮箱「设置 → 账号 → 开启 IMAP/SMTP 服务」生成（非邮箱密码）
  - 可同时启用双保险，任一渠道失败不影响其他渠道与业务主流程（发送失败静默仅记日志）
- **events 事件（"什么时候发、发什么"，可扩展列表）**：
  - 🩺 **patrol 巡检结果**：`enabled` + `strategy`（`abnormal` 仅异常/`always` 总是）+ `minIssues` 异常阈值 + 标题/正文模板
  - 🛡️ **gitBackup Git 备份结果**：`enabled` + `onSuccess`/`onFailure` 开关 + 标题/正文模板（成功/失败都按开关推送）
  - 新增事件仅需：后端加默认配置 + 一个渲染器 + 调用一次 `sendEvent()`；前端加一个事件卡片

**配置存储**：`preferences.notification_config` JSON（channels/events 嵌套，旧结构两级自动迁移：最早 `serverChanKey` 平铺 → 上一代顶层平铺 → 当前 channels/events）：
```json
{
  "channels": {
    "serverchan": { "enabled": false, "sendKey": "" },
    "email": { "enabled": false, "host": "smtp.qq.com", "port": 465, "user": "", "pass": "", "to": "" }
  },
  "events": {
    "patrol": {
      "enabled": true, "strategy": "abnormal", "minIssues": 1,
      "titleTemplate": "🩺 巡检发现 {issues} 条异常",
      "bodyTemplate": "正常 {ok} · 需代理 {blocked} · 打不开 {fail} · 跳过 {skip}\\n\\n检测时间：{time}"
    },
    "gitBackup": {
      "enabled": false, "onSuccess": true, "onFailure": true,
      "titleTemplate": "🛡️ 数据备份{result}",
      "bodyTemplate": "文件：{file}\\n大小：{size}\\n{reason}时间：{time}"
    }
  }
}
```
模板占位符：patrol 支持 `{total}{ok}{blocked}{fail}{skip}{issues}{time}`；gitBackup 支持 `{result}{file}{size}{reason}{time}`。

**后端**：
- [notifier.js](file:///d:/PersonHub/server/src/utils/notifier.js)：`DEFAULT_NOTIFICATION_CONFIG` / `EVENT_PLACEHOLDERS`（占位符说明供前端）/ `getNotificationConfig`（JSON 兜底 + 两级旧结构迁移 + 深度合并）/ `sendServerChan` / `sendEmail`（nodemailer，连接池 `close` 释放）/ `sendAllChannels`（遍历启用渠道）/ `sendEvent(eventType, data)` 统一入口 + `RENDERERS` 渲染器注册表；保留兼容 `notifyPatrolResult`、新增 `notifyGitBackup`
- [healthRunner.js](file:///d:/PersonHub/server/src/utils/healthRunner.js)：批任务结束调用 `notifyPatrolResult(stat)`（手动/定时统一）
- [scheduler.js](file:///d:/PersonHub/server/src/utils/scheduler.js) + [backupController.js](file:///d:/PersonHub/server/src/controllers/backupController.js)：定时与手动「立即备份」结束均调用 `notifyGitBackup(...)`（成功/失败都按事件开关推送）
- [prefsController.js](file:///d:/PersonHub/server/src/controllers/prefsController.js)：`parseNotificationConfig` 同步两级迁移 + `testNotify`（读取 channels）
- [preferences.js](file:///d:/PersonHub/server/src/routes/preferences.js)：`isValidNotificationConfig` 严格嵌套校验（只允许已知字段，非法 400）+ `POST /api/preferences/notify-test`（`channel: serverchan|email`）

**前端**：
- 独立菜单 [NotifyCenter.vue](file:///d:/PersonHub/web/src/views/admin/NotifyCenter.vue)（/admin/notify，[Layout.vue](file:///d:/PersonHub/web/src/views/admin/Layout.vue) 菜单 + [router](file:///d:/PersonHub/web/src/router/index.js) 路由）：持有完整配置，任一卡片保存整包提交
- [NotifyChannels.vue](file:///d:/PersonHub/web/src/components/admin/notify/NotifyChannels.vue)：渠道卡片（Server酱/邮箱子模块 + 独立开关 + 测试按钮）
- [NotifyEvents.vue](file:///d:/PersonHub/web/src/components/admin/notify/NotifyEvents.vue)：事件卡片（巡检结果 / Git 备份结果 + 模板占位符说明标签）
- [HealthMonitor.vue](file:///d:/PersonHub/web/src/views/admin/HealthMonitor.vue)：移除原通知卡片；旧组件 `SettingsNotification.vue` 已删除（备份在 `web/_backup_notify_center/`）
- [prefs.js](file:///d:/PersonHub/web/src/api/prefs.js) 的 `sendTestNotify(payload)` 支持 `{channel, sendKey}` / `{channel, config}`

**巡检间隔选项调整（2026-08-11）**：由 2/6/12/24 小时改为 **24 / 48 / 72 / 每周（168）小时**；前后端默认值与校验白名单同步（`health_config.intervalHours ∈ [24,48,72,168]`），旧值加载时自动规范化为 24。

**验证**：旧平铺配置迁移成功（SendKey/邮箱/巡检策略完整保留到新结构）；`npm run build` 通过；后端重启日志正常；测试通知接口按 `channel` 分别测试 serverchan/email。

> 后续可选：站内通知记录（notifications 表 + 后台铃铛）、钉钉/企微/飞书 webhook（改 URL 即可，notifier 已抽象 `sendAllChannels`）

#### 3. 历史巡检报告（✅ 2026-08-11 已实施）

- **背景**：巡检只有"最新状态"，历史轮次结果/趋势无法追溯
- **数据**：新表 `patrol_reports`（id/started_at/finished_at/trigger_type/total/ok/blocked/fail/skip/duration_ms/issue_links），每轮巡检/批量检测结束由 [healthRunner.js](file:///d:/PersonHub/server/src/utils/healthRunner.js) `persistPatrolReport` 写入，自动保留最近 100 轮（超出清理）；`issue_links` 存本轮非正常链接快照 `[{id,status}]`
- **接口**：[statsController.js](file:///d:/PersonHub/server/src/controllers/statsController.js) `getPatrolReports`（分页 / `?limit=N` 趋势）、`getPatrolReportDetail`（关联当前链接信息，已删除标记 gone）；[stats.js](file:///d:/PersonHub/server/src/routes/stats.js) `GET /api/stats/patrol/reports`、`GET /api/stats/patrol/reports/:id`
- **前端**：[PatrolReports.vue](file:///d:/PersonHub/web/src/components/admin/health/PatrolReports.vue)（新建）近 10 轮状态堆叠柱状图 + 报告列表（时间/触发/状态/耗时/异常数）+ 详情弹窗（异常链接明细）；挂载于 [HealthMonitor.vue](file:///d:/PersonHub/web/src/views/admin/HealthMonitor.vue) 巡检页
- **验证**：小批量触发写报告成功（trigger_type/total/issue_links 正确）；列表/详情/limit 模式/404/400 均通过

#### 4. 异常链接批量处理（✅ 2026-08-11 已实施）

- **背景**：巡检后异常链接只能看，无法批量处理
- **接口**：[linkController.js](file:///d:/PersonHub/server/src/controllers/linkController.js) `getIssues`（异常链接分页列表，down/fail/blocked/skip/未检测按严重度排序）、`batchCheckLinks`（批量重检，复用 `startHealthBatch` 任务锁）、`resetHealthBatch`（批量重置：清空 health_status/fail_streak/last_check_at，排入下次巡检优先队列）；[links.js](file:///d:/PersonHub/server/src/routes/links.js) `GET /api/links/issues`、`POST /api/links/batch-check`、`PUT /api/links/health/reset`
- **前端**：[IssueManage.vue](file:///d:/PersonHub/web/src/components/admin/health/IssueManage.vue)（新建）异常链接卡片：PC 表格多选 / 移动端卡片勾选（勾选框在底部操作栏左侧）+ 底部固定批量操作栏；批量操作：重新检测（后台任务 + 进度轮询）、重置状态、删除（软删除进回收站，逐条调用）
- **验证**：列表 total=219 排序正确、重置/重检成功、非法 ids 400

#### 5. 代码落位（已实施部分）

```
server/src/controllers/statsController.js   # getHealthOverview 返回 issueList
web/src/views/admin/LinkManage.vue          # 健康状态筛选 + onMounted 强制 admin 刷新
web/src/views/admin/Dashboard.vue           # 异常链接清单区块 + issue-tag 样式
web/src/components/admin/settings/SettingsHealth.vue  # 巡检进度恢复 + 4 列布局
```

#### 4. 验证

- [x] 前端 `npm run build` 通过（多轮）
- [x] `GET /api/stats/links/health` 返回 `issueList`（排序正确、含 failStreak/tls）
- [x] 后端重启无报错，巡检调度器正常

---

## 十一、候选功能池

> 已迁移至独立文档：[候选功能池.md](./候选功能池.md)

---

## 十二、部署前配置检查（必读）

> 登记时间：2026-08-10（部署前系统测试 S1 严重项）
> 本地开发无需配置以下项；**正式部署到公网前必须按此清单配置**，否则服务会拒绝启动或裸奔上线。

### 12.1 环境变量清单（server/.env）

| 变量 | 开发模式 | 生产模式（部署时必设） | 说明 |
|------|---------|----------------------|------|
| `NODE_ENV` | 不设（默认 development） | `production` | 激活 CORS 白名单、错误脱敏、JWT 密钥强度校验 |
| `JWT_SECRET` | 默认值即可 | ≥32 位随机串 | 签名登录/保险库 token 的唯一密钥；生产用默认值会拒绝启动 |
| `CORS_ORIGIN` | 不设（全开） | 前端域名，多个逗号分隔 | 生产缺此变量会拒绝启动 |
| `TRUST_PROXY` | 不设 | 反代部署时 `loopback` | 防限流按同一 IP 计数误锁全站 |
| `ADMIN_USERNAME/ADMIN_PASSWORD` | 默认 admin/admin123 | 改强密码 | 首次启动自动创建管理员 |
| `PORT` | 8000 | 8000 | 保持统一 |

### 12.2 正确配置示例

```ini
PORT=8000
NODE_ENV=production
JWT_SECRET=<随机32位以上，node -e "console.log(require('crypto').randomBytes(32).toString('hex'))" 生成>
CORS_ORIGIN=https://nav.yourdomain.com
TRUST_PROXY=loopback   # 仅当走 nginx/frpc 反代时设置
ADMIN_USERNAME=admin
ADMIN_PASSWORD=<强密码>
```

### 12.3 部署前提醒项（测试报告 S1 + 其他必办）

1. **环境配置**：按 12.2 配齐，重启后应看到 `[启动][CORS] 生产环境已收紧` 日志
2. ~~SSRF 收敛~~：~~`GET /api/links/fetch-title` 与 `GET /api/favicon?domain=` 需加内网/回环地址黑名单与 hostname 校验~~ ✅ **已解决（2026-08-10）**：新增 `server/src/utils/ssrfGuard.js`（`isPublicHostname`）统一校验，fetch-title 重定向逐跳校验、favicon 经 `isValidPublicDomain` 拒绝 IP/端口/路径/IP 直连；见测试报告 M3/M4
3. ~~字体 CDN~~：~~`fonts.loli.net` 国内不可达~~ ✅ **已解决（2026-08-10）**：字体已本地化（`lxgw-wenkai-webfont` + `@fontsource/noto-serif-sc`，动态 import，woff2 从本站加载），部署无需再处理字体
4. **备份**：`server/data/cozy-nav.db` + `server/data/favicons/` 目录一并备份（系统已内置每日 03:00 自动备份 #07，含 Git 异地可选，配置见数据管理页）
5. **pm2 自启**：`pm2 save && pm2 startup`；确认 `.env` 被 `.gitignore` 忽略（已确认）
6. **管理员登录密码**：部署后在后台改密，确保非 admin123

> 详细问题清单见 [部署前测试报告.md](./部署前测试报告.md)

---

## 十三、部署测试报告（2026-08-10 摘要）

> 完整版见 [部署前测试报告.md](./部署前测试报告.md)；本节为同步进开发方案的摘要，含结论、问题清单、修复状态与上线前动作。

### 13.1 测试结论

| 维度 | 结果 | 说明 |
|------|------|------|
| 后端 API 功能 | ✅ 55 通过 / 4 失败 | 4 项中 2 项为真实问题、2 项为文档与实现差异（均已修复） |
| 前端前台功能 | ✅ 17 项全部通过 | 首页/搜索/置顶/主题/字体/响应式 |
| 前端后台功能 | ✅ 全流程通过 | Dashboard/分类/书签/引擎/设置/健康检测 |
| 安全测试 | ⚠️ 无高危漏洞 | SSRF（M3/M4）已修复；剩余 1 项部署配置缺失（S1，严重，部署前须配置） |
| 性能测试 | ✅ 达标 | 首页 load 315ms（dev）；生产构建成功（24.86s） |
| 兼容性/响应式 | ⚠️ 部分覆盖 | PC 1440 / 移动 616 通过；Firefox/Safari/真机未实测 |

**问题总数 16 项**（严重 1 / 一般 4 / 轻微 11）；除 S1（部署配置）外，M1-M4 与 L1-L11 已全部修复/复核。

### 13.2 问题清单与修复状态

#### 🔴 严重（部署阻塞项）

| # | 问题 | 状态 |
|---|------|------|
| S1 | 部署环境配置不完整：`.env` 缺 `NODE_ENV=production`、缺 `CORS_ORIGIN`，`JWT_SECRET` 为默认值 | ⚠️ **部署前必须配置**（见第十二章）；正确配置后生产防护自动激活 |

#### 🟠 一般（已全部修复）

| # | 问题 | 修复 |
|---|------|------|
| M1 | `PUT /api/links/:id` 校验强制 `name.notEmpty()`，与控制器部分更新矛盾 | ✅ `name` 改 `optional().notEmpty()` |
| M2 | 字体 CDN（fonts.loli.net）国内不可达 | ✅ 字体本地化（`lxgw-wenkai-webfont` + `@fontsource/noto-serif-sc` 动态 import，woff2 从本站加载） |
| M3 | SSRF：`GET /api/links/fetch-title` 可探测内网 | ✅ `query('url').isURL()` + `ssrfGuard.isPublicHostname` 逐跳校验 + redirect 改 manual |
| M4 | SSRF：`GET /api/favicon?domain=` 公开无鉴权可触发任意出站 | ✅ `isValidPublicDomain` 拒绝 IP/端口/路径/IP 直连 + DNS 内网黑名单 |

#### 🟡 轻微（已全部修复/复核）

| # | 问题 | 修复 |
|---|------|------|
| L1 | 导出 HTML 分类名未转义 | ✅ 分类名过 `escapeHtml` |
| L2 | 业务错误拼接 `e.message` 泄漏内部细节 | ✅ 统一固定文案，细节仅写日志 |
| L3 | import overwrite 策略物理 DELETE | ✅ 改为 `UPDATE SET deleted_at` 软删除 |
| L4 | 导入导出接口无 express-validator | ✅ 补 validator（strategy ∈ {skip,overwrite}、html 非空） |
| L5 | 错误密码返回 400 而非 401 | ✅ 最终方案：HTTP 200 + 业务码 401（与主流网站一致） |
| L6 | fetch-title 对 GBK 页面标题解析失败 | ✅ `decodeHtml` 按 Content-Type/meta/BOM 检测编码，TextDecoder 支持 GBK |
| L7 | `GET /api/links` 无列表路由返回 404 HTML | ✅ 全局 404 对 `/api` 返回统一 JSON |
| L8 | ECharts 缺 `LegacyGridContainLabel` 插件 | ✅ Dashboard.vue 注册 echarts/features 插件 |
| L9 | naive-ui 弹窗关闭偶发失效 | ✅ 复核为自动化 artifact，非真实 bug |
| L10 | 主题切换背景 transition 滞后 | ✅ 确认非 bug（`.4s` 过渡为刻意设计） |
| L11 | 开发方案文档与实现差异（字段名/路由前缀） | ✅ 已同步开发方案第五章 |

### 13.3 已落地的工程优化（测试驱动）

| 优化点 | 说明 |
|--------|------|
| 分页 + 批量删除逻辑抽取 | `composables/usePagination.js` + `useBatchOps.js`，CategoryManage/LinkManage 复用 |
| favicon 抓取后端合并 | 共用 `fetchFaviconByDomain`，URL 版与 ID 版复用同一套流程 |
| 字母头像渲染统一 | 抽取 `components/ui/FaviconAvatar.vue`，PC 表格与移动端卡片复用 |
| Dashboard 体积优化 | echarts 按需引入 + 独立 vendor 分包（echarts-vendor 570KB/gzip 197KB 懒加载）；Dashboard chunk 518.78 → **13.46 kB** |

### 13.4 上线前动作（来自测试报告）

1. 按第十二章配置 `.env`（S1），重启后确认日志出现「生产环境已收紧」
2. 管理员登录后改强密码（确保非 admin123）
3. 建议抽测 Firefox/Safari/iOS/Android 真机（兼容性未覆盖项）
4. 公网低带宽下 Dashboard 首屏略慢（echarts-vendor 懒加载），可接受

---

## 十四、文档更新日志

| 日期 | 版本 | 更新内容 |
|---|---|---|
| 2026-08-09 | v1.0 | 初稿：六步开发计划 + 页面/API/数据设计 |
| 2026-08-10 | v1.1 | 登记开发点 #01-#08、#11；新增第十二章部署配置、第十三章部署测试报告；同步 #02 URL 判重、#07 自动备份实施 |
| 2026-08-11 | v1.2 | 本次全面修订：①技术栈/架构图更正为 `node:sqlite`；②第四章 DDL 补齐 `health_config/notification_config/backup_config`、`fail_streak/tls_expires_at`、`patrol_reports` 表；③第五章 API 表修正备份接口（config/run/list）、补 /health、/cold、patrol/reports、issues、batch-check、health/reset、notify-test 状态与新增；④第六章前端结构补齐 admin/ 组件树、DataManage/HealthMonitor/NotifyCenter 页面、backup.js、useTrashTable/useRenderCell、admin-common.css，移除已不存在的 Login.vue；⑤第七章补齐定时巡检/通知中心/自动备份/历史报告/异常批量/扩展条目；⑥第八章补 #09/#10 登记、#01 二期状态、修复 #07 乱码、完成度 32→34 项；⑦第十章巡检间隔默认值同步为 24/48/72/168；⑧品牌名统一为「悦行」 |
| 2026-08-11 | v1.3 | 排序体系：①第四章补 `category_sort_mode/link_sort_mode` 两列；②8.5 补「多模式排序」功能（前台 TopBar 下拉分组切换 + CategoryGrid computed 排序 + 后台表格列头 sorter）；③新建/导入未填排序权重自动 `MAX(sort_order)+1` 排末尾，同权重 `id ASC` 兜底；④书签/分类接口返回 `created_at` 支撑按添加时间排序；⑤新增 `useSort.js` composable；⑥第五章 API 表 /categories 与 /links 不再逐列标注，`GET /api/categories` 公开返回含 created_at |
| 2026-08-11 | v1.4 | 本轮（2026-08-11）新增功能合入：①设置页卡片折叠（CollapsibleCard 组件，localStorage 记忆）；②书签图标上传（createImageBitmap 解码期缩放 64px，512KB 限制，`up-` 前缀改名）；③外观设置合并（3 卡片→1 卡片「🎨 外观设置」+ 分段切换：外观主题/默认主题/自定义配色）；④基本+前台合并为「🌐 站点设置」+ 保险库+改密合并为「🔐 安全设置」（SettingsSite/SettingsSecurity 新建，Settings.vue 容器精简为 3 卡片）；⑤自定义配色外观联动（亮色模式锁定编辑亮色套，暗色锁定暗色套，auto 自由切换）；⑥视图布局开关（后台设置默认布局 + 前台手动切换仅本地生效，清除浏览器数据后回落默认）；⑦前台排序开关（后台控制排序按钮显隐，关闭时全站统一自定义顺序）；⑧分类上移/下移（全量重新编号 sort_order=1..N，根除导入数据重复值导致移动无效 bug）；⑨书签管理名称点击新标签页打开（renderAvatar 改为 `<a target="_blank">`，书签管理/链接巡检/回收站三处同步）；⑩分类移动后前台即时生效（全量重新编号保证 sort_order 唯一） |

