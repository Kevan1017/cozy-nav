# 悦行（cozy·nav）使用说明

> 版本：v1.0.3 · 更新日期：2026-08-12
> 本说明覆盖前台、后台、浏览器扩展的全部功能，按「快速上手 → 前台 → 后台 → 扩展 → 常见问题」编排。

---

## 目录

- [一、系统简介](#一系统简介)
- [二、快速上手](#二快速上手)
- [三、前台使用说明](#三前台使用说明)
- [四、后台使用说明](#四后台使用说明)
- [五、浏览器扩展使用说明](#五浏览器扩展使用说明)
- [六、常见问题](#六常见问题)

---

## 一、系统简介

悦行是一款个人导航首页系统，支持：

- **前台**：个性化首页——搜索、置顶书签、分类导航、多主题、多布局、字体切换、节日彩蛋
- **后台**：分类/书签管理、搜索引擎管理、链接巡检、数据导入导出与自动备份、通知推送、外观/安全设置
- **浏览器扩展**：一键收藏网页（弹窗 + 右键菜单）

访问地址：
- 正式版：`https://yue.lrevan.top`（后台 `/admin`）
- 演示版：`https://test1.lrevan.top`（后台 `/admin`，测试账号 admin / admin123）

---

## 二、快速上手

1. **首次访问前台**：打开首页即可看到搜索框、置顶栏、分类导航。数据为空时先进入后台创建分类和书签。
2. **进入后台**：前台右上角点「⚙️ 设置」→ 弹出登录框 → 输入管理员账号密码（默认 admin，密码见部署 `.env`，首次建库生效）。
3. **添加分类**：后台 →「分类管理」→「新增分类」，填写名称、副标题、选择 Emoji 与背景色。
4. **添加书签**：后台 →「书签管理」→「新建书签」，填写名称、URL、选择分类。
5. **使用扩展收藏**：安装扩展 → 配置服务器地址 → 登录 → 在任意网页右键「收藏到悦行」或点扩展图标收藏。
6. **个性化首页**：后台「网站设置」→「外观设置」选择主题与自定义配色，前台顶栏可切换亮暗、布局与字体。

---

## 三、前台使用说明

前台首页自上而下分为：顶部栏、Hero 区、搜索框、置顶栏、分类快捷导航、分类网格、页脚。

### 3.1 顶部栏（TopBar）

顶部栏包含品牌标识（Logo 或首字）、站点名称、副标题、日期胶囊和右侧工具栏。

| 功能 | 说明 |
|---|---|
| 主题切换按钮 ☀️/🌙/⏰ | 循环切换亮色 → 暗色 → 跟随时间（19:00-07:00 自动暗色） |
| 视图切换按钮 | 循环切换三种布局：标准视图（3 列大卡片）/ 列表视图（单列通栏）/ 紧凑视图（高密度） |
| 字体切换按钮 Aa | 循环切换：黑体（思源黑体）→ 楷体（Ma Shan Zheng）→ 宋体（思源宋体） |
| 排序下拉 | 分类与书签分别独立排序：自定义顺序 / 添加时间（新→旧、旧→新）/ 名称（A→Z、Z→A） |
| 设置入口 ⚙️ | 已登录直接进入后台；未登录弹出登录框（居中弹窗） |

> 工具栏按钮是否显示受后台开关控制（字体切换、视图布局、前台排序），见「后台 → 网站设置 → 前台显示」。

### 3.2 Hero 区域

- **时段问候**：根据当前时间自动问候（如「👋 下午好」），显示在标题上方。
- **大标题**：默认「嘿，今天想去哪里逛逛？」，「哪里」可被强调词高亮。
- **手写标语**：手写风格副标语，默认「— 你的小角落，随时出发 ♡」，可在后台自定义。
- **统计卡片**（右上角）：
  - 站点块：显示书签总数（两位补零，如「128」）
  - 分类块：显示分类总数（如「06」）
  - 倒计时块（可选）：开启节日倒计时后显示距最近节日天数（如「距 七夕」）
  - 底部标语：默认「— everything in its place —」，可在后台自定义

### 3.3 搜索框

- 输入关键词后**实时搜索**本地书签，下拉显示匹配结果（名称高亮、域名灰显）。
- 支持**键盘导航**：`↓`/`↑` 移动选择，`Enter` 打开选中项，`Esc` 关闭。
- 搜索框左侧可**切换搜索引擎**（后台配置），点击「出发」或回车使用所选引擎跳转搜索（结果页在当前窗口打开）。

### 3.4 置顶书签栏（PinStrip）

- 显示后台置顶的书签（最多 6 个），横滑排列，带序号（01、02…）。
- 点击卡片直接打开；头像优先显示网站 favicon，加载失败降级为字母头像。
- 无置顶时显示占位提示「暂无置顶书签」。

### 3.5 分类快捷导航（仅 PC ≥1024px）

顶部下方一条横向快捷导航，点击可跳转到对应分类卡片位置。

### 3.6 分类网格

分类卡片按视图模式渲染：

- **标准视图**：每行 3 个大卡片，卡片内书签条目 2 列排列。
- **列表视图**：分类单列通栏，书签条目多列密排；书签超过阈值（桌面 32 条 / 移动 10 条）自动折叠，点击「展开」查看全部。
- **紧凑视图**：3 列高密度，隐藏域名、缩小间距。

卡片元素：分类 Emoji + 名称 + 副标题 + 数量胶囊 + 书签列表（favicon + 名称 + 域名）。

- **加密分类**：显示「该分类已加密」锁定占位，点击弹出保险库密码框，解锁后显示内容。
- **加密书签**：显示锁定条目，解锁后正常显示。

### 3.7 页脚

版权信息（`Copyright ©2026 悦行`，年份自动跟随）+ 当前版本号（如 v1.0.3）+ 手写签名「一个软乎乎的小角落 ♡」。

### 3.8 其他

- **节日彩蛋**（仅 PC 非触摸屏）：春节灯笼🏮、圣诞雪花🎄、中秋月亮🌕、万圣蝙蝠🎃 等节日装饰自动飘落（开关见后台）。
- **返回顶部**：右下角悬浮按钮。
- **背景装饰**：blob 光斑 + 毛玻璃效果，跟随主题变化。

---

## 四、后台使用说明

登录入口：前台右上角 ⚙️ → 登录。或直接访问 `/admin`，未登录会自动弹出登录框。

后台布局：PC 左侧固定侧边栏导航；移动端顶部汉堡按钮展开抽屉菜单。

### 4.1 页面概览（Dashboard）

- **6 张统计卡**：书签总数 / 分类总数 / 置顶数 / 近 7 天新增 / 总访问量 / 今日访问。
- **近 7 天访问趋势**折线图，右上角显示「较上周 +x%」对比。
- **本周热门 TOP5**：访问量最高的 5 个书签榜单。
- **链接健康概览**：正常 / 需代理 / 打不开 / 死链 / 跳过数量汇总，异常链接可点击跳转书签管理定位。

### 4.2 分类管理

- **新增/编辑分类**：名称（必填）、副标题、Emoji（可随机 🎲）、背景色（10 种预设，可随机）、排序权重（留空自动排末尾）。
- **删除**：软删除进回收站；批量删除（勾选多条）；删除时返回被删除的链接数量。
- **回收站**：查看已删除分类，支持恢复 / 彻底删除 / 批量操作。分类软删除会级联软删除其下书签。
- **加密**：为分类开启加密，前台需要保险库密码解锁才能查看（保险库需先在设置中启用）。

### 4.3 书签管理

- **筛选栏**：按分类、关键词、健康状态（正常/需代理/打不开/死链/跳过/未检测）、置顶状态筛选。
- **新增/编辑书签**：名称、URL、所属分类、备注（仅后台可见）、加密开关。
  - URL 自动判重（规范化处理：去协议差异/尾斜杠/默认首页/跟踪参数等），重复时提示。
  - 保存后**异步抓取 favicon**，不阻塞响应；URL 变更会清空旧图标重新抓取。
- **置顶**：最多 6 个，超出提示「请先取消其他置顶」；置顶按钮 + 指定排序。
- **批量检测**：对选中书签发起健康检测。
- **批量删除**：勾选多条后删除（软删除进回收站）。
- **回收站**：恢复 / 彻底删除 / 批量操作。
- **移动端**：卡片列表展示，每个卡片底部操作栏含复选框 + 编辑/删除按钮；仅全选后显示「删除选中」。

### 4.4 搜索引擎管理

- **引擎列表**：名称、短标签、启用状态、排序。
- **新增/编辑引擎**：名称、短标签（如 G/B/AI）、唯一标识 key、URL 模板（必须包含 `{q}` 占位符，前后端双重校验）、配色（10 种）、排序权重。
- **启用/禁用**：禁用后前台搜索框不显示该引擎。
- **拖拽排序**：调整前台展示顺序。
- **前台展示数量**：设置前台搜索框显示几个引擎（如 3）。
- **删除**：至少保留一个引擎。

### 4.5 数据管理

#### 导出 / 导入

| 操作 | 说明 |
|---|---|
| 导出 JSON | 完整导出分类 + 书签 + 设置（JSON 文件） |
| 导出浏览器书签 | 导出为标准浏览器书签 HTML 文件，可导入 Chrome/Firefox/Edge |
| 导入 JSON | 导入悦行 JSON 备份 |
| 导入浏览器书签 | 导入浏览器导出的书签 HTML |

导入策略可选：**跳过重复**（推荐）/ **覆盖现有**。文件大小限制 2MB。

#### 自动备份

- **本地自动备份**：每天 03:00 生成数据库 + favicons 一致快照，保留最近 N 份（默认 5）。
- **Git 异地备份**：本地快照成功后自动推送到私有 Git 仓库（推荐 Gitee），页面内置分步引导 + 一键复制命令。
- **立即备份**按钮 + 最近备份记录（时间 / 大小 / Git 推送状态）。

### 4.6 链接巡检

- **巡检配置**：
  - 巡检开关（默认开启）
  - 巡检间隔：24h / 48h / 72h / 每周
  - 每轮检测条数（20-500）
  - 判死阈值：连续失败 N 次标记死链（默认 3）
  - 死链重检开关
  - TLS 预警：证书到期 30 天黄警 / 7 天红警
  - 冷链接天数：超 N 天未访问标记冷链接（默认 90）
- **立即巡检**：手动触发全量检测，实时进度条 + 完成统计。
- **异常处理**：列出打不开 / 死链 / TLS 预警链接，支持定位、编辑、删除。
- **历史巡检报告**：巡检趋势图 + 报告列表 + 每次异常明细。

> 检测状态：🟢正常 / 🟡需代理 / 🔴打不开 / 💀死链 / ⚪跳过 / ❔未检测。

### 4.7 通知中心

- **推送渠道**（怎么发，全局一份）：
  - **Server酱**：微信推送，填写 SendKey。
  - **QQ 邮箱**：SMTP 发送，填发件邮箱 + 授权码 + 服务器 + 收件邮箱（端口 465/587 可选）。
  - 可同时启用双渠道，支持「发送测试」。
- **推送事件**（什么时候发、发什么）：
  - 巡检结果：巡检完成 / 出现死链 / TLS 预警时推送。
  - Git 备份结果：备份成功 / 失败时推送。

> 任一渠道配置完整且开启后即可接收事件通知。

### 4.8 网站设置

设置页包含四张卡片，均可折叠。

#### ① 站点设置
- **基本配置**：网站标题、站点 Logo（PNG/JPG/WebP ≤300KB，可更换/移除/撤销）、站点关键词（逗号分隔，写入 meta）、站点描述（写入 meta）。
- **前台显示**：
  - 字体切换功能（开启后前台显示 Aa 按钮）
  - 节日彩蛋（春节灯笼/圣诞雪花/中秋月亮等装饰）
  - 节日倒计时（统计卡显示距最近节日天数）
  - 闲置书签标记（超 30 天未访问显示红色时钟角标，悬浮提示天数）
  - 视图布局（前台布局切换按钮；手动切换仅当前浏览器生效）
  - 前台排序（前台排序下拉）
  - 默认布局（标准/列表/紧凑，未手动切换或清除数据后回落此布局）
  - 统计卡标语（默认「— everything in its place —」，宽度上限 50）
  - 自定义 Hero 标语（默认「— 你的小角落，随时出发 ♡」，宽度上限 50）

#### ② 外观设置
- **外观主题**：亮色 / 暗色 / 跟随时间（与前台顶栏按钮同步）。
- **默认主题**：9 套配色方案，每套含亮/暗变体——
  琥珀青松、薄荷清凉、暮色橙橘、深海静谧、原木森野、葡萄紫韵、奶油拿铁、极光星夜、莫兰迪灰。
- **自定义配色**：逐项微调所有 CSS 变量（页面背景 / 顶栏 / Hero / 统计卡+搜索 / 置顶栏 / 分类卡 / 书签条目 / 页脚），每项含色板回显与「还原」基线。
  - 统计卡三块（站点块 / 分类块 / 倒计时块）在每个主题下颜色互不重复。
  - 外观主题为亮/暗时只能编辑对应那套；为跟随时间时可分别调整亮暗两套。

#### ③ 安全设置
- **登录密码**：修改管理员登录密码（需原密码，新密码至少 8 位，建议字母+数字）；修改成功后旧 Token 立即失效，自动退出重新登录。
- **保险库**（默认关闭）：
  - 功能开关：开启后可使用加密；**关闭会清除所有分类和书签的加密状态**（有确认弹窗）。
  - 设置密码：首次设置（≥8 位，建议字母+数字），成功后自动开启并解锁。
  - 解锁 / 锁定：解锁后可查看加密内容；锁定后立即隐藏。
  - 修改密码：修改后旧 Token 立即失效，需重新解锁。
  - 忘记密码：输入管理员登录密码验证后重置（不丢失加密数据）。

#### ④ 关于悦行
- 当前版本号（如 v1.0.3）、Git 提交、提交日期、运行环境（生产/开发）。
- **更新记录**：记录每次版本修复内容，支持新增/删除；新增时自动带出当前版本号与最近一次 Git 提交标题。

---

## 五、浏览器扩展使用说明

扩展功能：**一键收藏当前网页 / 任意链接到悦行**。

### 5.1 安装与首次配置

1. Chrome/Edge → 扩展程序 → 开发者模式 → 加载已解压的扩展程序 → 选择 `extension` 目录。
2. 点击扩展图标，弹出「设置」界面：
   - 填写**服务器地址**：正式版 `https://yue.lrevan.top`，演示版 `https://test1.lrevan.top`（生产用 https 域名）。
   - 点击「保存并继续」→ 进入登录界面，输入管理员账号密码登录。
3. 登录成功后进入收藏界面，可直接开始收藏。

> **切换服务器**：点弹窗底部「设置」可改地址。切换后旧环境的登录 Token 自动清除，需用新环境账号重新登录（各环境 JWT_SECRET 不同，旧 Token 无法跨环境使用）。

### 5.2 弹窗收藏

- 自动回填当前标签页的 URL 与标题。
- 选择分类（下拉列表），填写备注（可选）。
- 点击「收藏」；若地址已存在，弹出「仍要收藏（允许重复）」按钮可强制添加。

### 5.3 右键菜单收藏

- 在任意网页或链接上右键 →「收藏到悦行」。
- 子菜单列出全部分类（每次右键自动刷新分类列表），点击直接收藏到该分类（一步完成）。
- 点击父菜单标题 → 收藏到最近使用的分类（兜底）。
- 收藏成功 / 失败 / 重复均有系统通知（通知中心提示）。

### 5.4 其他

- 扩展底部显示当前登录状态与「退出登录」按钮。
- 仅支持 http/https 页面收藏；未配置服务器或未登录时会引导完成配置。

---

## 六、常见问题

**Q1：修改了主题配色，前台没变化？**
保存后即时生效；若同时改了亮暗两套，请确认「外观主题」当前模式。浏览器强缓存可强制刷新（Ctrl+F5）。

**Q2：版本号为什么不更新？**
版本号接口有进程内缓存，服务器执行 `git pull` 后必须 `pm2 restart` 对应进程（版本号读取 `package.json` + Git 提交信息）。

**Q3：保险库关闭后加密数据会丢吗？**
不会丢。关闭只清除加密状态（所有分类/书签恢复公开），数据本身保留；重新开启后可再次加密。

**Q4：忘记保险库密码怎么办？**
后台 → 安全设置 → 保险库 →「忘记密码」，输入管理员登录密码验证后设置新密码（数据不丢失）。

**Q5：导入书签显示失败？**
确认文件 ≤ 2MB，且为悦行 JSON 或标准浏览器书签 HTML；导入策略建议先选「跳过重复」。

**Q6：扩展收藏提示「已收藏过」？**
该 URL 已存在于书签库（按规范化 URL 判重），点击「仍要收藏（允许重复）」可强制添加。

**Q7：链接显示 ⚠️ 打不开/死链？**
后台 → 链接巡检可查看异常明细并「立即巡检」复测；需代理的站点（如 Google）标记为「需代理」而非死链。

**Q8：演示版数据被试用用户改乱了？**
演示版有定时清库计划任务，会重置为出厂演示数据（包含后台所有设置，如需保留主题等自定义配置，请先导出）。

---

*本说明随系统迭代更新，如有出入以实际界面为准。*
