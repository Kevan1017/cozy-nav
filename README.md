# 悦行 · cozy·nav

> 一款温暖风格的个人书签导航站：一站式收藏、搜索、巡检、统计与备份，提供 Web 管理界面与浏览器扩展（Chrome/Edge），帮助高效管理和整理网络书签。

## ✨ 主要特性

### 🌐 智能书签管理
- **快速保存** - 浏览器扩展一键保存当前页面为书签，右键菜单可直接选分类收藏
- **批量添加** - 多行粘贴一键批量创建书签，自动识别多种格式，重复自动跳过
- **分类管理** - 分类 + 置顶 + 排序，支持软删除与回收站恢复；近 500 个 Emoji 全部配中文名，选择器支持名称模糊搜索（如「动物」「导航」）与滚动懒加载
- **智能去重** - URL 规范化判重，重复收藏自动提示
- **导入导出** - 支持浏览器书签 HTML 与 JSCN 格式导入导出，导入后显示结果报告（新建/新增/更新/跳过统计）

### 🔍 搜索与导航
- **多搜索引擎** - 内置多种搜索引擎，支持后台自定义与前台切换
- **置顶快捷栏** - 常用书签置顶，一键直达
- **结果高亮** - 站内搜索关键词高亮（安全转义，防 XSS）

### 🛡️ 健康监测
- **链接可用性检查** - 直连 + DoH 双判定，识别打不开 / 疑似被墙
- **HTTPS/SSL 检测** - 监控证书到期时间，提前预警
- **告警通知** - 通过 Server酱 / 邮件推送巡检异常

### 📊 访问统计
- **访问趋势** - 近 N 天访问量折线图
- **热门 TOP** - 高频访问链接排行
- **冷链接分析** - 识别 30 天未访问的闲置书签

### 🔒 数据安全
- **保险库** - 分类 / 书签独立加密锁定，独立密码
- **自动备份** - 本地备份 + 坚果云 WebDAV 云端备份（增量指纹跳过，仅传数据库与 Logo，云端按保留份数自动清理）
- **操作日志** - 登录、增删改、导入导出、备份、巡检等关键操作全留痕，后台按模块筛选检索
- **软删除** - 删除先进回收站可恢复，彻底删除才物理清除

### 🎨 个性化定制
- **多主题预设** - 内置多套配色，亮 / 暗 / 自动三种模式
- **自定义配色** - 后台可视调整并固化为预设
- **字体切换** - 黑体 / 楷体 / 宋体一键切换
- **多视图布局** - 卡片 / 列表 / 紧凑三种模式
- **常用与闲置标记** - 常用书签条目左侧金色竖条、闲置链接名称右侧内联时钟图标（30 天未访问），开启开关后自动标记
- **无图模式** - 一键隐藏网站图标与头像，纯文字卡片更简洁省流量
- **节日彩蛋** - 春节灯笼、圣诞雪花等节日装饰
- **双端适配** - PC 与移动端完美适配

## 🏗️ 项目结构

```
cozy-nav/
├── extension/          # 浏览器扩展 (Chrome/Edge, Manifest V3)
│   ├── manifest.json   # 扩展配置
│   ├── popup.html      # 弹出界面
│   ├── popup.js        # 界面逻辑
│   ├── popup.css       # 弹出界面样式
│   ├── background.js   # 后台脚本（右键菜单收藏）
│   ├── common.js       # 扩展共用逻辑（API 封装 / 鉴权）
│   ├── gen-icons.ps1   # 图标生成脚本
│   └── icons/          # 图标资源
├── server/             # 后端服务 (Express + node:sqlite)
│   ├── src/
│   │   ├── app.js          # Express 应用入口
│   │   ├── controllers/    # 业务控制器
│   │   ├── routes/         # API 路由
│   │   ├── middlewares/    # 中间件（鉴权 / 校验 / 错误处理）
│   │   ├── utils/          # 工具函数（巡检 / favicon / 备份等）
│   │   └── db/             # 数据库初始化与迁移
│   └── .env.example        # 环境变量示例
└── web/                # 前端应用 (Vue 3 + Vite)
    ├── src/
    │   ├── api/            # API 接口封装
    │   ├── components/     # Vue 组件
    │   ├── views/          # 页面视图（前台 + 后台）
    │   ├── stores/         # Pinia 状态管理
    │   ├── composables/    # 组合式函数
    │   ├── theme/          # Naive UI 主题与配色预设
    │   ├── utils/          # 工具函数
    │   └── styles/         # 全局样式（tokens.css 等）
    └── vite.config.js      # Vite 配置（含 /api 代理）
```

## 🚀 快速开始（本地开发）

### 环境要求

- **Node.js ≥ 22.13**（后端使用内置 `node:sqlite` 驱动，零数据库配置）
- npm（随 Node 自带）

### 1. 后端启动

```bash
cd server
npm install
cp .env.example .env        # Windows 用 copy .env.example .env
                            # 修改 .env 中的 PORT、JWT_SECRET；PORT 保持 8000 与前端代理一致
npm run dev                 # 开发模式，监听 http://localhost:8000
```

### 2. 前端启动

```bash
cd web
npm install
npm run dev                 # 开发模式，访问 http://localhost:5173
                            # /api、/favicons、/logo 已自动代理到 8000
```

### 3. 浏览器扩展安装

1. 打开 Chrome/Edge，访问 `chrome://extensions/`
2. 开启 **开发者模式**
3. 点击 **加载已解压的扩展程序**，选择 `extension` 目录
4. 点击扩展图标，服务器地址填 `http://localhost:8000`，登录一次即可使用

> 首次启动自动创建管理员账号（`.env` 中的 `ADMIN_USERNAME` / `ADMIN_PASSWORD`，默认 `admin` / `admin123`，**生产环境务必修改**）。

## 📦 生产部署

### 1. 环境变量（`.env`）

```env
# 服务端口
PORT=8000

# 运行环境：development / production（生产必须设为 production）
NODE_ENV=production

# JWT 密钥：生产环境改为至少 32 位随机字符串（可用 node -e "console.log(require('crypto').randomBytes(32).toString('hex'))" 生成）
JWT_SECRET=your-long-random-secret

# 允许跨域的来源（生产环境改为你的前端域名，多个用逗号分隔）
CORS_ORIGIN=https://nav.example.com

# 浏览器扩展 ID 白名单（可选，不配置则默认放行所有 chrome-extension:// 来源，扩展仍需 JWT 鉴权）
CORS_EXTENSIONS=

# 反向代理信任（经 nginx/frp 反代部署必须设置，否则限流按 127.0.0.1 计数，一人误触限流全站登录被锁）
TRUST_PROXY=loopback

# 管理员账号（仅首次启动建库时生效）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123   # ← 务必改成强密码

# 种子演示数据（可选，默认 true；设为 false 时纯净启动，不写入示例分类与书签）
# SEED_DEMO_DATA=false
# 演示实例开关（可选，仅独立演示实例使用；设为 true 时生产环境放行默认密码，严禁用于正式环境）
# ALLOW_DEMO_PASSWORD=true
```

### 2. 安装依赖并构建前端

```bash
cd server && npm install
cd ../web && npm install
npm run build               # 产物输出到 web/dist
```

### 3. 进程守护（pm2）

```bash
npm install -g pm2
cd server
pm2 start src/app.js --name cozy-nav
pm2 save && pm2 startup    # 开机自启
```

### 4. Nginx 反向代理示例

```nginx
server {
    listen 80;
    server_name nav.example.com;

    # 前端静态资源
    location / {
        root /www/wwwroot/cozy-nav/web/dist;
        try_files $uri $uri/ /index.html;
    }

    # 后端 API
    location ^~ /api {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # 静态资源（favicon 与 logo 图片）
    location ^~ /favicons { proxy_pass http://127.0.0.1:8000; }
    location ^~ /logo { proxy_pass http://127.0.0.1:8000; }
}
```

> 建议通过 certbot 等工具配置 HTTPS（后端会下发 HSTS 头）。

## 🔒 安全注意事项

部署到公网前请务必完成：

- [ ] `ADMIN_PASSWORD` 改为强密码（默认 `admin123` 仅限本地）
- [ ] `JWT_SECRET` 改为至少 32 位随机字符串
- [ ] `NODE_ENV=production`（隐藏内部错误信息）
- [ ] `CORS_ORIGIN` 配置为你的前端域名
- [ ] 经 nginx/frp 反代部署时设置 `TRUST_PROXY=loopback`（否则限流按 127.0.0.1 计数，误伤全站登录/解锁）
- [ ] 首次启动后登录后台，建议开启「保险库」并设置独立密码，加密敏感书签
- [ ] 系统内置 SSRF 内网黑名单（fetch 标题 / favicon 抓取均过滤内网地址）

## 🖥️ 浏览器扩展使用

1. 点击工具栏扩展图标，在「设置」中填写你的服务器地址（如 `https://nav.example.com`，不含 `/api`）
2. 登录一次（保存 JWT，之后无需重复登录）
3. 收藏方式：
   - **弹窗收藏**：点击图标 → 选择分类 → 保存
   - **右键收藏**：网页上右键 → 「收藏到悦行」→ 选择分类 → 一步完成

## 📦 更新记录
### v1.1.5（2026-08-14）
- ⭐ 常用书签标记由名称末尾内联星标改为条目左侧金色竖条，闲置标记由右上角时钟角标改为名称右侧内联时钟图标（颜色可在外观设置中调整），两者同屏显示互不遮挡
- 🎨 后台新增「外观设置」独立菜单：主题配色 + 前台显示设置集中管理；网站设置页瘦身为纯站点配置
- 🧭 后台菜单栏优化：退出登录移出菜单独立成底部按钮、导航项按「内容管理 / 数据维护 / 通知日志 / 设置」分组、全部改用 Lucide 线性图标
- 😀 Emoji 选择器升级：约 486 个 emoji 全部配中文名，支持名称模糊搜索（如「动物」「导航」）、新增「导航常用」分组，网格滚动懒加载 + 加载中提示
- 📱 移动端最近备份记录保持单行显示，大小与坚果云状态不再换行
- 🧹 数据管理页精简：导出数据按钮上方标题、测试连接 / 立即备份按钮图标去除
- 📥 导入加固：浏览器书签 HTML 统一解析（兼容 Chrome / Edge / Firefox / Safari，嵌套文件夹自动拍平为「父/子」分类、忽略 ICON / LAST_MODIFIED 等属性差异）、文件特征校验（非书签文件直接拒绝）、空数据不再返回假成功、JSON 深度校验
- 🐛 修复导入：文件特征校验失败时误报 500（事务未开启却执行回滚），现正确返回 400 具体原因
- 📖 导入弹窗新增 JSON 格式说明：标注字段要求与最小可用格式示例
- 📁 导入文件选择时即校验 2MB 上限并即时提示，避免传到后端才被 413 拦截
- 🧪 引入 vitest 单元测试：覆盖导入文件大小校验边界（恰好 2MB 放行 / 超限拦截）

### v1.1.4（2026-08-14）
- ⭐ 常用书签标记改版：由右上角星标角标改为链接名称末尾内联金色小星，名称超长省略时星标保持完整可见，不再遮挡链接文本（所有视图模式统一）

### v1.1.3（2026-08-14）
- 📜 操作日志大幅扩充：新增导出/备份（JSON/HTML 导出、立即备份含坚果云上传成败、备份配置变更、测试 WebDAV）、安全审计（登录失败区分账号不存在/密码错误、保险库解锁）、巡检与批量操作（立即巡检、批量重检/重置健康状态/移动书签）、开关与设置（置顶/常用标记/锁定、通知测试发送、Logo 上传/移除、设置保存变更项）等 19 个记录点；日志页新增「💾 备份」「🩺 巡检」模块筛选
- ☁️ 备份通知新增 `{webdav}` 坚果云占位符：按真实上传结果动态显示（成功含上传文件数 / 失败含具体原因 / 未启用），兼容旧模板自动追加云端状态
- 🐛 修复增量备份失效：指纹原按整个数据库文件计算（操作日志/巡检报告/访问统计等高频写入导致每次备份都触发），改为只统计核心业务表（admin/categories/links/search_engines/preferences/changelog）+ 站点 Logo，运行痕迹与 favicons 缓存不再影响增量

### v1.1.2（2026-08-14）
- ✏️ 书签弹窗改版：域名｜头像文字、排序权重｜常用标记并排布局；URL 自动解析删除改纯手动获取；输入自动补协议（`ensureProtocol`）
- ⏱️ 抓取标题超时优化（5s → 10s，与前端超时对齐），超时独立提示「站点响应超时，可能被墙或访问过慢」
- 🩺 巡检增强：8 个配置项新增小字说明，label 左对齐；修复进度条刷新后消失；修复巡检邮件标题 `{issues}` 取值为空
- 💾 最近备份记录区分「本地 / 坚果云」两组展示，云端查询成功/失败状态提示（含限流说明）
- ☁️ 坚果云备份放弃大小统计（递归 PROPFIND 触发 503 限流），仅保留名称列表

### v1.1.1（2026-08-13）
- 📐 新建/编辑书签弹窗布局优化：按「基本信息 / 外观 / 高级」三区分组，图标获取/上传/移除按钮归位到预览区，头像文字与颜色合并，备注/排序权重/常用标记折叠收纳

### v1.1.0（2026-08-13）
- 📜 新增操作日志：登录/增删改/导入导出/密码等关键操作留痕，模块筛选 + 关键字检索，保留最近 5000 条
- 📌 置顶板块开关与常用书签星标（开发点 #12）
- 📱 移动端筛选栏优化

### v1.0.5（2026-08-12）
- 📋 新增书签批量添加：多行粘贴（纯 URL / Markdown 链接等 5 种格式）一键批量创建书签，重复自动跳过并显示明细
- 📊 导入数据增加结果报告：导入完成后弹窗显示新建分类 / 新增 / 更新 / 跳过统计与明细
- 🚫 新增无图模式：后台开关，前台隐藏网站图标与字母头像，纯文字卡片更省流量
- ☁️ 自动备份新增坚果云 WebDAV 云端备份（替换原 Git 异地备份）：增量指纹跳过、仅传数据库与 Logo 省流量、云端按保留份数自动清理

### v1.0.4（2026-08-12）
- ✨ 新增书签批量移动分类：勾选一个或多个书签后，可一键移动到其他分类（PC 端与移动端均支持）
- 🎨 Emoji 图标库扩充至约 290 个：新增表情/手势、动物、植物自然、食物饮品、物品生活、旅行交通、运动活动、符号等分组
- 📌 置顶书签数量上限从 6 个提升至 8 个，一行显示正好适配网页布局
- 📄 新增《悦行使用说明.md》文档，覆盖前后台全部功能与常见问题

### v1.0.3（2026-08-12）
- ✨ 新增 3 套配色主题：极光星夜 / 奶油拿铁 / 莫兰迪灰（共 9 套主题，亮 / 暗各一）
- 🎨 整体优化各主题配色：统计卡三块（站点块 / 分类块 / 倒计时块）颜色在每个主题下互不重复，且贴合主题风格
- 🎛️ 自定义配色面板升级：「站点数字块」「分类数字块」更名为「站点块」「分类块」，新增「倒计时块」配色项（开启节日倒计时后可用）

### v1.0.2（2026-08-12）
- 修复测试/正式环境切换后旧Token失效导致收藏失败的问题
- 版本号自动填及更新记录

### v1.0.1（2026-08-12）
- 🐛 修复：新建/编辑书签时未自动解析域名，导致书签管理「域名」列空白（浏览器扩展收藏的链接受影响）
- 新增 `server/scripts/fix-link-domain.js` 一键修复脚本，可批量补全历史书签缺失的域名

### v1.0.0（2026-08-11）
- 🎉 首个正式版本：完整书签管理、多搜索引擎、链接巡检、访问统计、保险库、自动备份、多主题、浏览器扩展、双端适配

## 🤝 贡献

欢迎提交 Issue 或 Pull Request：

1. Fork 本项目
2. 创建功能分支（`git checkout -b feature/AmazingFeature`）
3. 提交更改（`git commit -m 'Add some AmazingFeature'`）
4. 推送分支并提交 Pull Request

## 📧 联系

如有问题或建议，请在本仓库提交 Issue。
