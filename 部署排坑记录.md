# 悦行（cozy·nav）正式部署排坑记录

> 记录正式部署过程中踩过的坑与解决方案，防止下次部署再出错。
> 更新日期：2026-08-11　适用版本：master（含 `ceb5056` 及之后）

---

## 目录

1. [依赖安装阶段](#一依赖安装阶段)
2. [后端启动阶段](#二后端启动阶段)
3. [Nginx / 宝塔配置阶段](#三nginx--宝塔配置阶段)
4. [环境与配置要点](#四环境与配置要点)
5. [部署检查清单](#五部署检查清单)

---

## 一、依赖安装阶段

### 坑 1：npm 镜像 404 装不上依赖

- **现象**：`npm install` 报 `404 Not Found ... cdn.npmmirror.com ... @fontsource/noto-serif-sc`，即使加 `--registry=https://registry.npmjs.org/` 也没生效
- **原因**：
  1. 服务器全局 registry 被宝塔设为 npmmirror，且 npmmirror 恰好缺该包的下载文件（镜像同步不全）
  2. `package-lock.json` 里的 `resolved` 地址锁死了镜像源，仅换 registry 无效
- **解法**：项目级 `.npmrc` 写官方源 + 强制替换下载地址：
  ```bash
  npm config set registry https://registry.npmjs.org/ --location=project
  npm install --registry=https://registry.npmjs.org/ --replace-registry-host=always
  ```
  兜底方案（彻底重建锁文件）：
  ```bash
  rm -rf node_modules package-lock.json
  npm install --registry=https://registry.npmjs.org/
  ```
- **预防**：clone 后、install 前先配置 `.npmrc`；`package-lock.json` 用官方源生成并提交，避免锁死镜像地址

### 坑 2：`.npmrc` 用 `echo >` 写不进去

- **现象**：`cat .npmrc` 无输出，文件不存在
- **原因**：宝塔终端粘贴多行命令时，部分行（含重定向 `>`）可能未执行
- **解法**：改用 npm 官方命令写配置：
  ```bash
  npm config set registry https://registry.npmjs.org/ --location=project
  npm config get registry    # 必须输出 https://registry.npmjs.org/ 才算生效
  ```
- **预防**：写任何配置文件后，必须 `cat` 或 `ls` 验证成功再继续下一步

---

## 二、后端启动阶段

### 坑 3：端口占用 `EADDRINUSE`

- **现象**：`Error: listen EADDRINUSE: address already in use :::8000`
- **原因**：重复执行 `pm2 start` 产生多实例；或手动 `node src/app.js` 试跑后未停止
- **注意**：后端 [app.js](server/src/app.js) 在监听端口**之前**就执行 `setupDatabase()`——即使端口冲突启动失败，数据库也已经按当时的 `.env` 建好了
- **解法**：
  ```bash
  # 1. 释放端口
  fuser -k 8000/tcp
  # 无 fuser 时：lsof -ti:8000 | xargs -r kill -9

  # 2. 确认已释放（无输出 = 成功）
  lsof -i:8000

  # 3. 清理 pm2 残留实例
  pm2 list
  pm2 delete yuehang-prod

  # 4. 干净启动
  cd /www/wwwroot/yuehang/server
  pm2 start src/app.js --name yuehang-prod --cwd /www/wwwroot/yuehang/server
  ```
- **预防**：启动前 `pm2 list` + `lsof -i:端口` 确认空闲；统一用 pm2 管理进程，不要手动 `node` 跑

### 坑 4：`.env` 占位符被原样填入

- **现象**：登录提示「用户名或密码错误」
- **原因**：把 `ADMIN_PASSWORD=<wenJING1017>` 的**尖括号占位符原样保留**，实际密码变成带尖括号的 `<wenJING1017>`；`JWT_SECRET` 也填成了一段说明文字
- **解法**：修正 `.env` 后删库重启：
  ```bash
  rm -f /www/wwwroot/yuehang/server/data/cozy-nav.db
  pm2 restart yuehang-prod
  ```
- **预防**：
  - 尖括号 `<>` 是占位符标记，**值里绝对不能留**
  - `JWT_SECRET` 必须真正执行生成命令：
    ```bash
    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
    ```

### 坑 5：`.env` 里的密码只在「首次建库」生效

- **现象**：改了 `.env` 的 `ADMIN_PASSWORD` 后登录仍失败
- **原因**：管理员密码在第一次启动建库（admin 表为空）时写入数据库，之后修改 `.env` **不会**同步到数据库
- **解法**：
  - 删除 `server/data/cozy-nav.db` 重启重建（注意：清空该实例所有数据）
  - 或登录后台「设置」里修改密码
- **预防**：部署时一次配好 `.env` 再首次启动；建库后改密码一律走后台

### 坑 6：`.env` 值末尾残留多余引号

- **现象**：`SEED_DEMO_DATA=false''`（末尾多两个单引号），纯净启动失效，意外写入演示数据
- **原因**：从文档复制粘贴时带入残留字符
- **预防**：`.env` 每行核对格式；改完 `.env` 重启后看启动日志确认配置实际生效值

### 坑 7：全新库启动 `UNIQUE constraint failed: search_engines.key`

- **现象**：首次全新建库时启动崩溃一次（pm2 自动重启后正常），日志含：
  ```
  Error: UNIQUE constraint failed: search_engines.key
      at seedEngines (.../db/index.js:171)
      at seedDatabase (.../db/index.js:238)
  ```
- **原因**：代码 bug——`migrateDatabase()` 先对空库补种了搜索引擎，`seedDatabase()` 又种一次 → 唯一键冲突。本地旧库升级不会触发，**只有全新建库会暴露**
- **解法**：已修复并推送（commit `ceb5056`）：`seedDatabase()` 中 `seedEngines()` 改为幂等写入（先查 `search_engines` 是否为空再种）
- **预防**：涉及种子数据的改动，发布前必须在**全新空库**上验证一次启动流程

---

## 三、Nginx / 宝塔配置阶段

### 坑 8：`"location" directive is not allowed here`

- **现象**：`nginx -t` 报错，且报错位置在配置文件第 1 行：
  ```
  nginx: [emerg] "location" directive is not allowed here in ...yue.lrevan.top.conf:1
  ```
- **原因**：把 location 块放在了 `server { }` **外面**（整个文件被替换成了 location 片段，破坏了宝塔生成的 server 结构）
- **解法**：保留宝塔生成的 `server` 块（含 `listen`、`server_name`、SSL 配置），只把 location 插入到 server 块内部，推荐位置在 `#REWRITE-END` 之后
- **预防**：编辑宝塔站点配置文件时**只动 server 块内部**；SSL 证书路径等宝塔管理内容不要删

### 坑 9：favicon / logo 反代被宝塔静态缓存规则拦截

- **现象**（潜在坑，务必预防）：`/favicons/*.png`、`/logo/*.png` 请求不进后端，直接 404
- **原因**：宝塔自带的图片缓存正则 location（`location ~ .*\.(gif|jpg|png)$`）**优先级高于**普通前缀 location，会拦截同名文件请求
- **解法**：反代 location 必须加 `^~` 修饰符，命中后跳过正则匹配：
  ```nginx
  location ^~ /favicons {
      proxy_pass http://127.0.0.1:8000;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  }
  location ^~ /logo {
      proxy_pass http://127.0.0.1:8000;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  }
  ```
- **预防**：nginx 里想覆盖宝塔默认正则规则的路径，一律用 `^~`

### 标准站点配置片段（可直接复用）

```nginx
# SPA 路由回退（刷新子页面不 404）
location / {
    try_files $uri $uri/ /index.html;
}

# 后端 API 反代
location ^~ /api {
    proxy_pass http://127.0.0.1:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_connect_timeout 10s;
    proxy_read_timeout 60s;
}
```

---

## 四、环境与配置要点

| 配置项 | 要求 | 说明 |
|---|---|---|
| Node 版本 | ≥ 22.13 | 使用 `node:sqlite` 原生模块；v24 LTS 已验证可用 |
| `NODE_ENV` | `production` | 生产必设，否则错误信息脱敏、CORS 收紧等不生效 |
| `JWT_SECRET` | ≥ 32 位随机串 | 必须执行生成命令，不能写占位符；两个实例用不同密钥 |
| `CORS_ORIGIN` | 填域名 | 生产环境不填会**启动失败**；多个域名逗号分隔 |
| `TRUST_PROXY` | `loopback` | nginx 反代下必须设，否则限流按 127.0.0.1 计数，一人误触全站锁死 |
| `ADMIN_PASSWORD` | ≥ 8 位字母+数字 | 生产环境禁止 `admin123`；演示实例需 `ALLOW_DEMO_PASSWORD=true` 放行 |
| `SEED_DEMO_DATA` | 自用 `false` / 演示 `true` | 决定是否写入示例分类与书签 |
| 后端端口 | 不对外放行 | 只开 80/443，nginx 用 `127.0.0.1` 反代 |
| 数据库路径 | `server/data/cozy-nav.db` | 首次启动自动建库；删库重启 = 重置该实例全部数据 |

---

## 五、部署检查清单

**后端**
- [ ] `curl http://127.0.0.1:端口/api/health` 返回 `{"code":200,...}`
- [ ] `pm2 list` 显示进程 `online`，日志无报错
- [ ] 日志出现 `[数据库] 种子数据写入完成`（或纯净启动提示）

**前端**
- [ ] `web/dist/index.html` 存在
- [ ] 浏览器 https 访问正常，F5 刷新不 404
- [ ] 后台登录成功（admin + `.env` 密码）

**安全**
- [ ] `JWT_SECRET` 为随机 32 位（非占位符）
- [ ] `.env` 文件无残留引号/占位符
- [ ] 端口 8000/8001 未对外开放
- [ ] favicon / logo 图标正常显示（验证 `^~` 反代生效）

**演示版（独立实例）**
- [ ] 端口、JWT_SECRET、数据库完全独立
- [ ] `ALLOW_DEMO_PASSWORD=true` + `ADMIN_PASSWORD=admin123` + `SEED_DEMO_DATA=true`
- [ ] 测试账号 `admin/admin123` 可登录后台
- [ ] 被改乱后 `rm -f server/data/cozy-nav.db` + 重启即可重置
